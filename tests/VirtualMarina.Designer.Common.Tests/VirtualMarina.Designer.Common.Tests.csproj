<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Tests for the logic the two Designer apps share: the session's file / dirty / title workflow, the rename checks,
    the activity log, the command table and the launcher's liveness watch. No UI, so it runs anywhere.
    The test framework, runner and coverage come from tests/Directory.Build.props.
  -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\apps\VirtualMarina.Designer.Common\VirtualMarina.Designer.Common.csproj" />
  </ItemGroup>

</Project>
