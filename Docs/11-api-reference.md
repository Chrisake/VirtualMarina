# API reference

Every public type and member of the VirtualMarina libraries, with the summary from its XML documentation.
Generated by `Docs/tools/ApiDocGen` (`dotnet run --project Docs/tools/ApiDocGen -- Docs/11-api-reference.md`); do not edit by hand.
Guides with examples are listed in the [documentation index](README.md).

- **VirtualMarina.Blazor**: [MarinaDesignerPanel](#marinadesignerpanel), [MarinaView](#marinaview), [WebGlSceneRenderer](#webglscenerenderer)
- **VirtualMarina.Core.Api**: [BatchUpdateError](#batchupdateerror), [BatchUpdateResult](#batchupdateresult), [BerthAction](#berthaction), [BerthActionCollection](#berthactioncollection), [BerthActionInvokedEventArgs](#berthactioninvokedeventargs), [BerthActionStyle](#berthactionstyle), [BerthEventArgs](#bertheventargs), [BerthHoverEventArgs](#berthhovereventargs), [BerthLabelMode](#berthlabelmode), [BerthLabelModeExtensions](#berthlabelmodeextensions), [BerthPopup](#berthpopup), [BerthPopupChangedEventArgs](#berthpopupchangedeventargs), [BerthPopupKind](#berthpopupkind), [BerthSelectedEventArgs](#berthselectedeventargs), [BerthSelectionRejection](#berthselectionrejection), [BerthStatusChangedEventArgs](#berthstatuschangedeventargs), [BerthTooltip](#berthtooltip), [BerthTooltipLine](#berthtooltipline), [BerthUpdate](#berthupdate), [DefaultPopupContent](#defaultpopupcontent), [DisplayNames](#displaynames), [IMarinaVisualizer](#imarinavisualizer), [LayoutChangeKind](#layoutchangekind), [LayoutChangedEventArgs](#layoutchangedeventargs), [MarinaLocalization](#marinalocalization), [MarinaStatistics](#marinastatistics), [MarinaVisualizer](#marinavisualizer), [MarinaVisualizerOptions](#marinavisualizeroptions), [MultiBerthSelectedEventArgs](#multiberthselectedeventargs), [PierUpdate](#pierupdate), [RejectedBerth](#rejectedberth), [SelectionChangedEventArgs](#selectionchangedeventargs), [SelectionReason](#selectionreason), [SelectionResult](#selectionresult), [StatusColorScheme](#statuscolorscheme)
- **VirtualMarina.Core.Camera**: [CameraAngle](#cameraangle), [CameraConstraints](#cameraconstraints), [CameraPose](#camerapose), [CameraPreset](#camerapreset), [OrbitCamera](#orbitcamera)
- **VirtualMarina.Core.Design**: [BerthNamingScheme](#berthnamingscheme), [BerthSeparator](#berthseparator), [DesignActionUndoneEventArgs](#designactionundoneeventargs), [DesignDraftChange](#designdraftchange), [DesignDraftChangedEventArgs](#designdraftchangedeventargs), [DesignElementCreatedEventArgs](#designelementcreatedeventargs), [DesignElementCreatingEventArgs](#designelementcreatingeventargs), [DesignElementErasedEventArgs](#designelementerasedeventargs), [DesignElementRenamingEventArgs](#designelementrenamingeventargs), [DesignTool](#designtool), [DesignToolChangedEventArgs](#designtoolchangedeventargs), [DesignTreesPlantedEventArgs](#designtreesplantedeventargs), [DesignerSettings](#designersettings), [MarinaDesigner](#marinadesigner), [ReferenceImage](#referenceimage), [ReferenceImageChange](#referenceimagechange), [ReferenceImageChangedEventArgs](#referenceimagechangedeventargs), [ScaleLineDrawnEventArgs](#scalelinedrawneventargs)
- **VirtualMarina.Core.Domain**: [Berth](#berth), [BerthGenerator](#berthgenerator), [BerthStatus](#berthstatus), [BerthStatusExtensions](#berthstatusextensions), [BerthStatusFilter](#berthstatusfilter), [Boat](#boat), [BoatDimensions](#boatdimensions), [BoatType](#boattype), [BoatTypeCatalog](#boattypecatalog), [Divider](#divider), [DividerType](#dividertype), [HinterlandScenery](#hinterlandscenery), [LandArea](#landarea), [LandAreaBuilder](#landareabuilder), [LandKind](#landkind), [LandTree](#landtree), [MarinaDataBag](#marinadatabag), [MarinaLayout](#marinalayout), [MarinaLayoutBuilder](#marinalayoutbuilder), [MarinaLayoutException](#marinalayoutexception), [MarineTraffic](#marinetraffic), [MooringStyle](#mooringstyle), [MultiBerth](#multiberth), [OrientedRect](#orientedrect), [Pier](#pier), [PierBuilder](#pierbuilder), [PierServices](#pierservices), [PierSide](#pierside), [PierSides](#piersides), [PierType](#piertype), [Shoreline](#shoreline), [TrafficVessel](#trafficvessel), [TreeShape](#treeshape)
- **VirtualMarina.Core.Geometry**: [BoatMeshFactory](#boatmeshfactory), [BoundingBox](#boundingbox), [GlyphFont](#glyphfont), [LabelFont](#labelfont), [LandMeshFactory](#landmeshfactory), [MarinaMeshFactory](#marinameshfactory), [MarineTrafficPlanner](#marinetrafficplanner), [MeshBuilder](#meshbuilder), [MeshData](#meshdata), [MeshIds](#meshids), [MeshLibrary](#meshlibrary), [TrafficLane](#trafficlane)
- **VirtualMarina.Core.Input**: [CameraDragAction](#cameradragaction), [InputModifiers](#inputmodifiers), [MarinaInputController](#marinainputcontroller), [MarinaKey](#marinakey), [PointerButton](#pointerbutton)
- **VirtualMarina.Core.Mathematics**: [MarinaMath](#marinamath), [PolygonMath](#polygonmath)
- **VirtualMarina.Core.Picking**: [BerthHit](#berthhit), [Ray](#ray)
- **VirtualMarina.Core.Rendering**: [ColorRgba](#colorrgba), [ISceneRenderer](#iscenerenderer), [LabelStyle](#labelstyle), [LandStyle](#landstyle), [LightingSettings](#lightingsettings), [MarinaStyle](#marinastyle), [ReferenceImageLayer](#referenceimagelayer), [RenderAnimation](#renderanimation), [RenderFrame](#renderframe), [RenderObject](#renderobject), [SelectionStyle](#selectionstyle), [ShaderDialect](#shaderdialect), [ShaderSources](#shadersources), [StructureStyle](#structurestyle), [StyleSection](#stylesection), [ViewStyle](#viewstyle), [WaterSettings](#watersettings)
- **VirtualMarina.Core.Serialization**: [MarinaDocument](#marinadocument), [MarinaFormatException](#marinaformatexception), [MarinaJson](#marinajson), [ReferenceImageRecord](#referenceimagerecord)
- **VirtualMarina.Rendering.OpenGL**: [OpenGlSceneRenderer](#openglscenerenderer)
- **VirtualMarina.WinForms**: [MarinaDesignerPanel](#marinadesignerpanel), [MarinaViewControl](#marinaviewcontrol), [ReferenceImageLoader](#referenceimageloader)

## VirtualMarina.Blazor

<a id="marinadesignerpanel"></a>
### MarinaDesignerPanel

`class MarinaDesignerPanel : ComponentBase, IDisposable`

| Member | Description |
|---|---|
| `MarinaDesignerPanel()` | Creates an instance with default values. |
| `MarinaVisualizer Marina { get; set; }` | The visualizer whose designer this panel drives. Required. |
| `MarinaView? View { get; set; }` | The view showing `MarinaDesignerPanel.Marina`; needed to decode image files in the browser. |
| `long MaxImageFileSize { get; set; }` | Largest accepted image file, in bytes (default 25 MB). |
| `string? CssClass { get; set; }` | Extra CSS classes for the outer element. |
| `void Dispose()` | Unsubscribes from the designer. |
| `protected override void OnParametersSet()` | Subscribes to the designer's state changes. |

<a id="marinaview"></a>
### MarinaView

`class MarinaView : ComponentBase, IAsyncDisposable`

Blazor WebAssembly component that renders a `MarinaVisualizer` into a WebGL 2 canvas.

| Member | Description |
|---|---|
| `MarinaView()` | Creates an instance with default values. |
| `MarinaVisualizer Marina { get; set; }` | The visualizer to display. Required. |
| `MarinaStyle? MarinaStyle { get; set; }` | How the marina is drawn and animated (lighting, waves, status colors, boat opacities, land and trees, piers, labels, selection, camera). Assigned to `Marina.Style` when set; leave it null to keep the visualizer's style. (Named `MarinaStyle` because `MarinaView.Style` is the element's inline CSS.) |
| `string? CssClass { get; set; }` | Extra CSS classes for the outer element. |
| `string? Style { get; set; }` | Extra inline CSS for the outer element (it fills its parent by default). |
| `EventCallback<string> OnRendererReady { get; set; }` | Invoked once WebGL is running, with a description of the renderer/GPU. |
| `EventCallback<string> OnRendererError { get; set; }` | Invoked if WebGL cannot be initialized. |
| `string? RendererDescription { get; }` | Backend and GPU description once WebGL is running, otherwise null. |
| `ValueTask DisposeAsync()` | Stops the animation loop, releases WebGL resources and unsubscribes from the visualizer. |
| `Task<ReferenceImage?> LoadReferenceImageAsync(byte[] data, string contentType, float? metersPerPixel = null)` | Shows PNG, JPEG or WebP bytes as the designer's reference image (north at the top). The browser decodes the image, so no .NET image library is needed. Returns null when the bytes can't be decoded or the view isn't running yet. |
| `protected override Task OnAfterRenderAsync(bool firstRender)` | On first render: loads the JS module, creates the WebGL context and starts the animation loop. |
| `double[] OnAnimationFrame(double timestampMs, double cssWidth, double cssHeight)` | Renders a frame and returns the popup anchor as [visible (0/1), x, y] in CSS pixels. |
| `void OnDoubleClick(double x, double y, int button, int modifiers)` | Called by marinaWebGL.js; forwards to `MarinaInputController.DoubleClick`. Not for direct use. |
| `bool OnKeyDown(string key, int modifiers)` | Called by marinaWebGL.js; maps DOM keys to `MarinaKey`. Returns true when handled. Not for direct use. |
| `protected override void OnParametersSet()` | Subscribes to the visualizer's popup changes. |
| `void OnPointerDown(double x, double y, int button, int modifiers)` | Called by marinaWebGL.js; forwards to `MarinaInputController.PointerDown`. Not for direct use. |
| `void OnPointerLeave()` | Called by marinaWebGL.js; forwards to `MarinaInputController.PointerLeave`. Not for direct use. |
| `string OnPointerMove(double x, double y, int modifiers)` | Called by marinaWebGL.js; forwards to `MarinaInputController.PointerMove`. Returns the CSS cursor to show when no button is held: "pointer" over a selectable berth or boat, "crosshair" or "move" for designer tools, otherwise "grab". Not for direct use. |
| `void OnPointerUp(double x, double y, int button, int modifiers)` | Called by marinaWebGL.js; forwards to `MarinaInputController.PointerUp`. Not for direct use. |
| `void OnWheel(double notches, double x, double y)` | Called by marinaWebGL.js; forwards to `MarinaInputController.Wheel`. Not for direct use. |

<a id="webglscenerenderer"></a>
### WebGlSceneRenderer

`sealed class WebGlSceneRenderer : ISceneRenderer, IDisposable`

`ISceneRenderer` backed by WebGL 2 through synchronous JS interop (Blazor WebAssembly).

Binary data goes to JavaScript as base64 little-endian float/uint32 buffers: meshes once per mesh-library version, instance data only when `RenderFrame.SceneVersion` changes. Per frame, only ~67 floats of camera/lighting/time uniforms cross the boundary; waves, bobbing and pulses animate on the GPU.

| Member | Description |
|---|---|
| `string BackendName { get; }` | *(See the interface member.)* |
| `string? DeviceDescription { get; }` | GPU renderer and WebGL version reported by the browser, available after `WebGlSceneRenderer.Initialize`. |
| `void Dispose()` | Stops rendering. GPU resources are released when the view is destroyed on the JS side. |
| `void Initialize()` | *(See the interface member.)* |
| `void Render(RenderFrame frame)` | *(See the interface member.)* |
| `void Resize(int pixelWidth, int pixelHeight)` | The JS side sizes the canvas drawing buffer from its CSS size and devicePixelRatio. |

## VirtualMarina.Core.Api

<a id="batchupdateerror"></a>
### BatchUpdateError

`sealed record BatchUpdateError`

An update in a batch that could not be applied.

| Member | Description |
|---|---|
| `BatchUpdateError(string BerthId, string Message)` | An update in a batch that could not be applied. |
| `string BerthId { get; init; }` | The update's berth id. |
| `string Message { get; init; }` | Why it failed (unknown berth, invalid value, ...). |

<a id="batchupdateresult"></a>
### BatchUpdateResult

`sealed record BatchUpdateResult`

Outcome of `IMarinaVisualizer.BatchUpdate` or `SetBerthFlags`.

| Member | Description |
|---|---|
| `BatchUpdateResult(int AppliedCount, IReadOnlyList<BatchUpdateError> Errors)` | Outcome of `IMarinaVisualizer.BatchUpdate` or `SetBerthFlags`. |
| `int AppliedCount { get; init; }` | Number of updates applied. |
| `IReadOnlyList<BatchUpdateError> Errors { get; init; }` | Updates that failed; the others were still applied. |
| `bool Succeeded { get; }` | True when every update was applied. |

<a id="berthaction"></a>
### BerthAction

`sealed class BerthAction`

An entry in the actions window that opens on right-click.

| Member | Description |
|---|---|
| `BerthAction(string actionId, string caption, bool enabled = true)` | Creates an action. Usually added through `BerthActionCollection.Add`. |
| `string ActionId { get; }` | Identifier passed back in `BerthActionInvokedEventArgs.ActionId`. |
| `string Caption { get; set; }` | Text shown in the actions window. |
| `bool Enabled { get; set; }` | Disabled actions are shown grayed out and cannot be invoked. |
| `bool Visible { get; set; }` | Hidden actions are not shown. |
| `string? Description { get; set; }` | Hint shown when hovering the action (e.g. why it is disabled). |
| `string? Icon { get; set; }` | Short glyph drawn before the caption, e.g. an emoji or a single symbol character. |
| `string? ShortcutText { get; set; }` | Optional keyboard hint drawn right-aligned (informational only). |
| `BerthActionStyle Style { get; set; }` | Normal, Primary (highlighted) or Danger (red). |
| `bool BeginGroup { get; set; }` | Draw a separator above this action. |
| `bool KeepOpen { get; set; }` | Keep the actions window open after this action is invoked. |
| `object? Tag { get; set; }` | Host-owned value passed back with the invocation. |

<a id="berthactioncollection"></a>
### BerthActionCollection

`sealed class BerthActionCollection : Collection<BerthAction>`

Ordered, editable list of actions with lookup by `BerthAction.ActionId`.

| Member | Description |
|---|---|
| `BerthActionCollection()` | Creates an instance with default values. |
| `BerthAction Add(string actionId, string caption, bool enabled = true, string? icon = null)` | Creates, appends and returns an action; set further properties on the result. |
| `BerthAction? Find(string actionId)` | The action with this id (case-insensitive), or null. |
| `protected override void InsertItem(int index, BerthAction item)` | Rejects null actions. |
| `bool Remove(string actionId)` | Removes the action with this id. Returns false when none matched. |
| `protected override void SetItem(int index, BerthAction item)` | Rejects null actions. |

<a id="berthactioninvokedeventargs"></a>
### BerthActionInvokedEventArgs

`sealed class BerthActionInvokedEventArgs : EventArgs`

Data for `IMarinaVisualizer.BerthActionInvoked`: the user clicked an action in the actions window.

| Member | Description |
|---|---|
| `BerthActionInvokedEventArgs(BerthAction action, IReadOnlyList<Berth> berths)` | Creates the event data (raised by the visualizer; hosts normally don't construct it). |
| `string ActionId { get; }` | The `BerthAction.ActionId` of the clicked action. |
| `BerthAction Action { get; }` | The clicked action, including its `BerthAction.Tag`. |
| `Berth Berth { get; }` | The berth the actions window was opened on (the primary berth for a multi-selection). |
| `string BerthId { get; }` | Id of `BerthActionInvokedEventArgs.Berth`. |
| `IReadOnlyList<Berth> Berths { get; }` | Every berth the actions window was opened for (current snapshots). |
| `IReadOnlyList<Berth> ActionableBerths { get; }` | The berths in `BerthActionInvokedEventArgs.Berths` that are not read-only. |
| `bool IsMultiSelection { get; }` | True when the window was opened for two or more berths. |
| `bool KeepPopupOpen { get; set; }` | Set true to keep the actions window open (it closes by default unless `BerthAction.KeepOpen` is set). |

<a id="berthactionstyle"></a>
### BerthActionStyle

`enum BerthActionStyle`

Visual emphasis of a `BerthAction`.

| Value | Description |
|---|---|
| `Normal` = 0 | Regular entry. |
| `Primary` = 1 | The main action (drawn highlighted). |
| `Danger` = 2 | A destructive action (drawn in red). |

<a id="bertheventargs"></a>
### BerthEventArgs

`class BerthEventArgs : EventArgs`

Data for `IMarinaVisualizer.BerthClicked` and the base of `BerthSelectedEventArgs`. Carries an immutable snapshot of the berth at the time of the event.

| Member | Description |
|---|---|
| `BerthEventArgs(Berth berth, Pier? pier, PointerButton button = PointerButton.None, bool isDoubleClick = false, Vector3? worldPoint = null)` | Creates the event data. |
| `Berth Berth { get; }` | Snapshot of the berth when the event was raised. |
| `string BerthId { get; }` | The berth's id (`Berth.Id`). |
| `BerthStatus Status { get; }` | The berth's status (`Berth.Status`). |
| `Boat? Boat { get; }` | Boat assigned to the berth (moored, expected or temporarily away), if known. |
| `MarinaDataBag ExternalData { get; }` | Host-owned data bag of the berth; values written here persist with the berth (see `Berth.ExternalData`). |
| `Pier? Pier { get; }` | The pier the berth belongs to; null for a land berth. |
| `LandArea? LandArea { get; init; }` | The land area a land berth is on (`Berth.LandAreaId`); null for a water berth. |
| `PointerButton Button { get; }` | Button that triggered the event; `PointerButton.None` for programmatic selection. |
| `bool IsDoubleClick { get; }` | True when the event came from a double-click. |
| `Vector3? WorldPoint { get; }` | World-space point under the pointer, when the event came from a click. |

<a id="berthhovereventargs"></a>
### BerthHoverEventArgs

`sealed class BerthHoverEventArgs : EventArgs`

Data for `IMarinaVisualizer.BerthHoverChanged`.

| Member | Description |
|---|---|
| `BerthHoverEventArgs(Berth? berth)` | Creates the event data. |
| `Berth? Berth { get; }` | Berth under the pointer, or null when the pointer left all berths. |

<a id="berthlabelmode"></a>
### BerthLabelMode

`enum BerthLabelMode`

Which berths get their name written on the water. Set through `MarinaVisualizer.BerthLabelMode`.

| Value | Description |
|---|---|
| `None` = 0 | No labels (default). |
| `OnlyFree` = 1 | Only Free berths. |
| `NonOccupied` = 2 | Every berth that is not Occupied: Free, Reserved and Temporarily Free. |
| `All` = 3 | Every berth. |

<a id="berthlabelmodeextensions"></a>
### BerthLabelModeExtensions

`static class BerthLabelModeExtensions`

Helpers for `BerthLabelMode`.

| Member | Description |
|---|---|
| `static string GetDisplayName(BerthLabelMode mode)` | Human-readable name, e.g. "Non-occupied", for UI pickers. |
| `static bool Includes(BerthLabelMode mode, BerthStatus status)` | True when a berth with `status` is labeled in this mode. |

<a id="berthpopup"></a>
### BerthPopup

`sealed class BerthPopup`

The popup currently shown above the selection (content in `BerthTooltip` and `BerthAction`, see SelectionActions.cs). Host views render it and position it at `MarinaVisualizer.TryGetPopupAnchor` every frame.

| Member | Description |
|---|---|
| `BerthPopupKind Kind { get; }` | Tooltip (left-click) or actions window (right-click). |
| `IReadOnlyList<Berth> Berths { get; }` | The berths the popup is about. The last one is the primary berth the popup points at. |
| `Berth PrimaryBerth { get; }` | The berth the popup points at. |
| `bool IsMultiSelection { get; }` | True when the popup is for two or more berths. |
| `BerthTooltip Tooltip { get; }` | Tooltip content produced by the selection event handlers (a copy; editing it has no effect). |
| `IReadOnlyList<BerthAction> Actions { get; }` | Visible actions, in display order. Empty for `BerthPopupKind.Tooltip`. |
| `int Version { get; }` | Changes every time popup content changes; lets views skip re-layout. |

<a id="berthpopupchangedeventargs"></a>
### BerthPopupChangedEventArgs

`sealed class BerthPopupChangedEventArgs : EventArgs`

Data for `IMarinaVisualizer.PopupChanged`: the popup above the selection opened, closed, or changed content.

| Member | Description |
|---|---|
| `BerthPopupChangedEventArgs(BerthPopup? previous, BerthPopup? current)` | Creates the event data. |
| `BerthPopup? Previous { get; }` | The popup shown before the change, or null. |
| `BerthPopup? Current { get; }` | The popup now shown, or null when it closed. |

<a id="berthpopupkind"></a>
### BerthPopupKind

`enum BerthPopupKind`

Which popup is shown above the selection.

| Value | Description |
|---|---|
| `Tooltip` = 0 | Information about the selection (left-click). |
| `Actions` = 1 | Tooltip header plus clickable actions (right-click). |

<a id="berthselectedeventargs"></a>
### BerthSelectedEventArgs

`sealed class BerthSelectedEventArgs : BerthEventArgs`

Data for `IMarinaVisualizer.BerthSelected`: a single berth was selected (or re-clicked, or its popup content needs refreshing). Fill `BerthSelectedEventArgs.Tooltip` and `BerthSelectedEventArgs.Actions` to control the popup: a left click shows the tooltip, a right click (`BerthSelectedEventArgs.OpensActions`) shows the actions window.

| Member | Description |
|---|---|
| `BerthSelectedEventArgs(Berth berth, Pier? pier, MultiBerth? multiBerth, BerthTooltip tooltip, BerthActionCollection actions, SelectionReason reason, bool isNewSelection, PointerButton button = PointerButton.None, bool isDoubleClick = false, Vector3? worldPoint = null)` | Creates the event data (raised by the visualizer; hosts normally don't construct it). |
| `MultiBerth? MultiBerth { get; }` | The multi-berth the berth belongs to, if any. |
| `BerthTooltip Tooltip { get; }` | Pre-filled with berth, pier, status and boat details (`DefaultPopupContent.ForBerth`). Edit freely. |
| `BerthActionCollection Actions { get; }` | Empty by default. Add the actions available for this berth; they are shown on right-click. |
| `SelectionReason Reason { get; }` | Why the event was raised: a click, an API call, or a content refresh. |
| `bool IsNewSelection { get; }` | False when the berth was already the selection (re-click or refresh). |
| `bool OpensActions { get; }` | True when the actions window will open (right-click), false for the tooltip. |

<a id="berthselectionrejection"></a>
### BerthSelectionRejection

`enum BerthSelectionRejection`

Why a berth id passed to `MarinaVisualizer.SetSelection` was not selected.

| Value | Description |
|---|---|
| `NotFound` = 0 | No berth has this id. |
| `Disabled` = 1 | The berth is disabled (`IsDisabled`). |
| `Hidden` = 2 | The berth is hidden (`IsVisible = false`). |
| `FilteredOut` = 3 | The berth's status is excluded by the status filter. |

<a id="berthstatuschangedeventargs"></a>
### BerthStatusChangedEventArgs

`sealed class BerthStatusChangedEventArgs : EventArgs`

Data for `IMarinaVisualizer.BerthStatusChanged`: a berth's status or assigned boat changed.

| Member | Description |
|---|---|
| `BerthStatusChangedEventArgs(Berth previous, Berth current)` | Creates the event data. |
| `Berth Previous { get; }` | Snapshot of the berth before the change. |
| `Berth Current { get; }` | Snapshot of the berth after the change. |
| `string BerthId { get; }` | The berth's id. |
| `BerthStatus OldStatus { get; }` | Status before the change. |
| `BerthStatus NewStatus { get; }` | Status after the change. |
| `Boat? OldBoat { get; }` | Boat before the change. |
| `Boat? NewBoat { get; }` | Boat after the change. |

<a id="berthtooltip"></a>
### BerthTooltip

`sealed class BerthTooltip`

Tooltip content shown above the selection. The visualizer pre-fills it from the berth data; handlers of `IMarinaVisualizer.BerthSelected` and `IMarinaVisualizer.MultiBerthSelected` may change, extend or clear it.

| Member | Description |
|---|---|
| `BerthTooltip()` | Creates an instance with default values. |
| `string? Title { get; set; }` | Bold heading. Default: the berth's display name, or "N berths selected". |
| `string? Subtitle { get; set; }` | Muted line under the title. Default: the pier name(s). |
| `ColorRgba? AccentColor { get; set; }` | Color of the tooltip's accent bar. Defaults to the berth's status color. |
| `IList<BerthTooltipLine> Lines { get; }` | Label/value rows. |
| `string? Footer { get; set; }` | Optional free text under the rows. |
| `bool IsVisible { get; set; }` | Set false to show no tooltip for this selection. |
| `BerthTooltip AddLine(string label, string? value, bool emphasize = false)` | Appends a label/value row. |
| `void Clear()` | Removes the title, subtitle, rows, footer and accent (the default content), keeping `BerthTooltip.IsVisible`. |
| `bool RemoveLine(string label)` | Removes the first row with this label (case-insensitive), e.g. `RemoveLine("Owner")`. Returns false when none matched. |
| `BerthTooltip SetLine(string label, string? value)` | Replaces the value of the first line with this label, or adds the line. |

<a id="berthtooltipline"></a>
### BerthTooltipLine

`sealed record BerthTooltipLine`

| Member | Description |
|---|---|
| `BerthTooltipLine(string Label, string? Value)` | Positional constructor; the parameters are described on the properties below. |
| `string Label { get; init; }` | Left column. Empty for a full-width value. |
| `string? Value { get; init; }` | Right column. |
| `bool IsEmphasized { get; init; }` | Draw the value in bold. |

<a id="berthupdate"></a>
### BerthUpdate

`sealed record BerthUpdate`

Partial change to a berth, for `IMarinaVisualizer.UpdateBerth(BerthUpdate)` and `BatchUpdate`. Only non-null members are applied, so a batch can mix status changes, boat assignments, geometry edits and interaction flags.

| Member | Description |
|---|---|
| `BerthUpdate(string BerthId)` | Partial change to a berth, for `IMarinaVisualizer.UpdateBerth(BerthUpdate)` and `BatchUpdate`. Only non-null members are applied, so a batch can mix status changes, boat assignments, geometry edits and interaction flags. |
| `string BerthId { get; init; }` | The berth to change. |
| `BerthStatus? Status { get; init; }` | New status. Free also removes the boat. |
| `Boat? Boat { get; init; }` | Boat to assign. Ignored when the resulting status is Free. |
| `bool ClearBoat { get; init; }` | Removes the assigned boat (applied before `BerthUpdate.Boat`). |
| `string? Label { get; init; }` | New display label. |
| `Vector2? Center { get; init; }` | New center of the water area, in plan coordinates. |
| `float? HeadingDegrees { get; init; }` | New heading (bow direction) in degrees. |
| `float? Length { get; init; }` | New length in meters. |
| `float? Width { get; init; }` | New width in meters. |
| `float? MaxDraft { get; init; }` | New maximum draft in meters. |
| `bool? HasFingerPiers { get; init; }` | Show or hide the automatic finger piers. |
| `bool? IsVisible { get; init; }` | Show or hide the berth (see `Berth.IsVisible`). |
| `bool? IsDisabled { get; init; }` | Disable or enable the berth (see `Berth.IsDisabled`). |
| `bool? IsReadOnly { get; init; }` | Make the berth read-only or editable (see `Berth.IsReadOnly`). |
| `IReadOnlyDictionary<string, string>? Metadata { get; init; }` | Replaces the berth's `Berth.Metadata`. |
| `IReadOnlyDictionary<string, object?>? ExternalData { get; init; }` | Entries written into the berth's `Berth.ExternalData` (merged; a null value is stored as null). |
| `static BerthUpdate Flags(string berthId, bool? visible = null, bool? disabled = null, bool? readOnly = null)` | Sets the interaction flags; null leaves a flag unchanged. |
| `static BerthUpdate Free(string berthId)` | Marks the berth Free and removes any boat. |
| `static BerthUpdate Geometry(string berthId, Vector2? center = null, float? headingDegrees = null, float? length = null, float? width = null)` | Moves or resizes the berth; null leaves a value unchanged. |
| `static BerthUpdate Occupy(string berthId, Boat boat)` | Marks the berth Occupied by `boat`. |
| `static BerthUpdate Reserve(string berthId, Boat? expectedBoat = null)` | Marks the berth Reserved, optionally for a known incoming boat. |
| `static BerthUpdate TemporarilyFree(string berthId, Boat? boat = null)` | Marks the berth Temporarily Free. Keeps the assigned boat unless `boat` replaces it. |

<a id="defaultpopupcontent"></a>
### DefaultPopupContent

`static class DefaultPopupContent`

Builds the tooltip the visualizer pre-fills before raising the selection events.

| Member | Description |
|---|---|
| `const int MaxListedBerths = 6` | Maximum number of per-berth rows in a multi-selection tooltip. |
| `static BerthTooltip ForBerth(Berth berth, Pier? pier, MultiBerth? multiBerth, StatusColorScheme colors, LandArea? landArea = null)` | The default single-berth tooltip: title = berth name, subtitle = pier (or land area for a land berth), rows for status, berth size and draft, boat name, type, size, owner, registration, expected arrival/return, multi-berth and read-only access. Accent = status color. |
| `static BerthTooltip ForBerths(IReadOnlyList<Berth> berths, Func<string, Pier?> pierLookup, StatusColorScheme colors, Func<string, LandArea?>? landLookup = null)` | The default multi-selection tooltip: "N berths selected", pier and land area names, counts per status and read-only, then one row per berth (up to `DefaultPopupContent.MaxListedBerths`, with an "and N more" footer). |

<a id="displaynames"></a>
### DisplayNames

`static class DisplayNames`

Readable, localized names for the settings a design UI shows in its pickers, so the WinForms panel, the Blazor panel and the Designer application all offer the same words.

The text comes from the core library's resources and follows `MarinaLocalization.Culture`. `Pier.GetDisplayName`, `BoatTypeCatalog.GetDisplayName` and `GetDisplayName()` on `BerthStatus` and `BerthLabelMode` name the rest.

| Member | Description |
|---|---|
| `static string GetDisplayName(DesignTool tool)` | Name of a design tool, e.g. "Land berths", for a toolbar button. |
| `static string GetDisplayName(BerthSeparator separator)` | Name of a berth separator, e.g. "Pier every other berth". |
| `static string GetDisplayName(PierServices services)` | Name of a pier's services, e.g. "Power and water". |
| `static string GetDisplayName(PierSides sides)` | Name of the berthing sides of a pier, e.g. "Boats on the left only". |
| `static string GetDisplayName(LandKind kind)` | Name of a land surface, e.g. "Lawn or park". |
| `static string GetDisplayName(HinterlandScenery scenery)` | Name of what covers the mainland behind the shore, e.g. "Countryside". |
| `static string GetDisplayName(DividerType type)` | Name of a divider, e.g. "Mooring piles". |
| `static string GetDisplayName(MooringStyle style)` | Name of a mooring style: "Alongside" or "Bow-in". |

<a id="imarinavisualizer"></a>
### IMarinaVisualizer

`interface IMarinaVisualizer`

The API the host application (typically an ERP) uses to drive the 3D marina: layout, berth status, selection, tooltips and actions, camera and appearance. It is UI-framework and graphics-API agnostic: the same instance drives the WinForms/OpenGL view (`MarinaViewControl`) and the Blazor/WebGL view (`<MarinaView>`).

Threading. Not thread-safe. Call it from the UI thread that owns the view (marshal with `Control.BeginInvoke` in WinForms or `InvokeAsync` in Blazor when data arrives on a background thread). Events. Raised synchronously on the calling thread, after the state change has been applied, so handlers can read the new state and call back into the API. Snapshots. `Berth`, `Pier`, `Boat`, `Divider` and `MultiBerth` are immutable records. Getters and events return snapshots; change state only through this API. The exception is `Berth.ExternalData`, a mutable bag for host data shared by all snapshots of a berth. Ids of piers, berths, dividers and berths are compared case-insensitively. Implementing this interface is not supported: it describes what `MarinaVisualizer` offers, and members are added to it in feature releases, which would break an outside implementation. Depend on it to keep host code testable — a mocking library fills in new members by itself — but let `MarinaVisualizer` be the only real implementation. `ISceneRenderer` is the interface meant to be implemented outside the library; anything added to that one comes with a default implementation. See `Docs/16-compatibility.md`.

| Member | Description |
|---|---|
| `string MarinaName { get; set; }` | Name of the loaded layout (`MarinaLayout.Name`). Set it to rename the marina; it is stored with the layout. |
| `MarinaDesigner Designer { get; }` | The layout designer: draw land areas, piers and berths in the view and trace a calibrated reference image (see `MarinaDesigner`). Off until `MarinaDesigner.IsActive` is set. |
| `Shoreline? Shoreline { get; }` | The mainland behind the marina, or null when the marina stands in open water. Set it with `IMarinaVisualizer.SetShoreline`. |
| `MarineTraffic MarineTraffic { get; }` | The passing traffic out at sea. `MarineTraffic.None` until it is switched on with `IMarinaVisualizer.SetMarineTraffic`. |
| `int TrafficLaneCount { get; }` | How many lanes the traffic actually found room for. Fewer than asked for means the clearance left little open water; zero means the traffic cannot be drawn at all. |
| `Berth? SelectedBerth { get; }` | The primary (most recently clicked or last listed) selected berth, or null. |
| `IReadOnlyList<Berth> SelectedBerths { get; }` | All selected berths in selection order; the last one is `IMarinaVisualizer.SelectedBerth`. |
| `Berth? HoveredBerth { get; }` | The berth under the pointer, or null. |
| `BerthPopup? ActivePopup { get; }` | The tooltip or actions window currently shown above the selection, or null. |
| `bool TooltipsEnabled { get; set; }` | Show a tooltip on left-click (default true). |
| `bool ActionsEnabled { get; set; }` | Open the actions window on right-click (default true). |
| `bool MultiSelectEnabled { get; set; }` | Allow Ctrl+click or Shift+click multi-selection (default true). |
| `BerthLabelMode BerthLabelMode { get; set; }` | Which berths have their name written on the water: None (default), OnlyFree, NonOccupied or All. |
| `BerthStatusFilter StatusFilter { get; }` | Statuses currently shown. Filtered-out berths keep their structure but lose status visuals, boats, labels and interaction. |
| `OrbitCamera Camera { get; }` | The orbit camera. Use it for low-level control (poses, constraints, field of view). |
| `IReadOnlyList<CameraPreset> CameraPresets { get; }` | Built-in presets (Overview, Top Down, Sea Side, East, West, Low Angle, one per pier) followed by custom presets. |
| `CameraAngle? DefaultFocusAngle { get; set; }` | Angle used by focus calls that don't pass one, by `SelectBerth(id, focusCamera: true)` and by double-click. Null (default) keeps the current yaw and looks down at least 35°. Set `CameraAngle.TopDown` for a plan view. |
| `LightingSettings Lighting { get; }` | Sun, ambient light, specular and fog. Changes apply on the next frame. |
| `WaterSettings Water { get; }` | Water colors and wave animation. Changes apply on the next frame. The same object as `Style.Water`. |
| `MarinaStyle Style { get; set; }` | Everything about how the marina is drawn and animated: lighting, water and waves, status colors and boat opacities, land and trees, piers, labels, selection and camera (see `MarinaStyle`). Change its properties or assign a new instance. |
| `event EventHandler<BerthEventArgs>? BerthClicked` | A berth (its water area or its boat) was clicked or double-clicked with any mouse button. Raised after the selection change the click caused. Never raised for disabled berths. |
| `event EventHandler<BerthSelectedEventArgs>? BerthSelected` | A single berth was selected: by a left or right click (including on an already selected berth), through the API, or because the open popup's content needs refreshing (`BerthSelectedEventArgs.Reason`). Fill `BerthSelectedEventArgs.Tooltip` (pre-filled with berth and boat details) and `BerthSelectedEventArgs.Actions` (empty) to control the popup: a left click shows the tooltip, a right click shows the actions window. |
| `event EventHandler<MultiBerthSelectedEventArgs>? MultiBerthSelected` | Two or more berths are selected (Ctrl+click or Shift+click, right-click inside a multi-selection, or `IMarinaVisualizer.SetSelection`). Fill the tooltip and actions for the whole selection. |
| `event EventHandler<SelectionChangedEventArgs>? SelectionChanged` | The set of selected berths or the primary berth changed, including the selection being cleared. |
| `event EventHandler? SelectionCleared` | The selection became empty (raised right after `IMarinaVisualizer.SelectionChanged`). |
| `event EventHandler<BerthActionInvokedEventArgs>? BerthActionInvoked` | The user clicked an enabled action in the actions window (or `IMarinaVisualizer.InvokeBerthAction` was called). The window closes afterwards unless `BerthActionInvokedEventArgs.KeepPopupOpen` or `BerthAction.KeepOpen` is set. |
| `event EventHandler<BerthPopupChangedEventArgs>? PopupChanged` | The tooltip/actions popup opened, closed or changed content. Host views re-render `IMarinaVisualizer.ActivePopup`. |
| `event EventHandler<BerthHoverEventArgs>? BerthHoverChanged` | The berth under the pointer changed (null when the pointer left all berths). Disabled berths are never hovered. |
| `event EventHandler<BerthStatusChangedEventArgs>? BerthStatusChanged` | A berth's status or assigned boat changed, through any API (single updates, batches, berths). Raised once per berth, with the previous and current snapshots. |
| `event EventHandler<LayoutChangedEventArgs>? LayoutChanged` | Piers, berths, dividers, berths or land areas were added, updated or removed, or the layout was initialized or cleared. Inside `IMarinaVisualizer.BeginUpdate` or `IMarinaVisualizer.BatchUpdate` the notifications are coalesced into one `LayoutChangeKind.BatchUpdated`. |
| `void AddBerth(Berth berth)` | Adds a berth. Its position, heading and size are absolute plan coordinates (see `Berth`). |
| `Berth AddBerth(string berthId, string pierId, Vector2 center, float headingDegrees, float length, float width, string? label = null)` | Adds a Free berth at an explicit position, orientation and size. |
| `void AddBerths(IEnumerable<Berth> berths)` | Adds several berths with a single `IMarinaVisualizer.LayoutChanged` notification. |
| `void AddCameraPreset(CameraPreset preset)` | Adds a custom preset, replacing one with the same name. Custom presets survive layout changes. |
| `void AddDivider(Divider divider)` | Adds a divider between berths: a finger pier, a row of piles or a floating boom. |
| `void AddDividers(IEnumerable<Divider> dividers)` | Adds several dividers with a single `IMarinaVisualizer.LayoutChanged` notification. |
| `void AddLandArea(LandArea landArea)` | Adds a land area (quay, lawn or breakwater) and builds its mesh. |
| `void AddPier(Pier pier)` | Adds a pier. Use the `Pier` constructor (shore-end start point) or `Pier.FromCenter` (center point) to set position, size, orientation and `PierType`. |
| `bool AddToSelection(string berthId)` | Adds a berth to the selection, making it primary. Returns false when it can't be selected. |
| `bool ApplyCameraPreset(string presetName, bool immediate = false)` | Moves the camera to a preset by name (case-insensitive). Returns false when no preset has this name. |
| `Berth AssignBoat(string berthId, Boat boat)` | Marks the berth Occupied (red) by `boat`. |
| `MultiBerth AssignBoatToBerths(IEnumerable<string> berthIds, Boat boat, BerthStatus status = BerthStatus.Occupied, MooringStyle style = MooringStyle.Alongside, string? multiBerthId = null)` | Puts a single boat in several berths at once. Every member berth takes `status` and `boat` and gets `Berth.MultiBerthId`; the boat is drawn once across them and finger piers between them are hidden. |
| `BatchUpdateResult BatchUpdate(IEnumerable<BerthUpdate> updates)` | Applies many partial updates with a single scene rebuild and one `IMarinaVisualizer.LayoutChanged`. Failing updates (unknown berth, invalid values) are collected in the result instead of throwing; the others are applied. |
| `IDisposable BeginUpdate()` | Starts a batch: `IMarinaVisualizer.LayoutChanged` notifications are coalesced and popup refreshes deferred until the returned scope is disposed. Scopes can be nested. |
| `Pier ChangePierId(string pierId, string newPierId)` | Gives a pier another id. Its berths and dividers are re-pointed at the new id, and the pier keeps everything else, including its place in the order and its display name. Returns the renamed pier and raises `LayoutChangeKind.PierRenamed`. |
| `void ClearLayout()` | Removes everything (piers, berths, dividers, berths, land) and clears the selection. |
| `void ClearSelection()` | Clears the selection and closes the popup. |
| `void ClosePopup()` | Closes the tooltip or actions window (the selection is kept). |
| `object[] ExportObjects()` | The whole marina as a flat array of its immutable records, in dependency order: `LandArea`s, `Pier`s, `Divider`s, `Berth`s, then `MultiBerth`s (see `MarinaLayout.ToObjects`). Rebuild a layout with `MarinaLayout.FromObjects`. |
| `bool FocusBerth(string berthId, bool immediate = false)` | Centers the camera on a berth at `IMarinaVisualizer.DefaultFocusAngle`, zoomed to show it whole. False when the berth doesn't exist. |
| `bool FocusBerth(string berthId, CameraAngle angle, bool immediate = false)` | Centers the camera on a berth from a specific angle, zoomed to show it whole. False when the berth doesn't exist. |
| `bool FocusBerths(IEnumerable<string> berthIds, CameraAngle? angle = null, bool immediate = false)` | Moves the camera so every listed berth (and its boat) is in view: the target is the middle of the berths and the distance is the closest that fits them all, with a margin. Unknown ids are ignored; disabled and hidden berths are included. |
| `bool FocusPier(string pierId, CameraAngle? angle, bool immediate = false)` | Moves the camera so the whole pier is in view, with every berth along it, at `angle` (or `IMarinaVisualizer.DefaultFocusAngle` when null — pass `CameraAngle.TopDown` for a plan view of the pier). |
| `bool FocusPier(string pierId, bool immediate = false)` | Moves the camera to the pier's close-up view. False when the pier doesn't exist. |
| `bool FocusSelection(CameraAngle? angle = null, bool immediate = false)` | Frames the selected berths (see `IMarinaVisualizer.FocusBerths`). False when nothing is selected. |
| `Berth? GetBerth(string berthId)` | The berth with this id, or null. |
| `IReadOnlyList<Berth> GetBerths()` | All berths, in the order they were added. |
| `IReadOnlyList<Berth> GetBerthsByLandArea(string landAreaId)` | Land berths on a land area (see `Berth.OnLand`). |
| `IReadOnlyList<Berth> GetBerthsByPier(string pierId)` | Berths belonging to a pier. |
| `IReadOnlyList<Berth> GetBerthsByStatus(BerthStatus status)` | Berths with a given status. |
| `Divider? GetDivider(string dividerId)` | The divider with this id, or null. |
| `IReadOnlyList<Divider> GetDividers()` | All dividers, in the order they were added. |
| `IReadOnlyList<Divider> GetDividersByPier(string pierId)` | Dividers whose `Divider.PierId` is `pierId`. |
| `LandArea? GetLandArea(string landAreaId)` | The land area with this id, or null. |
| `IReadOnlyList<LandArea> GetLandAreas()` | All land areas, in layout order. |
| `MarinaLayout GetLayout()` | A snapshot of the current marina, suitable for saving and passing back to `IMarinaVisualizer.InitializeLayout`. |
| `MultiBerth? GetMultiBerth(string multiBerthId)` | The berth with this id, or null. |
| `MultiBerth? GetMultiBerthFor(string berthId)` | The berth a berth belongs to, or null. |
| `IReadOnlyList<MultiBerth> GetMultiBerths()` | All multi-berths. |
| `Pier? GetPier(string pierId)` | The pier with this id, or null. |
| `IReadOnlyList<Pier> GetPiers()` | All piers, in the order they were added. |
| `MarinaStatistics GetStatistics()` | Berth counts per status, for dashboards. |
| `ColorRgba GetStatusColor(BerthStatus status)` | The current color of a status. |
| `IReadOnlyList<TrafficVessel> GetTrafficVessels()` | Where every passing vessel is right now, for a host that wants to draw its own marker or label. The list is a snapshot: the vessels have moved on by the next frame. |
| `BerthHit? HitTest(float x, float y)` | Hit-tests a point in view pixels (origin top-left) against visible, unfiltered berths and boats. Returns the nearest hit, or null. Disabled berths are hit (they block what's behind them) but input ignores them. |
| `void InitializeLayout(MarinaLayout layout)` | Replaces the whole marina: piers, berths, dividers, multi-berths and land. Clears the selection and popup, re-centers the water, regenerates the built-in camera presets and resets the camera to the overview. |
| `bool InvokeBerthAction(string actionId)` | Invokes an action of the open actions window: raises `IMarinaVisualizer.BerthActionInvoked` and closes the window unless kept open. Returns false when no actions window is open or the action doesn't exist, is disabled or hidden. |
| `bool IsBerthSelected(string berthId)` | True when the berth is part of the selection. |
| `bool IsBerthVisible(string berthId)` | True when the berth exists, is not hidden and passes the status filter. |
| `Berth MarkTemporarilyFree(string berthId, Boat? boat = null)` | Marks the berth Temporarily Free (yellow): the berth holder's boat is away. The boat stays assigned and is drawn as a ghost. Pass `boat` to set or replace it. |
| `MultiBerth MoorAlongside(IEnumerable<string> berthIds, Boat boat, BerthStatus status = BerthStatus.Occupied, string? multiBerthId = null)` | Moors one boat alongside (parallel to the pier) across two or more berths, e.g. a superyacht taking several small berths. Shortcut for `IMarinaVisualizer.AssignBoatToBerths` with `MooringStyle.Alongside`. |
| `void RefreshPopup()` | Rebuilds the open popup's content by raising the selection event with `SelectionReason.Refresh`. |
| `Berth ReleaseBerth(string berthId)` | Marks the berth Free (green) and removes its boat. On a berth member this releases the whole berth. |
| `bool ReleaseMultiBerth(string multiBerthId)` | Removes the berth and sets all its berths Free. Returns false when no berth has this id. |
| `bool RemoveBerth(string berthId)` | Removes a berth, dropping it from the selection. If it belongs to a multi-berth the berth shrinks, or dissolves when fewer than two berths remain (the last one keeps the boat). Returns false when no berth has this id. |
| `bool RemoveDivider(string dividerId)` | Removes a divider. Returns false when no divider has this id. |
| `bool RemoveFromSelection(string berthId)` | Removes a berth from the selection. Returns false when it wasn't selected. |
| `bool RemoveLandArea(string landAreaId, bool removeBerths = true)` | Removes a land area. Returns false when no land area has this id. |
| `bool RemovePier(string pierId, bool removeBerths = true)` | Removes a pier and its dividers. |
| `bool RemoveShoreline()` | Takes the mainland away, leaving open water. Returns false when there was none. |
| `Berth RenameBerth(string berthId, string newBerthId)` | Gives a berth another name. The berth keeps everything else — its place in the marina, its boat, its `Berth.ExternalData` and its place in the selection — and its multi-berth follows it. Returns the renamed berth and raises `LayoutChangeKind.BerthRenamed`. |
| `Berth ReserveBerth(string berthId, Boat? expectedBoat = null)` | Marks the berth Reserved (blue), optionally for a known incoming boat (drawn as a translucent ghost). |
| `void ResetCamera(bool immediate = false)` | Moves the camera to the Overview preset. |
| `void ResetStatusColors()` | Restores the default status colors and overlay opacities. |
| `CameraPreset SaveCameraPreset(string name, string? description = null)` | Saves where the camera is now as a custom preset, so a host can offer "go back to this view" later. Replaces a custom preset of the same name. |
| `bool SelectBerth(string berthId, bool focusCamera = false)` | Makes one berth the only selected berth (raising `IMarinaVisualizer.BerthSelected`), optionally focusing the camera on it. Returns false, changing nothing, when the berth doesn't exist or is hidden, disabled or filtered out. |
| `int SelectBerths(IEnumerable<string> berthIds)` | Replaces the selection with the selectable berths among `berthIds`. Returns how many were selected. |
| `Berth SetBerthDisabled(string berthId, bool disabled)` | Disables or enables a berth. Disabled berths are drawn in gray (boat desaturated) and cannot be hovered, selected, right-clicked or acted on. Disabling a selected berth removes it from the selection. |
| `BatchUpdateResult SetBerthFlags(IEnumerable<string> berthIds, bool? visible = null, bool? disabled = null, bool? readOnly = null)` | Sets interaction flags on many berths with a single scene rebuild. Null leaves a flag unchanged. |
| `Berth SetBerthReadOnly(string berthId, bool readOnly)` | Makes a berth read-only: it can be selected and shows its tooltip, but its actions window never opens. |
| `Berth SetBerthStatus(string berthId, BerthStatus status, Boat? boat = null)` | Sets a berth's status. Free always removes the boat; for other statuses a null `boat` keeps the current boat. |
| `Berth SetBerthVisible(string berthId, bool visible)` | Shows or hides a berth. Hidden berths draw nothing (not even finger piers) and cannot be interacted with. |
| `void SetMarineTraffic(MarineTraffic? traffic)` | Sets the passing traffic and lays out the lanes it runs along. Lanes are kept clear of the marina, the land areas and the mainland by `MarineTraffic.Clearance`, so nothing sails over a quay. |
| `SelectionResult SetSelection(IEnumerable<string> berthIds, bool focusCamera = false, CameraAngle? focusAngle = null)` | Replaces the selection with one or more berths. Disabled berths are discarded, as are hidden, filtered-out and unknown ids and duplicates; `SelectionResult.Rejected` lists them with the reason. The rest are selected in the given order (the last becomes primary) and `IMarinaVisualizer.BerthSelected` or `IMarinaVisualizer.MultiBerthSelected` is raised. When nothing remains the selection is cleared. |
| `SelectionResult SetSelection(params string[] berthIds)` | Replaces the selection with the given berths (disabled ones are discarded), without moving the camera. |
| `void SetShoreline(Shoreline? shoreline)` | Sets (or replaces) the mainland behind the marina and builds its mesh. It is drawn beneath the land areas, so a quay traced along the shore sits on top of it and the two read as one piece of ground. |
| `void SetStatusColor(BerthStatus status, ColorRgba color)` | Changes the pad, buoy and ghost-boat color of a status. |
| `void SetStatusFilter(BerthStatusFilter filter)` | Shows only berths whose status is in `filter`; filtered-out berths leave the selection. |
| `bool ShowActions()` | Opens the actions window for the current selection (raising the selection event with a right-click button). Falls back to the tooltip when every selected berth is read-only or there are no visible actions. |
| `void ShowAllStatuses()` | Shows every status (`BerthStatusFilter.All`). |
| `bool ShowTooltip()` | Shows the tooltip for the current selection, raising the selection event again to collect content. False when nothing is shown. |
| `bool TryGetPopupAnchor(out Vector2 screenPoint)` | Where the popup should point, in view pixels: just above the primary selected berth. Call every frame (the camera moves). False when no popup is open or the point is behind the camera. |
| `void UpdateBerth(Berth berth)` | Replaces a berth definition (matched by id). The berth keeps its `Berth.ExternalData` instance (entries from a different dictionary are merged in). A status or boat change on a member of a multi-berth changes the whole berth. |
| `Berth UpdateBerth(BerthUpdate update)` | Applies a partial update (only the members set on `update`) and returns the resulting berth. A status or boat change on a member of a multi-berth changes the whole berth; setting it Free releases the berth. |
| `void UpdateDivider(Divider divider)` | Replaces a divider definition (matched by id). |
| `void UpdateLandArea(LandArea landArea)` | Replaces a land area definition (matched by id) and rebuilds its mesh. Its land berths keep their positions. |
| `MultiBerth UpdateMultiBerth(MultiBerth berth)` | Replaces a berth's boat, status, style and/or member berths. Berths no longer listed become Free. |
| `MultiBerth UpdateMultiBerth(string multiBerthId, Boat? boat = null, BerthStatus? status = null, MooringStyle? style = null, IEnumerable<string>? berthIds = null)` | Partial update of a berth; null arguments leave values unchanged. Berths no longer listed become Free. |
| `void UpdatePier(Pier pier)` | Replaces a pier definition (matched by id). Berths and dividers keep their absolute positions. |
| `Pier UpdatePier(PierUpdate update)` | Changes a pier's position, size, orientation, type, deck height or name, keeping everything else. Resizing or rotating keeps the pier's center unless a new start point is given. |

<a id="layoutchangekind"></a>
### LayoutChangeKind

`enum LayoutChangeKind`

What changed in a `IMarinaVisualizer.LayoutChanged` notification.

| Value | Description |
|---|---|
| `Initialized` = 0 | `IMarinaVisualizer.InitializeLayout` loaded a new marina. |
| `Cleared` = 1 | `IMarinaVisualizer.ClearLayout` removed everything. |
| `PierAdded` = 2 | A pier was added (`LayoutChangedEventArgs.PierId`). |
| `PierUpdated` = 3 | A pier was updated. |
| `PierRemoved` = 4 | A pier was removed. |
| `PierRenamed` = 19 | A pier was given another id (`LayoutChangedEventArgs.PierId` is the new one). |
| `BerthAdded` = 5 | A berth was added (`LayoutChangedEventArgs.BerthId`). |
| `BerthUpdated` = 6 | A berth was updated (geometry, status, boat, flags, ...). |
| `BerthRemoved` = 7 | A berth was removed. |
| `BerthRenamed` = 8 | A berth was given another name (`LayoutChangedEventArgs.BerthId` is the new one). |
| `BatchUpdated` = 9 | Several changes made inside `IMarinaVisualizer.BeginUpdate` or a batch, coalesced into one notification. |
| `DividerAdded` = 10 | A divider was added (`LayoutChangedEventArgs.DividerId`). |
| `DividerUpdated` = 11 | A divider was updated. |
| `DividerRemoved` = 12 | A divider was removed. |
| `MultiBerthAdded` = 13 | A multi-berth was created (`LayoutChangedEventArgs.MultiBerthId`). |
| `MultiBerthUpdated` = 14 | A multi-berth changed boat, status, style or member berths. |
| `MultiBerthRemoved` = 15 | A multi-berth was released or dissolved. |
| `LandAreaAdded` = 16 | A land area was added (`LayoutChangedEventArgs.LandAreaId`). |
| `LandAreaUpdated` = 17 | A land area was updated (outline, height, kind or name). |
| `LandAreaRemoved` = 18 | A land area was removed. |
| `ShorelineChanged` = 19 | The mainland behind the marina was set, redrawn or taken away (`IMarinaVisualizer.SetShoreline`). |
| `MarineTrafficChanged` = 20 | The passing traffic was switched on or off, or its settings changed (`IMarinaVisualizer.SetMarineTraffic`). |

<a id="layoutchangedeventargs"></a>
### LayoutChangedEventArgs

`sealed class LayoutChangedEventArgs : EventArgs`

Data for `IMarinaVisualizer.LayoutChanged`. The id properties that apply to `LayoutChangedEventArgs.Kind` are set.

| Member | Description |
|---|---|
| `LayoutChangedEventArgs(LayoutChangeKind kind, string? pierId = null, string? berthId = null, string? dividerId = null, string? multiBerthId = null, string? landAreaId = null)` | Creates the event data. |
| `LayoutChangeKind Kind { get; }` | What changed. |
| `string? PierId { get; }` | The affected pier (for pier, berth and divider changes), or null. |
| `string? BerthId { get; }` | The affected berth, or null. |
| `string? DividerId { get; }` | The affected divider, or null. |
| `string? MultiBerthId { get; }` | The affected multi-berth, or null. |
| `string? LandAreaId { get; }` | The affected land area (for land area changes and land berth changes), or null. |

<a id="marinalocalization"></a>
### MarinaLocalization

`static class MarinaLocalization`

The language of every piece of text the visualizer, the designer and the host panels display.

Text lives in a `Strings.resx` per assembly and is looked up through `CultureInfo.CurrentUICulture`, so an application that already sets the UI culture needs nothing here. `MarinaLocalization.Culture` is the one-line way to set it for the whole application, including threads started later. Adding a language means adding a `Strings.<culture>.resx` next to the neutral file (see `Docs/15-localization.md`); a culture without a translation falls back to the neutral English text, so nothing ever comes out blank.

| Member | Description |
|---|---|
| `static CultureInfo? Culture { get; set; }` | The culture text is looked up in. Null (the default) follows `CultureInfo.CurrentUICulture`. Setting it applies to the calling thread and to every thread started afterwards. |
| `static IReadOnlyList<CultureInfo> AvailableCultures()` | The cultures the assembly has text for: the neutral language plus each shipped `Strings.<culture>.resx`. |

<a id="marinastatistics"></a>
### MarinaStatistics

`sealed record MarinaStatistics`

Occupancy counts for dashboards (`IMarinaVisualizer.GetStatistics`). Hidden and disabled berths are counted too.

| Member | Description |
|---|---|
| `MarinaStatistics(int TotalBerths, int Free, int Occupied, int Reserved, int TemporarilyFree = 0)` | Occupancy counts for dashboards (`IMarinaVisualizer.GetStatistics`). Hidden and disabled berths are counted too. |
| `int TotalBerths { get; init; }` | All berths. |
| `int Free { get; init; }` | Free berths. |
| `int Occupied { get; init; }` | Occupied berths. |
| `int Reserved { get; init; }` | Reserved berths. |
| `int TemporarilyFree { get; init; }` | Temporarily free berths. |
| `double OccupancyRate { get; }` | Occupied berths as a fraction of all berths (0–1). |

<a id="marinavisualizer"></a>
### MarinaVisualizer

`sealed class MarinaVisualizer : IMarinaVisualizer`

Default `IMarinaVisualizer` implementation. Owns the marina state, selection, popup, filter, camera and input handling, and produces a `RenderFrame` for whichever `ISceneRenderer` the host view uses.

Most hosts don't create one directly: `MarinaViewControl.Marina` (WinForms) owns one, and `<MarinaView Marina="...">` (Blazor) takes one. API members are documented on `IMarinaVisualizer`. A custom view drives it once per display frame: and forwards pointer and keyboard input to `MarinaVisualizer.Input`.

| Member | Description |
|---|---|
| `MarinaVisualizer()` | Creates an empty marina with default water and lighting. Load one with `MarinaVisualizer.InitializeLayout`. |
| `MarinaVisualizer(MarinaVisualizerOptions options)` | Creates an empty marina with custom water and lighting settings. |
| `const string OverviewPresetName = "Overview"` | Name of the built-in whole-marina preset used by `MarinaVisualizer.ResetCamera`. |
| `string MarinaName { get; set; }` | *(See the interface member.)* |
| `OrbitCamera Camera { get; }` | *(See the interface member.)* |
| `MarinaInputController Input { get; }` | Platform-neutral input handling. Host views forward pointer and keyboard events here; configure drag bindings, zoom step and hover through it. |
| `LightingSettings Lighting { get; }` | *(See the interface member.)* |
| `WaterSettings Water { get; }` | *(See the interface member.)* |
| `MarinaStyle Style { get; set; }` | *(See the interface member.)* |
| `MarinaDesigner Designer { get; }` | *(See the interface member.)* |
| `MeshLibrary Meshes { get; }` | Meshes available to the scene. Register a replacement under an existing id (e.g. `MeshIds.ForBoat`) to swap in imported models. |
| `StatusColorScheme Colors { get; }` | Current status colors and overlay opacities (read-only view). Change them with `MarinaVisualizer.SetStatusColor`, `MarinaVisualizer.SetDisabledColor`, `MarinaVisualizer.SetOverlayOpacity` and `MarinaVisualizer.ResetStatusColors`. |
| `BerthLabelMode BerthLabelMode { get; set; }` | Which berths have their name (`Berth.DisplayName`) written on the water next to their open end, sized to fit the berth's width. Hidden and filtered-out berths are never labeled. Default `BerthLabelMode.None`. |
| `double Time { get; }` | Seconds of animation time accumulated through `MarinaVisualizer.Update`. |
| `Vector2 ViewportSize { get; }` | View size in pointer units (usually pixels), as last set with `MarinaVisualizer.SetViewportSize`. Defaults to 1280 × 720. |
| `Shoreline? Shoreline { get; }` | *(See the interface member.)* |
| `MarineTraffic MarineTraffic { get; }` | *(See the interface member.)* |
| `int TrafficLaneCount { get; }` | *(See the interface member.)* |
| `bool TooltipsEnabled { get; set; }` | Show a tooltip above the selection on left-click (default true). |
| `bool ActionsEnabled { get; set; }` | Show the actions window on right-click (default true). |
| `bool MultiSelectEnabled { get; set; }` | Allow Ctrl+click or Shift+click to build a multi-selection (default true). |
| `bool ShowTooltipOnApiSelection { get; set; }` | Show the tooltip when host code selects berths through the API (default true). |
| `Berth? SelectedBerth { get; }` | The primary (most recently clicked) selected berth. |
| `IReadOnlyList<Berth> SelectedBerths { get; }` | All selected berths in selection order; the last one is `MarinaVisualizer.SelectedBerth`. |
| `bool IsMultiSelection { get; }` | True when two or more berths are selected. |
| `BerthPopup? ActivePopup { get; }` | The tooltip or actions window currently shown above the selection, or null. |
| `BerthStatusFilter StatusFilter { get; }` | *(See the interface member.)* |
| `Berth? HoveredBerth { get; }` | *(See the interface member.)* |
| `IReadOnlyList<CameraPreset> CameraPresets { get; }` | *(See the interface member.)* |
| `CameraAngle? DefaultFocusAngle { get; set; }` | Angle used when a focus call doesn't pass one (including double-click and `SelectBerth(id, focusCamera: true)`). Null (default) keeps the current yaw and looks down at least 35°. |
| `float FocusMargin { get; set; }` | Fraction of the view kept free around focused berths on each side (0–0.45, default 0.12). |
| `float MinFocusDistance { get; set; }` | Closest the camera gets when focusing (so a single berth still shows some context). Default 25 m. |
| `event EventHandler<BerthEventArgs>? BerthClicked` | *(See the interface member.)* |
| `event EventHandler<BerthSelectedEventArgs>? BerthSelected` | *(See the interface member.)* |
| `event EventHandler<MultiBerthSelectedEventArgs>? MultiBerthSelected` | *(See the interface member.)* |
| `event EventHandler<SelectionChangedEventArgs>? SelectionChanged` | *(See the interface member.)* |
| `event EventHandler? SelectionCleared` | *(See the interface member.)* |
| `event EventHandler<BerthActionInvokedEventArgs>? BerthActionInvoked` | *(See the interface member.)* |
| `event EventHandler<BerthPopupChangedEventArgs>? PopupChanged` | *(See the interface member.)* |
| `event EventHandler<BerthHoverEventArgs>? BerthHoverChanged` | *(See the interface member.)* |
| `event EventHandler<BerthStatusChangedEventArgs>? BerthStatusChanged` | *(See the interface member.)* |
| `event EventHandler<LayoutChangedEventArgs>? LayoutChanged` | *(See the interface member.)* |
| `void AddBerth(Berth berth)` | *(See the interface member.)* |
| `Berth AddBerth(string berthId, string pierId, Vector2 center, float headingDegrees, float length, float width, string? label = null)` | *(See the interface member.)* |
| `void AddBerths(IEnumerable<Berth> berths)` | *(See the interface member.)* |
| `void AddCameraPreset(CameraPreset preset)` | Adds a custom preset (replacing one with the same name). Custom presets survive layout changes. |
| `void AddDivider(Divider divider)` | *(See the interface member.)* |
| `void AddDividers(IEnumerable<Divider> dividers)` | *(See the interface member.)* |
| `void AddLandArea(LandArea landArea)` | *(See the interface member.)* |
| `void AddPier(Pier pier)` | *(See the interface member.)* |
| `bool AddToSelection(string berthId)` | Adds a berth to the selection (making it primary). Returns false when it can't be selected. |
| `bool ApplyCameraPreset(string presetName, bool immediate = false)` | *(See the interface member.)* |
| `Berth AssignBoat(string berthId, Boat boat)` | *(See the interface member.)* |
| `MultiBerth AssignBoatToBerths(IEnumerable<string> berthIds, Boat boat, BerthStatus status = BerthStatus.Occupied, MooringStyle style = MooringStyle.Alongside, string? multiBerthId = null)` | Puts a single boat in several berths at once. Every member berth takes `status` and `boat`, and the boat is drawn once across them. |
| `BatchUpdateResult BatchUpdate(IEnumerable<BerthUpdate> updates)` | *(See the interface member.)* |
| `IDisposable BeginUpdate()` | *(See the interface member.)* |
| `RenderFrame BuildRenderFrame()` | Builds the frame description for a renderer. Rebuilds instance data only when the scene changed. |
| `Pier ChangePierId(string pierId, string newPierId)` | *(See the interface member.)* |
| `void ClearLayout()` | *(See the interface member.)* |
| `void ClearSelection()` | *(See the interface member.)* |
| `void ClosePopup()` | *(See the interface member.)* |
| `CameraPose ComputeFocusPose(IReadOnlyCollection<Berth> berths, CameraAngle? angle = null)` | The pose `MarinaVisualizer.FocusBerths` would move to, without moving the camera. |
| `object[] ExportObjects()` | *(See the interface member.)* |
| `bool FocusBerth(string berthId, bool immediate = false)` | Centers the camera on a berth at `MarinaVisualizer.DefaultFocusAngle`, zoomed to show it whole. |
| `bool FocusBerth(string berthId, CameraAngle angle, bool immediate = false)` | Centers the camera on a berth, seen from `angle`, zoomed to show it whole. |
| `bool FocusBerths(IEnumerable<string> berthIds, CameraAngle? angle = null, bool immediate = false)` | Moves the camera so every listed berth (and its boat) is in view: the target is the middle of the berths and the distance is the closest that fits them, with `MarinaVisualizer.FocusMargin` around them. |
| `bool FocusPier(string pierId, bool immediate = false)` | *(See the interface member.)* |
| `bool FocusPier(string pierId, CameraAngle? angle, bool immediate = false)` | *(See the interface member.)* |
| `bool FocusSelection(CameraAngle? angle = null, bool immediate = false)` | Focuses on the current selection. Returns false when nothing is selected. |
| `Berth? GetBerth(string berthId)` | *(See the interface member.)* |
| `IReadOnlyList<Berth> GetBerths()` | *(See the interface member.)* |
| `IReadOnlyList<Berth> GetBerthsByLandArea(string landAreaId)` | *(See the interface member.)* |
| `IReadOnlyList<Berth> GetBerthsByPier(string pierId)` | *(See the interface member.)* |
| `IReadOnlyList<Berth> GetBerthsByStatus(BerthStatus status)` | *(See the interface member.)* |
| `Divider? GetDivider(string dividerId)` | *(See the interface member.)* |
| `IReadOnlyList<Divider> GetDividers()` | *(See the interface member.)* |
| `IReadOnlyList<Divider> GetDividersByPier(string pierId)` | *(See the interface member.)* |
| `LandArea? GetLandArea(string landAreaId)` | *(See the interface member.)* |
| `IReadOnlyList<LandArea> GetLandAreas()` | *(See the interface member.)* |
| `MarinaLayout GetLayout()` | *(See the interface member.)* |
| `MultiBerth? GetMultiBerth(string multiBerthId)` | *(See the interface member.)* |
| `MultiBerth? GetMultiBerthFor(string berthId)` | The multi-berth a berth belongs to, or null. |
| `IReadOnlyList<MultiBerth> GetMultiBerths()` | *(See the interface member.)* |
| `Pier? GetPier(string pierId)` | *(See the interface member.)* |
| `IReadOnlyList<Pier> GetPiers()` | *(See the interface member.)* |
| `MarinaStatistics GetStatistics()` | *(See the interface member.)* |
| `ColorRgba GetStatusColor(BerthStatus status)` | *(See the interface member.)* |
| `IReadOnlyList<TrafficVessel> GetTrafficVessels()` | *(See the interface member.)* |
| `Vector3? GetWaterPoint(float x, float y)` | Point on the water plane under a view pixel, if the ray hits it. |
| `BerthHit? HitTest(float x, float y)` | Hit-tests a point in view pixels (origin top-left) against visible, unfiltered berths and boats. Disabled berths are hit (so they block what's behind them), but input ignores them. |
| `void InitializeLayout(MarinaLayout layout)` | *(See the interface member.)* |
| `void InvalidateScene()` | Forces the render object list to be rebuilt on the next frame. |
| `bool InvokeBerthAction(string actionId)` | Invokes an action of the open actions window: raises `MarinaVisualizer.BerthActionInvoked` and closes the window (unless the action or handler keeps it open). Returns false when there is no such enabled action. |
| `bool IsBerthSelected(string berthId)` | *(See the interface member.)* |
| `bool IsBerthVisible(string berthId)` | True when the berth exists, is not hidden, and passes the status filter. |
| `Berth MarkTemporarilyFree(string berthId, Boat? boat = null)` | Marks the berth Temporarily Free (yellow): the berth holder's boat is away. The assigned boat is kept and drawn as a ghost; pass `boat` to set or replace it. |
| `MultiBerth MoorAlongside(IEnumerable<string> berthIds, Boat boat, BerthStatus status = BerthStatus.Occupied, string? multiBerthId = null)` | Moors one boat alongside (parallel to the pier) across several berths, e.g. a superyacht taking three small berths. |
| `void RefreshPopup()` | Rebuilds the open popup's content by raising the selection event with `SelectionReason.Refresh`. |
| `Berth ReleaseBerth(string berthId)` | *(See the interface member.)* |
| `bool ReleaseMultiBerth(string multiBerthId)` | Removes the multi-berth and sets all its berths Free. |
| `bool RemoveBerth(string berthId)` | *(See the interface member.)* |
| `bool RemoveCameraPreset(string presetName)` | Removes a custom preset by name. Built-in presets can't be removed. Returns false when nothing was removed. |
| `bool RemoveDivider(string dividerId)` | *(See the interface member.)* |
| `bool RemoveFromSelection(string berthId)` | *(See the interface member.)* |
| `bool RemoveLandArea(string landAreaId, bool removeBerths = true)` | *(See the interface member.)* |
| `bool RemovePier(string pierId, bool removeBerths = true)` | *(See the interface member.)* |
| `bool RemoveShoreline()` | *(See the interface member.)* |
| `Berth RenameBerth(string berthId, string newBerthId)` | *(See the interface member.)* |
| `Berth ReserveBerth(string berthId, Boat? expectedBoat = null)` | *(See the interface member.)* |
| `void ResetCamera(bool immediate = false)` | *(See the interface member.)* |
| `void ResetStatusColors()` | *(See the interface member.)* |
| `CameraPreset SaveCameraPreset(string name, string? description = null)` | *(See the interface member.)* |
| `bool SelectBerth(string berthId, bool focusCamera = false)` | Makes `berthId` the only selected berth and raises `MarinaVisualizer.BerthSelected`. Returns false (and changes nothing) when the berth doesn't exist, is hidden, disabled or filtered out. |
| `int SelectBerths(IEnumerable<string> berthIds)` | Replaces the selection with the selectable berths among `berthIds` (the last one becomes primary) and raises `MarinaVisualizer.BerthSelected` or `MarinaVisualizer.MultiBerthSelected`. Returns the number of selected berths. |
| `Berth SetBerthDisabled(string berthId, bool disabled)` | Disabled berths are drawn in gray and cannot be hovered, selected, right-clicked or acted on. |
| `BatchUpdateResult SetBerthFlags(IEnumerable<string> berthIds, bool? visible = null, bool? disabled = null, bool? readOnly = null)` | Sets interaction flags on many berths with a single scene rebuild; null leaves a flag unchanged. |
| `Berth SetBerthReadOnly(string berthId, bool readOnly)` | Read-only berths can be selected and show their tooltip, but their actions window does not open. |
| `Berth SetBerthStatus(string berthId, BerthStatus status, Boat? boat = null)` | *(See the interface member.)* |
| `Berth SetBerthVisible(string berthId, bool visible)` | Hidden berths are not drawn at all and cannot be interacted with. |
| `void SetDisabledColor(ColorRgba color)` | Changes the pad and buoy color used for disabled berths (boats of disabled berths are always desaturated). |
| `void SetMarineTraffic(MarineTraffic? traffic)` | *(See the interface member.)* |
| `void SetOverlayOpacity(float padOpacity, float ghostBoatOpacity)` | Sets the opacity of status pads and of reserved/temporarily free "ghost" boats. Values are clamped to 0.05–1. |
| `SelectionResult SetSelection(params string[] berthIds)` | Replaces the selection. See `MarinaVisualizer.SetSelection`. |
| `SelectionResult SetSelection(IEnumerable<string> berthIds, bool focusCamera = false, CameraAngle? focusAngle = null)` | Replaces the selection with one or more berths. Disabled berths are discarded, as are hidden, filtered-out and unknown ids and duplicates; the result lists what was skipped and why. The remaining berths are selected in the given order (the last becomes primary) and `MarinaVisualizer.BerthSelected` or `MarinaVisualizer.MultiBerthSelected` is raised as for a click. When nothing remains the selection is cleared. |
| `void SetShoreline(Shoreline? shoreline)` | *(See the interface member.)* |
| `void SetStatusColor(BerthStatus status, ColorRgba color)` | *(See the interface member.)* |
| `void SetStatusFilter(BerthStatusFilter filter)` | Shows only berths whose status is in `filter`. Filtered-out berths cannot be clicked or selected. |
| `void SetStatusFilter(params BerthStatus[] visibleStatuses)` | Shows only berths with one of the given statuses, e.g. `SetStatusFilter(BerthStatus.Free, BerthStatus.TemporarilyFree)`. |
| `void SetViewportSize(float width, float height)` | Sets the view size in the same units as pointer coordinates (usually pixels). |
| `bool ShowActions()` | Shows the actions window for the current selection (raising the selection event with `PointerButton.Right`). Falls back to the tooltip when the selection is read-only or has no actions. |
| `void ShowAllStatuses()` | *(See the interface member.)* |
| `bool ShowTooltip()` | Shows the tooltip for the current selection (raising the selection event again to collect content). |
| `bool TryGetPopupAnchor(out Vector2 screenPoint)` | Where the popup should point, in view pixels: just above the primary selected berth's selection marker. Call every frame (the camera moves). False when no popup is open or the anchor is behind the camera. |
| `bool TryProjectToScreen(Vector3 worldPoint, out Vector2 screenPoint)` | Projects a world point to view pixels (origin top-left). False when the point is behind the camera. |
| `void Update(double deltaSeconds)` | Advances animation time and camera smoothing. |
| `void UpdateBerth(Berth berth)` | *(See the interface member.)* |
| `Berth UpdateBerth(BerthUpdate update)` | *(See the interface member.)* |
| `void UpdateDivider(Divider divider)` | *(See the interface member.)* |
| `void UpdateLandArea(LandArea landArea)` | *(See the interface member.)* |
| `MultiBerth UpdateMultiBerth(MultiBerth multiBerth)` | Changes a multi-berth: its boat, status, mooring style and/or member berths. Berths no longer listed become Free. |
| `MultiBerth UpdateMultiBerth(string multiBerthId, Boat? boat = null, BerthStatus? status = null, MooringStyle? style = null, IEnumerable<string>? berthIds = null)` | Partial update of a multi-berth; null arguments leave values unchanged. |
| `void UpdatePier(Pier pier)` | *(See the interface member.)* |
| `Pier UpdatePier(PierUpdate update)` | *(See the interface member.)* |

<a id="marinavisualizeroptions"></a>
### MarinaVisualizerOptions

`sealed class MarinaVisualizerOptions`

Construction options for `MarinaVisualizer`.

| Member | Description |
|---|---|
| `MarinaVisualizerOptions()` | Creates an instance with default values. |
| `WaterSettings Water { get; init; }` | Water surface settings. `WaterSettings.Size` and `WaterSettings.GridResolution` can only be set here. |
| `LightingSettings Lighting { get; init; }` | Initial lighting; can also be changed later through `MarinaVisualizer.Lighting`. |
| `Vector2 WaterCenter { get; init; }` | Center of the water grid in plan coordinates. |
| `MarinaStyle? Style { get; init; }` | The initial style (colors, waves, opacities, ...). When set, its `MarinaStyle.Lighting` and `MarinaStyle.Water` are used instead of `MarinaVisualizerOptions.Lighting` and `MarinaVisualizerOptions.Water`. |

<a id="multiberthselectedeventargs"></a>
### MultiBerthSelectedEventArgs

`sealed class MultiBerthSelectedEventArgs : EventArgs`

Data for `IMarinaVisualizer.MultiBerthSelected`: two or more berths are selected. Fill `MultiBerthSelectedEventArgs.Tooltip` and `MultiBerthSelectedEventArgs.Actions` for the whole selection.

| Member | Description |
|---|---|
| `MultiBerthSelectedEventArgs(IReadOnlyList<Berth> berths, BerthTooltip tooltip, BerthActionCollection actions, SelectionReason reason, bool isNewSelection, PointerButton button = PointerButton.None)` | Creates the event data (raised by the visualizer; hosts normally don't construct it). |
| `IReadOnlyList<Berth> Berths { get; }` | Selected berths in selection order. The last one is `MultiBerthSelectedEventArgs.PrimaryBerth`. |
| `Berth PrimaryBerth { get; }` | The most recently clicked berth; the popup is drawn above it. |
| `IReadOnlyList<string> BerthIds { get; }` | Ids of `MultiBerthSelectedEventArgs.Berths`, in selection order. |
| `IReadOnlyList<Berth> ActionableBerths { get; }` | Selected berths that are not read-only, i.e. the ones actions may apply to. |
| `BerthTooltip Tooltip { get; }` | Pre-filled with a summary of the selection (`DefaultPopupContent.ForBerths`). Edit freely. |
| `BerthActionCollection Actions { get; }` | Empty by default. Add actions that apply to the whole selection. |
| `SelectionReason Reason { get; }` | Why the event was raised: a click, an API call, or a content refresh. |
| `bool IsNewSelection { get; }` | False when the set of selected berths did not change (re-click or refresh). |
| `PointerButton Button { get; }` | Button that triggered the event; `PointerButton.None` for API calls. |
| `bool OpensActions { get; }` | True when the actions window will open (right-click), false for the tooltip. |

<a id="pierupdate"></a>
### PierUpdate

`sealed record PierUpdate`

Partial change to a pier's position, size, orientation, type or name, for `IMarinaVisualizer.UpdatePier(PierUpdate)`. Only non-null members are applied. When the length or heading changes without a new `PierUpdate.Start`, the pier keeps its center.

| Member | Description |
|---|---|
| `PierUpdate(string PierId)` | Partial change to a pier's position, size, orientation, type or name, for `IMarinaVisualizer.UpdatePier(PierUpdate)`. Only non-null members are applied. When the length or heading changes without a new `PierUpdate.Start`, the pier keeps its center. |
| `string PierId { get; init; }` | The pier to change. |
| `string? Name { get; init; }` | New display name. |
| `Vector2? Start { get; init; }` | New shore-end point. Ignored when `PierUpdate.Center` is set. |
| `Vector2? Center { get; init; }` | New center point (applied after length and heading changes). |
| `float? HeadingDegrees { get; init; }` | New heading in degrees. |
| `float? Length { get; init; }` | New length in meters. |
| `float? Width { get; init; }` | New deck width in meters. |
| `PierType? Type { get; init; }` | New construction type (changes the look; the default deck height follows unless `PierUpdate.DeckHeight` was set explicitly). |
| `float? DeckHeight { get; init; }` | New deck height above the water, in meters. |
| `float? PilingSpacing { get; init; }` | New support spacing in meters. |
| `PierSides? BerthingSides { get; init; }` | New berthing sides (single- or double-sided pier). Existing berths are not moved or removed. |
| `PierServices? Services { get; init; }` | New power/water pedestals (drawn beside the pier's berths). |

<a id="rejectedberth"></a>
### RejectedBerth

`sealed record RejectedBerth`

A berth id that `SetSelection` skipped.

| Member | Description |
|---|---|
| `RejectedBerth(string BerthId, BerthSelectionRejection Reason)` | A berth id that `SetSelection` skipped. |
| `string BerthId { get; init; }` | The id as passed in. |
| `BerthSelectionRejection Reason { get; init; }` | Why it was skipped. |

<a id="selectionchangedeventargs"></a>
### SelectionChangedEventArgs

`sealed class SelectionChangedEventArgs : EventArgs`

Data for `IMarinaVisualizer.SelectionChanged`.

| Member | Description |
|---|---|
| `SelectionChangedEventArgs(Berth? previous, Berth? current)` | Creates the event data for a single-berth selection change. |
| `SelectionChangedEventArgs(IReadOnlyList<Berth> previousBerths, IReadOnlyList<Berth> currentBerths)` | Creates the event data. |
| `Berth? Previous { get; }` | The previous primary berth. |
| `Berth? Current { get; }` | The new primary (most recently clicked) berth, or null when the selection was cleared. |
| `IReadOnlyList<Berth> PreviousBerths { get; }` | The previous selection, in selection order. |
| `IReadOnlyList<Berth> CurrentBerths { get; }` | The new selection, in selection order (empty when cleared). |
| `bool IsMultiSelection { get; }` | True when two or more berths are now selected. |

<a id="selectionreason"></a>
### SelectionReason

`enum SelectionReason`

Why a selection event (`IMarinaVisualizer.BerthSelected` / `IMarinaVisualizer.MultiBerthSelected`) was raised.

| Value | Description |
|---|---|
| `Pointer` = 0 | The user clicked a berth (left or right button, possibly with Ctrl). |
| `Api` = 1 | Host code called a selection or popup method (e.g. `SetSelection`, `ShowActions`). |
| `Refresh` = 2 | The selection did not change, but a selected berth's data did (or `IMarinaVisualizer.RefreshPopup` was called) while the popup was open, so the tooltip and actions are being rebuilt. |

<a id="selectionresult"></a>
### SelectionResult

`sealed record SelectionResult`

Outcome of a selection change made through the API.

| Member | Description |
|---|---|
| `SelectionResult(IReadOnlyList<string> SelectedBerthIds, IReadOnlyList<RejectedBerth> Rejected, bool Changed)` | Outcome of a selection change made through the API. |
| `IReadOnlyList<string> SelectedBerthIds { get; init; }` | The selection after the call, in selection order (the last one is primary). |
| `IReadOnlyList<RejectedBerth> Rejected { get; init; }` | Ids that were skipped, with the reason. |
| `bool Changed { get; init; }` | False when the selection was already exactly this. |
| `int Count { get; }` | Number of selected berths. |
| `bool IsEmpty { get; }` | True when nothing is selected. |

<a id="statuscolorscheme"></a>
### StatusColorScheme

`sealed class StatusColorScheme : StyleSection`

Status colors, pad and boat opacities per status, and status markers (`MarinaStyle.Status`). Change the properties directly, or use `MarinaVisualizer.SetStatusColor` and `MarinaVisualizer.SetOverlayOpacity`.

| Member | Description |
|---|---|
| `StatusColorScheme()` | Creates a scheme with the default colors and opacities. |
| `static readonly ColorRgba DefaultFree` | Default Free color (green). |
| `static readonly ColorRgba DefaultOccupied` | Default Occupied color (red). |
| `static readonly ColorRgba DefaultReserved` | Default Reserved color (blue). |
| `static readonly ColorRgba DefaultTemporarilyFree` | Default Temporarily Free color (yellow). |
| `static readonly ColorRgba DefaultDisabled` | Default color of disabled berths (gray). |
| `ColorRgba FreeColor { get; set; }` | Color of Free berths. |
| `ColorRgba OccupiedColor { get; set; }` | Color of Occupied berths. |
| `ColorRgba ReservedColor { get; set; }` | Color of Reserved berths. |
| `ColorRgba TemporarilyFreeColor { get; set; }` | Color of Temporarily Free berths. |
| `ColorRgba DisabledColor { get; set; }` | Pad and marker color of disabled berths (their boats are drawn in gray). |
| `float PadOpacity { get; set; }` | Opacity of the colored berth pads, 0–1 (default 0.45; 0 hides pads except for hover and selection). |
| `float OccupiedBoatOpacity { get; set; }` | Opacity of boats in Occupied berths, 0–1 (default 1; 0 hides them). |
| `float ReservedBoatOpacity { get; set; }` | Opacity of the expected ("ghost") boat in Reserved berths, 0–1 (default 0.4; 0 hides it). |
| `float TemporarilyFreeBoatOpacity { get; set; }` | Opacity of the away ("ghost") boat in Temporarily Free berths, 0–1 (default 0.4; 0 hides it). |
| `float GhostBoatOpacity { get; set; }` | Opacity of the "ghost" boats of reserved and temporarily free berths. Reading returns `StatusColorScheme.ReservedBoatOpacity`; setting changes both ghost opacities. |
| `float GhostBoatTint { get; set; }` | How strongly ghost boats take on their status color, 0 (white) to 1 (status color); default 0.55. |
| `bool ShowStatusMarkers { get; set; }` | Show the colored status buoys on the water (posts for land berths). Default true. |
| `float StatusMarkerScale { get; set; }` | Size multiplier of status buoys and posts, 0.2–5 (default 1). |
| `ColorRgba Get(BerthStatus status)` | The color of a status (gray for unknown values). |
| `float GetBoatOpacity(BerthStatus status)` | Opacity of the boat drawn for a status (0 for Free). |
| `void Reset()` | Restores the default colors, opacities and markers. |
| `void Set(BerthStatus status, ColorRgba color)` | Changes the color of a status. |

## VirtualMarina.Core.Camera

<a id="cameraangle"></a>
### CameraAngle

`readonly record struct CameraAngle`

Viewing direction for focusing the camera: where it sits around the target, and how steeply it looks down.

| Member | Description |
|---|---|
| `CameraAngle(float YawDegrees, float PitchDegrees)` | Viewing direction for focusing the camera: where it sits around the target, and how steeply it looks down. |
| `float YawDegrees { get; init; }` | Compass angle of the camera around the target (0° = camera on the +Z side looking toward −Z, 180° = on the −Z side looking toward +Z). |
| `float PitchDegrees { get; init; }` | Elevation above the horizon: 90° looks straight down. Clamped by `CameraConstraints` (at most 89°). |
| `static CameraAngle TopDown { get; }` | Straight down, with +Z (away from the shore in the usual layout) at the top of the screen. |
| `static CameraAngle Of(CameraPose pose)` | The angle of an existing pose. |
| `static CameraAngle TopDownFacing(float yawDegrees)` | Straight down with the given compass orientation. |

<a id="cameraconstraints"></a>
### CameraConstraints

`sealed class CameraConstraints`

Limits that keep the camera usable: no flipping, no dipping under water, no flying away.

| Member | Description |
|---|---|
| `CameraConstraints()` | Creates an instance with default values. |
| `float MinPitchDegrees { get; set; }` | Lowest elevation angle. Keeps the view above the horizon. |
| `float MaxPitchDegrees { get; set; }` | Highest elevation angle. Must stay below 90° so the view never flips over. |
| `float MinDistance { get; set; }` | Closest the camera may get to its target, in meters. |
| `float MaxDistance { get; set; }` | Farthest the camera may get from its target, in meters (the visualizer raises it to fit the layout). |
| `float MinEyeHeight { get; set; }` | Minimum eye height above the water plane, in meters. |
| `Vector2 TargetBoundsMin { get; set; }` | Minimum corner of the plan-view region (X, Z) the target may move within (layout bounds plus a margin). |
| `Vector2 TargetBoundsMax { get; set; }` | Maximum corner of the plan-view region (X, Z) the target may move within. |

<a id="camerapose"></a>
### CameraPose

`readonly record struct CameraPose`

An orbit-camera state: the point looked at, plus spherical offset angles and distance.

| Member | Description |
|---|---|
| `CameraPose(Vector3 Target, float YawDegrees, float PitchDegrees, float Distance)` | An orbit-camera state: the point looked at, plus spherical offset angles and distance. |
| `Vector3 Target { get; init; }` | Focus point (usually on the water plane). |
| `float YawDegrees { get; init; }` | Compass angle of the camera around the target (0° = camera on the +Z side). |
| `float PitchDegrees { get; init; }` | Elevation above the horizon (90° = straight down). |
| `float Distance { get; init; }` | Distance from target to eye in meters. |

<a id="camerapreset"></a>
### CameraPreset

`sealed record CameraPreset`

A named viewpoint staff can jump to (`IMarinaVisualizer.ApplyCameraPreset`).

| Member | Description |
|---|---|
| `CameraPreset(string Name, CameraPose Pose, string? Description = null)` | A named viewpoint staff can jump to (`IMarinaVisualizer.ApplyCameraPreset`). |
| `string Name { get; init; }` | Unique name (case-insensitive), e.g. "Fuel pier". |
| `CameraPose Pose { get; init; }` | Camera target, angles and distance. |
| `string? Description { get; init; }` | Optional description, e.g. for a tooltip in a preset menu. |
| `bool IsBuiltIn { get; init; }` | True for presets generated automatically from the layout (rebuilt when the layout changes). |

<a id="orbitcamera"></a>
### OrbitCamera

`sealed class OrbitCamera`

Smoothed orbit/pan/zoom camera. Input changes the `OrbitCamera.DesiredPose`; `OrbitCamera.Update` eases the current `OrbitCamera.Pose` toward it frame by frame.

| Member | Description |
|---|---|
| `OrbitCamera()` | Creates an instance with default values. |
| `CameraConstraints Constraints { get; }` | Limits on pitch, distance, eye height and target area. The visualizer updates the distance and target limits when the layout changes. |
| `float FieldOfViewDegrees { get; set; }` | Vertical field of view in degrees (default 45). |
| `float NearPlane { get; set; }` | Near clipping plane in meters. |
| `float FarPlane { get; set; }` | Far clipping plane in meters (raised automatically for large marinas). |
| `float Smoothing { get; set; }` | Exponential smoothing rate (1/s). Higher is snappier; 0 or less disables smoothing. |
| `CameraPose Pose { get; }` | The pose currently on screen. |
| `CameraPose DesiredPose { get; }` | The pose the camera is easing toward. |
| `Vector3 Position { get; }` | World-space eye position of the current pose. |
| `bool IsMoving { get; }` | True while the camera is still easing toward `OrbitCamera.DesiredPose`. |
| `static Vector3 ComputeEye(CameraPose pose)` | World-space eye position for a pose. |
| `CameraPose Constrain(CameraPose pose)` | Applies `OrbitCamera.Constraints` to a pose. |
| `Matrix4x4 GetProjectionMatrix(float aspectRatio)` | OpenGL-style perspective projection (clip Z in −1..1) for the given width/height ratio. |
| `Matrix4x4 GetViewMatrix()` | View matrix of the current pose (System.Numerics row-vector convention). |
| `void Orbit(float deltaYawDegrees, float deltaPitchDegrees)` | Rotates the desired pose around its target (pitch is clamped by `OrbitCamera.Constraints`). |
| `void Pan(float deltaXPixels, float deltaYPixels, float viewportHeightPixels)` | Drags the scene by a screen-space delta so the ground under the pointer follows it. |
| `void PanWorld(float rightMeters, float forwardMeters)` | Moves the target along the ground in the camera's own orientation (meters). |
| `Ray ScreenPointToRay(float x, float y, float viewportWidth, float viewportHeight)` | World-space ray through a pixel (origin top-left) of the current view. |
| `void SetPose(CameraPose pose, bool immediate = false)` | Moves to a pose, animating unless `immediate` is set. Yaw takes the shortest way round. |
| `void Update(float deltaSeconds)` | Eases the current pose toward the desired pose. Called once per frame by the visualizer. |
| `Vector2? WorldToScreen(Vector3 world, float viewportWidth, float viewportHeight)` | Projects a world position to view pixels (origin top-left); null when behind the camera. |
| `void Zoom(float factor, Vector3? focusPoint = null)` | Zooms by a factor (>1 moves closer). With a focus point, the view zooms toward it (e.g. the spot under the mouse cursor). |

## VirtualMarina.Core.Design

<a id="berthnamingscheme"></a>
### BerthNamingScheme

`sealed record BerthNamingScheme`

How the designer names the berths it draws: the pattern, the first number and how far it counts on. Set it on `MarinaDesigner.BerthNaming`.

The pattern is plain text with tokens in braces, written into every new berth's name (which is also its id, so a pattern with no number in it only ever names one berth — the rest fall back to a numbered form): `{pier}`Id of the pier the berth belongs to, or of the land area for a berth ashore `{pierName}`The pier's display name `{side}``BerthNamingScheme.LeftSide` or `BerthNamingScheme.RightSide`; empty for a berth ashore `{number}`The running number, padded to `BerthNamingScheme.NumberDigits` digits Numbering starts at `BerthNamingScheme.StartNumber` and goes up by `BerthNamingScheme.Increment`. Names already taken are skipped, so a second row on the same side carries on after the first instead of clashing with it.

| Member | Description |
|---|---|
| `BerthNamingScheme()` | Creates an instance with default values. |
| `static readonly BerthNamingScheme Default` | The default scheme: `{pier}-{side}{number}` from 1 upward, two digits, e.g. `A-L01`. |
| `string Pattern { get; init; }` | Pattern for a berth on a pier. Default `{pier}-{side}{number}`. |
| `string? LandPattern { get; init; }` | Pattern for a slot ashore; null (the default) uses `BerthNamingScheme.Pattern`, where `{side}` is empty. |
| `int? LandStartNumber { get; init; }` | The first number offered to a slot ashore; null (the default) counts from `BerthNamingScheme.StartNumber`. |
| `int? LandIncrement { get; init; }` | Step between slots ashore; null (the default) uses `BerthNamingScheme.Increment`. |
| `int? LandNumberDigits { get; init; }` | Digits a slot ashore is padded to; null (the default) uses `BerthNamingScheme.NumberDigits`. |
| `int StartNumber { get; init; }` | The first number offered. Default 1. |
| `int Increment { get; init; }` | Step from one berth to the next. Default 1. |
| `int NumberDigits { get; init; }` | Digits the number is padded to with leading zeros; 1 writes it as it is. Default 2, so 1 becomes `01`. |
| `string LeftSide { get; init; }` | What `{side}` becomes on the left-hand side of a pier. Default `L`. |
| `string RightSide { get; init; }` | What `{side}` becomes on the right-hand side of a pier. Default `R`. |
| `string Format(Pier pier, PierSide side, int number)` | The name this scheme gives berth number `number` on one side of a pier. |
| `string Format(LandArea landArea, int number)` | The name this scheme gives berth number `number` on a land area. |
| `IEnumerable<string> Validate()` | Problems that would stop the scheme from naming anything, empty when it is sound. |

<a id="berthseparator"></a>
### BerthSeparator

`enum BerthSeparator`

What separates the berths the designer adds (`MarinaDesigner.BerthSeparators`). Except for `BerthSeparator.FingerPiers` and `BerthSeparator.None`, each value generates `Divider` elements shared by neighbouring berths.

| Value | Description |
|---|---|
| `FingerPiers` = 0 | The berths' own finger piers along both long sides (`Berth.HasFingerPiers`). Default. |
| `None` = 1 | Nothing between the berths but the gap (`MarinaDesigner.BerthGap`, at least `MarinaDesigner.MinimumSeparatorGap`). |
| `FingerPier` = 2 | A walkable finger pier between neighbours (`DividerType.FingerPier`). |
| `PairedFingerPiers` = 6 | A finger pier at every other boundary, so the berths sit in pairs: each boat has a pier on one side and its neighbour on the other, and the boats at the ends of the row get a pier on their outer side. |
| `Piles` = 3 | A row of mooring piles (`DividerType.Piles`). |
| `Boom` = 4 | A floating boom (`DividerType.Boom`). |
| `SinglePile` = 5 | A single mooring pile at the outer end of each berth boundary (`DividerType.SinglePile`). |

<a id="designactionundoneeventargs"></a>
### DesignActionUndoneEventArgs

`sealed class DesignActionUndoneEventArgs : EventArgs`

Data for `MarinaDesigner.ActionUndone`: the last designer change was reverted.

| Member | Description |
|---|---|
| `DesignActionUndoneEventArgs(string description, int remainingSteps)` | Creates the event data. |
| `string Description { get; }` | What was undone, e.g. "Add 4 berths". |
| `int RemainingSteps { get; }` | How many steps can still be undone. |

<a id="designdraftchange"></a>
### DesignDraftChange

`enum DesignDraftChange`

What happened to the drawing in progress (`MarinaDesigner.DraftChanged`).

| Value | Description |
|---|---|
| `PointAdded` = 0 | A point was added (a land outline point, a pier's start, the first end of a berth row or scale line). |
| `PointRemoved` = 1 | The last point was removed (Backspace). |
| `Canceled` = 2 | The drawing was abandoned (Escape, tool change, designer turned off). |
| `Completed` = 3 | The drawing was finished and turned into an element (or a scale line). |

<a id="designdraftchangedeventargs"></a>
### DesignDraftChangedEventArgs

`sealed class DesignDraftChangedEventArgs : EventArgs`

Data for `MarinaDesigner.DraftChanged`: the user is drawing.

| Member | Description |
|---|---|
| `DesignDraftChangedEventArgs(DesignTool tool, DesignDraftChange change, IReadOnlyList<Vector2> points)` | Creates the event data. |
| `DesignTool Tool { get; }` | The tool being used. |
| `DesignDraftChange Change { get; }` | What happened. |
| `IReadOnlyList<Vector2> Points { get; }` | The points placed so far, in plan coordinates (a snapshot; empty after `DesignDraftChange.Canceled`). |

<a id="designelementcreatedeventargs"></a>
### DesignElementCreatedEventArgs

`sealed class DesignElementCreatedEventArgs : EventArgs`

Data for `MarinaDesigner.ElementCreated`: the element was added to the marina (a `LayoutChanged` was raised too).

| Member | Description |
|---|---|
| `DesignElementCreatedEventArgs(DesignTool tool, LandArea? landArea, Pier? pier, IReadOnlyList<Berth> berths, IReadOnlyList<Divider> dividers)` | Creates the event data. |
| `DesignTool Tool { get; }` | The tool that produced the element. |
| `LandArea? LandArea { get; }` | The land area added, if any. |
| `Pier? Pier { get; }` | The pier added, if any. |
| `IReadOnlyList<Berth> Berths { get; }` | The berths added (empty unless berths were added). |
| `IReadOnlyList<Divider> Dividers { get; }` | The dividers added with the berths. |
| `Shoreline? Shoreline { get; init; }` | The mainland that was set (`DesignTool.DrawShoreline`), if any. |

<a id="designelementcreatingeventargs"></a>
### DesignElementCreatingEventArgs

`sealed class DesignElementCreatingEventArgs : EventArgs`

Data for `MarinaDesigner.ElementCreating`: the user finished drawing and the element is about to be added. Handlers can change it (e.g. assign ERP ids or names) or cancel.

| Member | Description |
|---|---|
| `DesignElementCreatingEventArgs(DesignTool tool, LandArea? landArea, Pier? pier, IReadOnlyList<Berth> berths, IReadOnlyList<Divider> dividers)` | Creates the event data. |
| `DesignTool Tool { get; }` | The tool that produced the element. |
| `LandArea? LandArea { get; set; }` | The land area to add (`DesignTool.DrawLandArea`). Replace it to change the id, name or outline. |
| `Pier? Pier { get; set; }` | The pier to add (`DesignTool.DrawPier`). If you change its id, berths created later reference the new id. |
| `IReadOnlyList<Berth> Berths { get; set; }` | The berths to add (`DesignTool.AddBerths`, or the one land berth of `DesignTool.AddLandBerths`); empty for other tools. |
| `IReadOnlyList<Divider> Dividers { get; set; }` | Dividers generated between the new berths, when `MarinaDesigner.BerthSeparators` asks for them. |
| `Shoreline? Shoreline { get; set; }` | The mainland to set (`DesignTool.DrawShoreline`); null for other tools. Replace it to change its height, surface or scenery before it is drawn. |
| `bool Cancel { get; set; }` | Set to true to discard the drawing. |

<a id="designelementerasedeventargs"></a>
### DesignElementErasedEventArgs

`sealed class DesignElementErasedEventArgs : EventArgs`

Data for `MarinaDesigner.ElementErased`.

| Member | Description |
|---|---|
| `DesignElementErasedEventArgs(object element, IReadOnlyList<Berth> removedBerths, IReadOnlyList<Divider> removedDividers)` | Creates the event data. |
| `object Element { get; }` | The removed `Berth`, `Pier` or `LandArea`. |
| `IReadOnlyList<Berth> RemovedBerths { get; }` | Every berth removed (the berth itself, or the pier's or land area's berths). |
| `IReadOnlyList<Divider> RemovedDividers { get; }` | Dividers removed with them: a pier's dividers, or the ones left without a berth on either side. A divider still shared with a remaining berth stays. |

<a id="designelementrenamingeventargs"></a>
### DesignElementRenamingEventArgs

`sealed class DesignElementRenamingEventArgs : EventArgs`

A berth or pier is about to be renamed (`MarinaDesigner.ElementRenaming`). Put the new name in `DesignElementRenamingEventArgs.NewName` — or leave it as it is, or set `DesignElementRenamingEventArgs.Cancel`, to leave the element alone.

| Member | Description |
|---|---|
| `DesignElementRenamingEventArgs(Berth? berth, Pier? pier, string currentName)` | Creates the arguments. |
| `Berth? Berth { get; }` | The berth being renamed, or null when a pier is. |
| `Pier? Pier { get; }` | The pier being renamed, or null when a berth is. |
| `string CurrentName { get; }` | The name the element has now: a berth's id, or a pier's display name. |
| `string NewName { get; set; }` | The name to give it. Starts as `DesignElementRenamingEventArgs.CurrentName`; leaving it unchanged does nothing. |
| `string? NewPierId { get; set; }` | For a pier, the id to give it, which its berths and dividers follow. Starts as the pier's current id; leaving it unchanged moves nothing. Ignored for a berth, whose name is its id. |
| `bool Cancel { get; set; }` | Set to true to leave the element alone. |

<a id="designtool"></a>
### DesignTool

`enum DesignTool`

What clicks in the 3D view do while `MarinaDesigner.IsActive` is true.

| Value | Description |
|---|---|
| `Navigate` = 0 | Clicks do nothing; dragging, the wheel and the keyboard move the camera as usual. |
| `DrawLandArea` = 1 | Click to add outline points of a new `LandArea` (kind and height from the designer settings). Finish with a double-click, Enter, a right-click or a click on the first point; Backspace removes the last point; Escape cancels. |
| `DrawPier` = 2 | Click the shore end, then the far end of a new `Pier` (type, width and berthing sides from the settings). Shift snaps the angle to 15°. |
| `AddBerths` = 3 | Click beside a pier where the first berth should go, then click where the row should end: berths of the configured width, length and depth fill the space on that side. Clicking twice in the same place adds one berth. |
| `Erase` = 4 | Click a berth, pier or land area to remove it (a pier or land area is removed with its berths). |
| `MoveReferenceImage` = 5 | Drag with the left button to move the reference image. |
| `MeasureScale` = 6 | Click both ends of the image's scale bar. `MarinaDesigner.ScaleLineDrawn` is raised; then call `MarinaDesigner.CalibrateReferenceImage` with the length the bar represents. |
| `PlantTrees` = 7 | Click a lawn (`LandKind.Grass`) to scatter trees on it at `MarinaDesigner.TreeDensity`. Each click replaces the trees it has with new, randomly placed ones; Ctrl+click (or a right-click) removes them. |
| `AddLandBerths` = 8 | Click a land area to put a land berth (`Berth.OnLand`) there, then click again to aim its bow (Shift snaps to 15°). Clicking the same spot twice uses `MarinaDesigner.LandBerthHeading`. |
| `Rename` = 9 | Click a berth or a pier to give it another name. The designer asks the host for the new name through `MarinaDesigner.ElementRenaming`, so the application decides how to ask for it. |
| `EditServices` = 10 | Click a berth to give it the pedestals in `MarinaDesigner.BerthServices`; hold Alt or Ctrl to change every berth down that side of the pier at once. The berths about to change are highlighted. |
| `SelectArea` = 11 | Drag a box over the water to select every berth whose middle falls inside it. Hold Shift or Ctrl to add to the selection already made instead of replacing it. |
| `DrawShoreline` = 12 | Draw the coast of the mainland behind the marina (`Shoreline`). Click to place points along it — two are enough for a straight coast — then press Enter, and click the side of the line that is land. Backspace removes the last point; Escape cancels. Drawing a new one replaces the one already there. |

<a id="designtoolchangedeventargs"></a>
### DesignToolChangedEventArgs

`sealed class DesignToolChangedEventArgs : EventArgs`

Data for `MarinaDesigner.ToolChanged`.

| Member | Description |
|---|---|
| `DesignToolChangedEventArgs(DesignTool previous, DesignTool current)` | Creates the event data. |
| `DesignTool Previous { get; }` | The tool before the change. |
| `DesignTool Current { get; }` | The tool now active. |

<a id="designtreesplantedeventargs"></a>
### DesignTreesPlantedEventArgs

`sealed class DesignTreesPlantedEventArgs : EventArgs`

Data for `MarinaDesigner.TreesPlanted` (raised when trees are scattered or removed).

| Member | Description |
|---|---|
| `DesignTreesPlantedEventArgs(LandArea landArea, int previousCount)` | Creates the event data. |
| `LandArea LandArea { get; }` | The land area with its new `LandArea.Trees` (empty after a removal). |
| `int PreviousCount { get; }` | How many trees it had before. |

<a id="designersettings"></a>
### DesignerSettings

`sealed record DesignerSettings`

The designer's tool settings as a plain value: what the next land area, pier, berth row or land berth will look like. Stored in a marina file (`MarinaDocument.Designer`) so a design reopens the way it was left, and handy for presets ("small boats", "superyachts").

| Member | Description |
|---|---|
| `DesignerSettings()` | Creates an instance with default values. |
| `LandKind LandKind { get; init; }` | Kind of land area the land tool draws. |
| `float LandHeight { get; init; }` | Height of new land areas above the water, in meters. |
| `float TreeDensity { get; init; }` | Trees per 1000 m² on new lawns and from the tree tool. |
| `PierType PierType { get; init; }` | Construction of new piers. |
| `float PierWidth { get; init; }` | Deck width of new piers, in meters. |
| `PierSides PierBerthingSides { get; init; }` | Sides of new piers where boats berth. |
| `float BerthWidth { get; init; }` | Width of new berths, in meters. |
| `float BerthLength { get; init; }` | Length of new berths, in meters. |
| `float BerthDepth { get; init; }` | Water depth of new berths, in meters. |
| `BerthSeparator BerthSeparators { get; init; }` | What separates new berths. |
| `float BerthGap { get; init; }` | Space between neighbouring berths, in meters. |
| `bool AlignBerthsToExisting { get; init; }` | Line new rows up with the berths already on that side of the pier. |
| `PierServices BerthServices { get; init; }` | Power/water pedestals switched on for a pier when berths are added to it. |
| `float LandBerthHeading { get; init; }` | Bow direction of new land berths, in degrees. |
| `float SnapDistancePixels { get; init; }` | Snapping distance of the drawing tools, in pixels. |
| `string PierNamePattern { get; init; }` | Name given to a new pier, where `{pier}` stands for its generated id. |
| `BerthNamingScheme BerthNaming { get; init; }` | How new berths are named: the pattern, the first number and the step between them. |
| `void ApplyTo(MarinaDesigner designer)` | Applies these settings to a designer. Values outside the allowed ranges are clamped instead of throwing. |
| `static DesignerSettings FromDesigner(MarinaDesigner designer)` | Reads the current settings of a designer. |

<a id="marinadesigner"></a>
### MarinaDesigner

`sealed class MarinaDesigner`

Interactive layout designer: draw land areas as polygons, draw piers, add berths along piers, put boats ashore on land berths, plant trees and erase elements directly in the 3D view, with undo, optionally tracing a calibrated aerial image. Available as `MarinaVisualizer.Designer`.

Turn it on with `MarinaDesigner.IsActive` and pick a `MarinaDesigner.Tool`. While active, clicks go to the designer instead of selecting berths; dragging, the wheel and the keyboard still move the camera. The host views (and the ready-made designer panels for WinForms and Blazor) forward input as usual, so no extra wiring is needed. Everything the designer creates goes through the normal API (`AddLandArea`, `AddPier`, `AddBerths`, ...), so `LayoutChanged` is raised as well as the designer's own events, and every change it makes can be taken back with `MarinaDesigner.Undo` (Ctrl+Z in the views). Use `MarinaVisualizer.ExportObjects` or `MarinaVisualizer.GetLayout` to save the result. Coordinates: north is −Z (plan −Y) and east is +X, so an image with north at the top lies unmirrored under a camera with yaw 0 (see `MarinaDesigner.ViewTopDown`).

| Member | Description |
|---|---|
| `const float MinimumPierLength = 1f` | Shortest pier the `DesignTool.DrawPier` tool creates, in meters. |
| `const float MinimumSeparatorGap = 0.3f` | Gap left between berths separated by `BerthSeparator.None` when `MarinaDesigner.BerthGap` is smaller, in meters. |
| `const int MaxUndoSteps = 50` | How many changes `MarinaDesigner.Undo` can step back through (older ones are dropped). |
| `bool IsActive { get; set; }` | Designer mode. Turning it on closes the popup, clears the selection and hover, and routes clicks to `MarinaDesigner.Tool`. Turning it off abandons any drawing in progress. |
| `DesignTool Tool { get; set; }` | What clicks do while `MarinaDesigner.IsActive`. Changing it abandons any drawing in progress. |
| `string ToolHint { get; }` | A one-line instruction for the current tool and drawing state, for a status bar or panel. |
| `LandKind LandKind { get; set; }` | Kind of land area drawn by `DesignTool.DrawLandArea`. Default `LandKind.Quay`. |
| `float LandHeight { get; set; }` | Top height of new land areas above the water, 0–50 m (default 1). |
| `HinterlandScenery Scenery { get; set; }` | What is scattered across the mainland drawn by `DesignTool.DrawShoreline`. Default `HinterlandScenery.Countryside`. |
| `float TreeDensity { get; set; }` | Trees per 1000 m² scattered on new lawns (`LandKind.Grass`) and by `DesignTool.PlantTrees`, 0–100 (default 8; 0 = no trees). Positions are random when drawn and then stored with the land area, so they stay put. |
| `PierType PierType { get; set; }` | Construction of piers drawn by `DesignTool.DrawPier`. Default `PierType.FloatingWooden`. |
| `float PierWidth { get; set; }` | Deck width of new piers, 0.5–30 m (default 2.5). |
| `PierSides PierBerthingSides { get; set; }` | Berthing sides of new piers (`Pier.BerthingSides`). Default `PierSides.Both`. |
| `float BerthWidth { get; set; }` | Width of new berths (along the pier), 1–50 m (default 5). |
| `float BerthLength { get; set; }` | Length of new berths (away from the pier), 1–150 m (default 12). |
| `float BerthDepth { get; set; }` | Water depth of new berths, stored as `Berth.MaxDraft`, 0.1–50 m (default 3). |
| `BerthSeparator BerthSeparators { get; set; }` | What separates new berths: their own finger piers (default), nothing at all, or generated `Divider` elements (finger pier, piles, boom or a single pile at the outer end). |
| `float BerthGap { get; set; }` | Space left between neighbouring berths, 0–20 m (default 0, berths touching). With `BerthSeparator.None` at least `MarinaDesigner.MinimumSeparatorGap` is used, so the berths never touch without a separator. |
| `bool AlignBerthsToExisting { get; set; }` | True (default): a row of berths lines up with the nearest existing berth edge on that side, or with the pier's start, in whole berth pitches. False: the first berth starts exactly where you click (or at `fromAlong`), at any offset from the pier's start. |
| `PierServices BerthServices { get; set; }` | Power/water pedestals switched on for a pier when berths are added to it (default `PierServices.None`). They are drawn on the berthing sides only, next to the berths that exist (see `Pier.Services`). |
| `BerthNamingScheme BerthNaming { get; set; }` | How the berths drawn from now on are named: the pattern, the first number and the step between them (default `BerthNamingScheme.Default`, giving `A-L01`, `A-L02`, ...). |
| `string PierNamePattern { get; set; }` | Name given to a pier as it is drawn (default `Pier {pier}`, e.g. "Pier A"). `{pier}` stands for the pier's generated id; text without it names every new pier the same, which is allowed — names need not be unique. |
| `float LandBerthHeading { get; set; }` | Direction a boat stored on a land berth points, in degrees (0° = +Z, 90° = +X; default 0). Used by `DesignTool.AddLandBerths` when both clicks land on the same spot, and by `MarinaDesigner.CreateLandBerth` by default. |
| `float SnapDistancePixels { get; set; }` | Points within this many pixels of an existing corner, pier end or land edge snap to it (default 12, 0 turns snapping off). Holding Alt also turns it off. |
| `float FogFactor { get; set; }` | Multiplier for the distance fog while the designer is active (default 0.15; 1 keeps the normal fog), so the marina and the reference image stay clear when seen from high above. |
| `IReadOnlyList<Vector2> DraftPoints { get; }` | Points placed so far in the current drawing, in plan coordinates. |
| `bool HasDraft { get; }` | True while a drawing is in progress (at least one point placed). |
| `Vector2? PointerPosition { get; }` | Plan position under the pointer (after snapping), or null when the pointer isn't over the view. |
| `IReadOnlyList<Vector2>? ShorelineAwaitingSide { get; }` | The coast that has been drawn and is waiting for a click to say which side of it is land, or null. Enter moves a `DesignTool.DrawShoreline` drawing into this state (see `MarinaDesigner.PickShorelineSide`). |
| `bool CanUndo { get; }` | True when `MarinaDesigner.Undo` would revert something. |
| `string? UndoDescription { get; }` | What the next `MarinaDesigner.Undo` would revert (e.g. "Add 4 berths"), or null when there is nothing to undo. |
| `int UndoCount { get; }` | How many changes can still be undone (at most `MarinaDesigner.MaxUndoSteps`). |
| `ReferenceImage? ReferenceImage { get; }` | The image shown to trace the marina, or null. |
| `Vector2 ReferenceImageCenter { get; set; }` | Center of the reference image in plan coordinates. |
| `float ReferenceImageMetersPerPixel { get; set; }` | Ground size of one image pixel, in meters. Set it directly or with `MarinaDesigner.CalibrateReferenceImage`. |
| `float ReferenceImageOpacity { get; set; }` | Opacity of the reference image, 0–1 (default 0.6). |
| `bool ReferenceImageVisible { get; set; }` | Show the reference image (default true). It is shown whether or not the designer is active. |
| `bool ReferenceImageAboveScene { get; set; }` | True (default): the image is drawn over land, piers and boats, so it stays visible while tracing. False: it lies on the water and land and structures hide it. Drawing previews are always on top. |
| `Vector2 ReferenceImageSize { get; }` | Ground size of the reference image in meters (X = east–west, Y = north–south), or zero without an image. |
| `ValueTuple<Vector2, Vector2>? ScaleLine { get; }` | The last line drawn with `DesignTool.MeasureScale`, or null. |
| `ValueTuple<Vector2, Vector2>? ReferenceImageBounds { get; }` | Plan-view bounds of the reference image (north-west and south-east corners), or null without an image. |
| `ValueTuple<Vector2, Vector2>? SelectionBox { get; }` | The box being dragged with `DesignTool.SelectArea`, in plan coordinates, or null. |
| `event EventHandler? ActiveChanged` | `MarinaDesigner.IsActive` changed. |
| `event EventHandler<DesignToolChangedEventArgs>? ToolChanged` | `MarinaDesigner.Tool` changed. |
| `event EventHandler<DesignDraftChangedEventArgs>? DraftChanged` | The user placed or removed a point, or finished or abandoned a drawing. |
| `event EventHandler<DesignElementCreatingEventArgs>? ElementCreating` | A drawing is complete and its element is about to be added; handlers can change or cancel it. |
| `event EventHandler<DesignElementCreatedEventArgs>? ElementCreated` | A land area, pier or berths were added by the designer. |
| `event EventHandler<DesignElementRenamingEventArgs>? ElementRenaming` | A berth or pier was clicked with the `DesignTool.Rename` tool: put the new name in `DesignElementRenamingEventArgs.NewName`. Without a handler the tool does nothing. |
| `event EventHandler<DesignElementErasedEventArgs>? ElementErased` | A berth, pier or land area was removed with the `DesignTool.Erase` tool (or `MarinaDesigner.Erase`). |
| `event EventHandler<DesignTreesPlantedEventArgs>? TreesPlanted` | Trees were scattered on a land area (`DesignTool.PlantTrees`, `MarinaDesigner.PlantTrees`, or a new lawn). |
| `event EventHandler<DesignActionUndoneEventArgs>? ActionUndone` | The last change was reverted by `MarinaDesigner.Undo`. |
| `event EventHandler<ScaleLineDrawnEventArgs>? ScaleLineDrawn` | A scale line was drawn with `DesignTool.MeasureScale`. |
| `event EventHandler<ReferenceImageChangedEventArgs>? ReferenceImageChanged` | The reference image was set, cleared, moved, scaled or restyled. |
| `event EventHandler? StateChanged` | Any designer state changed: activity, tool, a setting, the drawing in progress, the scale line or the reference image. Designer panels listen to this to refresh their controls. |
| `bool CalibrateReferenceImage(float knownLengthMeters)` | Rescales the reference image so the last `MarinaDesigner.ScaleLine` becomes `knownLengthMeters` long. The image scales about the line's first end, which stays put. Returns false without an image or scale line. |
| `bool CalibrateReferenceImage(Vector2 start, Vector2 end, float knownLengthMeters)` | Rescales the reference image so the segment `start`–`end` (in plan coordinates, drawn over the image at its current scale) becomes `knownLengthMeters` long. `start` stays put. |
| `bool CancelDraft()` | Abandons the drawing in progress. Returns false when there was none. |
| `Pier ChangePierId(string pierId, string newPierId)` | Gives a pier another id, which its berths and dividers follow, and records the change for `MarinaDesigner.Undo`. Returns the pier under its new id. |
| `void ClearHistory()` | Forgets every recorded change, so `MarinaDesigner.Undo` does nothing until the designer changes something again. |
| `void ClearReferenceImage()` | Removes the reference image and the scale line. |
| `bool ClearScaleLine()` | Forgets the measuring line, once the image has been scaled by it and the line is only in the way. Returns false when there was none. |
| `bool CompleteDraft()` | Finishes the drawing in progress: closes a land outline (3+ points), ends a pier at the pointer, or adds the anchored berth. Returns true when an element was created. |
| `IReadOnlyList<Berth> CreateBerths(string pierId, PierSide side, float fromAlong, float toAlong)` | Adds a row of berths of the current `MarinaDesigner.BerthWidth`, `MarinaDesigner.BerthLength` and `MarinaDesigner.BerthDepth` on one side of a pier, covering the stretch between two distances from the pier's start (equal distances add one berth), separated by `MarinaDesigner.BerthSeparators` and `MarinaDesigner.BerthGap`. With `MarinaDesigner.AlignBerthsToExisting` the row lines up with existing berths on that side; otherwise it starts exactly at `fromAlong`. Places already taken are skipped, and `MarinaDesigner.BerthServices` switches the pier's pedestals on. Returns the berths added (empty when none fit or a handler cancels). |
| `LandArea? CreateLandArea(IReadOnlyList<Vector2> outline)` | Adds a land area with the given outline and the current `MarinaDesigner.LandKind` and `MarinaDesigner.LandHeight`, raising `MarinaDesigner.ElementCreating` and `MarinaDesigner.ElementCreated`. Returns null when a handler cancels. |
| `Berth? CreateLandBerth(string landAreaId, Vector2 position, float? headingDegrees = null)` | Adds a land berth (`Berth.OnLand`) of the current `MarinaDesigner.BerthWidth` and `MarinaDesigner.BerthLength` to a land area, where a boat is stored or worked on ashore. Raises `MarinaDesigner.ElementCreating` and `MarinaDesigner.ElementCreated`; returns null when a handler cancels. |
| `Pier? CreatePier(Vector2 start, Vector2 end)` | Adds a pier from `start` (shore end) to `end` with the current `MarinaDesigner.PierType`, `MarinaDesigner.PierWidth` and `MarinaDesigner.PierBerthingSides`. Returns null when a handler cancels. |
| `Shoreline? CreateShoreline(IReadOnlyList<Vector2> line, bool landOnLeft)` | Sets the mainland behind the marina from a drawn coast and the side of it that is land, using the current `MarinaDesigner.Scenery`. Whatever mainland was there is replaced. Returns null when a handler cancels. |
| `bool DeleteShoreline()` | Takes the mainland away, leaving the marina in open water. Returns false when there was none. |
| `bool Erase(object element)` | Removes a berth, or a pier or land area with its berths, and raises `MarinaDesigner.ElementErased`. Dividers left without a berth on either side go too (a pier takes all of its dividers). Returns false when the element doesn't exist. |
| `bool EraseBerthsOfPier(string pierId)` | Removes every berth on a pier, and the separators that only served them, leaving the pier itself in place. This is what the eraser does when Alt is held over one of the pier's berths. Records one step for `MarinaDesigner.Undo` and raises `MarinaDesigner.ElementErased` with the pier as the element. |
| `bool FocusReferenceImage(bool immediate = false)` | Looks straight down, north up, at the whole reference image. Returns false without an image. |
| `Shoreline? PickShorelineSide(Vector2 landSide)` | Makes the mainland from the coast waiting for a side, putting the land on the side `landSide` falls on. Returns null when nothing is waiting, or when a handler cancels. |
| `LandArea? PlantTrees(string landAreaId, float? treesPer1000SquareMeters = null)` | Replaces the trees of a lawn with new, randomly placed ones (kept clear of its land berths) and raises `MarinaDesigner.TreesPlanted`. Returns the updated land area, or null when it doesn't exist or is not a `LandKind.Grass` area. |
| `bool RemoveLastPoint()` | Removes the last placed point. Returns false when there was none. |
| `LandArea? RemoveTrees(string landAreaId)` | Removes every tree from a land area (of any kind) and raises `MarinaDesigner.TreesPlanted` with an empty `LandArea.Trees`. Returns the updated land area, or null when it doesn't exist or has no trees. |
| `Berth RenameBerth(string berthId, string newBerthId)` | Gives one berth another name, keeping everything else about it, and records the change for `MarinaDesigner.Undo`. Returns the renamed berth. |
| `Pier RenamePier(string pierId, string name)` | Gives a pier a display name (`Pier.Name`), the one shown in tooltips and the camera preset, and records the change for `MarinaDesigner.Undo`. The pier's id, and the berth names built from it, stay as they are. Returns the renamed pier. |
| `IReadOnlyList<string> SelectBerthsInArea(Vector2 from, Vector2 to, bool add = false)` | Selects every berth whose middle lies inside a box in plan coordinates. This is what `DesignTool.SelectArea` does when the drag ends. |
| `IReadOnlyList<Berth> SetBerthServices(string berthId, bool wholeSide = false)` | Gives berths the pedestals in `MarinaDesigner.BerthServices`, and records one step for `MarinaDesigner.Undo`. This is what `DesignTool.EditServices` does when a berth is clicked. |
| `void SetReferenceImage(ReferenceImage image, float? metersPerPixel = null, Vector2? center = null)` | Shows an image to trace (north at the top). Without `metersPerPixel` it is sized to cover the current layout (at least 300 m wide) until calibrated; without `center` it is centered on the camera target. |
| `bool Undo()` | Reverts the last change the designer made: a drawn land area, pier, berth row or land berth is removed again, erased elements come back with their dividers, and trees are restored. Raises `MarinaDesigner.ActionUndone` (and the usual `LayoutChanged` notifications). Returns false when there is nothing to undo. |
| `void ViewTopDown(bool immediate = false)` | Looks straight down with north (−Z) at the top of the view, as aerial images are shown: the best angle to trace an image. Keeps the camera target and distance. |

<a id="referenceimage"></a>
### ReferenceImage

`sealed class ReferenceImage`

A picture (typically a top-down aerial or satellite screenshot of the real marina) shown under or over the scene while designing, so the layout can be traced on it. North is at the top of the image.

The core library has no image codecs, so the host supplies the pixels: either decoded RGBA (`ReferenceImage.ctor`, e.g. from a WinForms `Bitmap`) or the original PNG/JPEG bytes plus the pixel size (`ReferenceImage.FromEncoded`, which the browser decodes). Every renderer accepts RGBA; the WebGL renderer also accepts encoded data. Instances are immutable; create a new one to change the picture.

| Member | Description |
|---|---|
| `ReferenceImage(int pixelWidth, int pixelHeight, byte[] rgba, byte[]? encodedData = null, string? contentType = null)` | Creates an image from decoded pixels. |
| `int PixelWidth { get; }` | Width in pixels. |
| `int PixelHeight { get; }` | Height in pixels. |
| `byte[]? Rgba { get; }` | Decoded pixels (RGBA, top row first), or null for an encoded image. |
| `byte[]? EncodedData { get; }` | The original file bytes when they are known, whether or not `ReferenceImage.Rgba` is also set. |
| `string? ContentType { get; }` | MIME type of `ReferenceImage.EncodedData`, or null when there is none. |
| `bool CanBeSaved { get; }` | True when the original file bytes are present, so this image can be stored in a marina file. |
| `int Key { get; }` | Process-unique number identifying this image; renderers upload a texture once per key. |
| `static ReferenceImage FromEncoded(byte[] encodedData, int pixelWidth, int pixelHeight, string contentType = "image/png")` | Creates an image from PNG, JPEG or WebP file bytes whose pixel size the host already knows. Only renderers that can decode images themselves (the WebGL renderer) can draw it; desktop hosts should decode to RGBA instead. |

<a id="referenceimagechange"></a>
### ReferenceImageChange

`enum ReferenceImageChange`

What changed about the reference image (`MarinaDesigner.ReferenceImageChanged`).

| Value | Description |
|---|---|
| `Set` = 0 | A new image was set. |
| `Cleared` = 1 | The image was removed. |
| `Moved` = 2 | The image was moved (`MarinaDesigner.ReferenceImageCenter`). |
| `Scaled` = 3 | The image scale changed (`MarinaDesigner.ReferenceImageMetersPerPixel`), e.g. by calibration. |
| `AppearanceChanged` = 4 | Opacity, visibility or layering changed. |

<a id="referenceimagechangedeventargs"></a>
### ReferenceImageChangedEventArgs

`sealed class ReferenceImageChangedEventArgs : EventArgs`

Data for `MarinaDesigner.ReferenceImageChanged`.

| Member | Description |
|---|---|
| `ReferenceImageChangedEventArgs(ReferenceImageChange change, ReferenceImage? image, Vector2 center, float metersPerPixel)` | Creates the event data. |
| `ReferenceImageChange Change { get; }` | What changed. |
| `ReferenceImage? Image { get; }` | The current image, or null after `ReferenceImageChange.Cleared`. |
| `Vector2 Center { get; }` | Center of the image in plan coordinates. |
| `float MetersPerPixel { get; }` | Meters per image pixel. |

<a id="scalelinedrawneventargs"></a>
### ScaleLineDrawnEventArgs

`sealed class ScaleLineDrawnEventArgs : EventArgs`

Data for `MarinaDesigner.ScaleLineDrawn`: the user drew a line over the reference image's scale bar. Set `ScaleLineDrawnEventArgs.KnownLengthMeters` to calibrate immediately, or call `MarinaDesigner.CalibrateReferenceImage` later (e.g. after asking the user).

| Member | Description |
|---|---|
| `ScaleLineDrawnEventArgs(Vector2 start, Vector2 end, float measuredLength)` | Creates the event data. |
| `Vector2 Start { get; }` | First end, in plan coordinates. |
| `Vector2 End { get; }` | Second end, in plan coordinates. |
| `float MeasuredLength { get; }` | Length of the line in meters at the image's current scale. |
| `float? KnownLengthMeters { get; set; }` | The real length the scale bar represents, in meters. When set by a handler, the image is calibrated right away. |

## VirtualMarina.Core.Domain

<a id="berth"></a>
### Berth

`sealed record Berth`

A single berth: a water berth along a pier (`Berth.PierId`), or a land berth on a `LandArea` (`Berth.LandAreaId`) where a boat is stored or maintained ashore. Immutable: the visualizer stores snapshots and hands them out in events, so host code can never change marina state without going through the API.

The one deliberate exception is `Berth.ExternalData`, a mutable bag shared by every snapshot of the same berth, where host code can keep its own objects.

| Member | Description |
|---|---|
| `Berth(string id, string pierId, Vector2 center, float headingDegrees, float length, float width)` | Creates a Free berth at an explicit position, orientation and size. |
| `string Id { get; init; }` | ERP identifier (unique within the marina, case-insensitive). |
| `string? PierId { get; init; }` | Id of the pier this berth belongs to; null for a land berth. |
| `string? LandAreaId { get; init; }` | Id of the `LandArea` a land berth is on; null for a water berth along a pier. |
| `string? Label { get; init; }` | Human-readable label, e.g. "A-12". Falls back to `Berth.Id`. |
| `Vector2 Center { get; init; }` | Center of the berth's water area (or land spot) in plan coordinates (X = world X, Y = world Z). |
| `float HeadingDegrees { get; init; }` | Direction a moored boat's bow points (normally toward the pier). |
| `float Length { get; init; }` | Usable length in meters, along the heading. |
| `float Width { get; init; }` | Usable width in meters, across the heading. |
| `float? MaxDraft { get; init; }` | Maximum boat draft in meters, if known (shown in the default tooltip). |
| `BerthStatus Status { get; init; }` | Occupancy status; controls the pad, buoy and boat rendering. Default `BerthStatus.Free`. |
| `Boat? Boat { get; init; }` | The moored boat (Occupied), expected boat (Reserved) or away boat (TemporarilyFree). Always null when Free. For a berth in a `MultiBerth`, every member berth carries the berth's boat. |
| `bool HasFingerPiers { get; init; }` | Draw narrow finger piers along both long sides of the berth. Ignored for land berths. |
| `bool IsVisible { get; init; }` | When false the berth is not drawn at all (not even its finger piers) and cannot be interacted with. |
| `bool IsDisabled { get; init; }` | When true the berth is drawn in gray (its boat desaturated) and cannot be hovered, selected, right-clicked or acted on. |
| `bool IsReadOnly { get; init; }` | When true the berth looks normal and can be selected and show its tooltip, but its actions window does not open. |
| `PierServices? Services { get; init; }` | Power and water at this berth, overriding `Pier.Services`. Null (the default) takes whatever the pier offers, which is what most berths do; set it where one stretch of a pier was upgraded and the rest was not. |
| `string? MultiBerthId { get; }` | Id of the `MultiBerth` this berth belongs to, if any. Managed by the visualizer. |
| `IReadOnlyDictionary<string, string> Metadata { get; init; }` | Read-only string attributes supplied with the berth definition (e.g. power, water). For mutable host objects use `Berth.ExternalData`. |
| `MarinaDataBag ExternalData { get; init; }` | Host-owned objects attached to this berth (contract ids, cached ERP records, ...). The same instance is shared by every snapshot of the berth, so values written from an event handler are visible in later events and in `GetBerth`. The visualizer never reads it. |
| `string DisplayName { get; }` | `Berth.Label` when set, otherwise `Berth.Id`. Used for tooltips and water labels. |
| `bool IsInteractive { get; }` | Visible and not disabled: can be hovered, selected and show a tooltip. |
| `bool AllowsActions { get; }` | Interactive and not read-only: its actions window can open. |
| `bool IsOnLand { get; }` | True for a land berth (`Berth.LandAreaId` is set). |
| `bool IsInMultiBerth { get; }` | True when the berth is part of a `MultiBerth`. |
| `OrientedRect Bounds { get; }` | Spatial boundary of the berth's water area. |
| `Vector2 Forward { get; }` | Unit plan-view vector a moored boat's bow points along (toward the pier end of the berth). |
| `Vector2 Right { get; }` | Unit plan-view vector across the berth: the heading's local +X axis, `(cos h, −sin h)`. For heading 0° it is +X. |
| `static Berth OnLand(string id, string landAreaId, Vector2 position, float headingDegrees = 0f, float length = 12f, float width = 5f)` | Creates a Free land berth: a spot on a `LandArea` where a boat is stored or maintained ashore (boatyard, hard standing, dry stack). It is drawn on the land's surface, its boat rests on cradle stands, and it is selected, colored and updated like any other berth. |

<a id="berthgenerator"></a>
### BerthGenerator

`static class BerthGenerator`

Computes berth and divider geometry relative to a pier. Useful to ERP code that stores only berth numbers.

| Member | Description |
|---|---|
| `static IReadOnlyList<Berth> AlongPier(Pier pier, PierSide side, int count, float berthWidth, float berthLength, float startOffset = 2f, float gap = 0f, int firstNumber = 1)` | Generates berths perpendicular to a pier, bows pointing at the pier. Ids follow the pattern `{PierId}-L01` / `{PierId}-R01`. |
| `static Berth AtPier(Pier pier, string id, PierSide side, float offsetAlong, float berthWidth, float berthLength)` | A single berth perpendicular to the pier, bow toward it. |
| `static Divider DividerAtPier(Pier pier, string id, PierSide side, float offsetAlong, float berthLength, DividerType type)` | One divider standing at `offsetAlong` meters from the pier's start, running away from the pier on `side`: the boundary between two berths. Finger piers are 75% of the berth length, everything else runs the full length. |
| `static IReadOnlyList<Divider> DividersAlongPier(Pier pier, PierSide side, int count, float berthWidth, float berthLength, DividerType type, float startOffset = 2f, float gap = 0f)` | Dividers at the `count` + 1 boundaries of berths laid out like `BerthGenerator.AlongPier`. Finger piers are 75% of the berth length; piles and booms run the full length. |

<a id="berthstatus"></a>
### BerthStatus

`enum BerthStatus`

Occupancy state of a berth.

| Value | Description |
|---|---|
| `Free` = 0 | Available. Color-coded green by default. |
| `Occupied` = 1 | A boat is moored in the berth. Color-coded red by default. |
| `Reserved` = 2 | Held for a boat that is arriving shortly. Color-coded blue by default. |
| `TemporarilyFree` = 3 | The berth holder's boat is away (e.g. cruising) and the berth can be let out for a while. The boat stays assigned and is drawn as a ghost, like Reserved. Color-coded yellow by default. |

<a id="berthstatusextensions"></a>
### BerthStatusExtensions

`static class BerthStatusExtensions`

Helpers for `BerthStatus` and `BerthStatusFilter`.

| Member | Description |
|---|---|
| `static bool CanHaveBoat(BerthStatus status)` | True for statuses that can carry a boat (everything except Free). |
| `static BerthStatusFilter FromStatuses(IEnumerable<BerthStatus> statuses)` | A filter showing exactly the given statuses. |
| `static string GetDisplayName(BerthStatus status)` | Human-readable name, e.g. "Temporarily Free". Used by the default tooltip. |
| `static bool Includes(BerthStatusFilter filter, BerthStatus status)` | True when `filter` shows berths with `status`. |
| `static bool ShowsGhostBoat(BerthStatus status)` | True for statuses that show the assigned boat as a translucent ghost (Reserved, TemporarilyFree). |
| `static BerthStatusFilter ToFilter(BerthStatus status)` | The filter flag matching a status. |

<a id="berthstatusfilter"></a>
### BerthStatusFilter

`[Flags] enum BerthStatusFilter`

Set of statuses used to filter which berths are shown (`IMarinaVisualizer.SetStatusFilter`). Combine with `|`.

| Value | Description |
|---|---|
| `None` = 0 | No status: every berth's status visuals and boats are hidden. |
| `Free` = 1 | Free berths. |
| `Occupied` = 2 | Occupied berths. |
| `Reserved` = 4 | Reserved berths. |
| `TemporarilyFree` = 8 | Temporarily free berths. |
| `All` = 15 | Every status (default). |

<a id="boat"></a>
### Boat

`sealed record Boat`

A vessel assigned to (or expected at) a berth. Immutable: use `with` expressions to derive changes.

| Member | Description |
|---|---|
| `Boat(string id, string name, BoatType type)` | Creates a boat with the type's nominal length and beam (see `BoatTypeCatalog.GetNominalDimensions`). |
| `string Id { get; init; }` | ERP identifier of the vessel. |
| `string Name { get; init; }` | Boat name shown in tooltips. |
| `BoatType Type { get; init; }` | Category; selects the 3D model, which is scaled to `Boat.LengthMeters` × `Boat.BeamMeters`. |
| `float LengthMeters { get; init; }` | Length overall in meters. Defaults to the type's nominal length. |
| `float BeamMeters { get; init; }` | Beam in meters. Defaults to the type's nominal beam. |
| `string? OwnerName { get; init; }` | Owner shown in the default tooltip. |
| `string? RegistrationNumber { get; init; }` | Registration number shown in the default tooltip. |
| `DateTimeOffset? ExpectedArrival { get; init; }` | For reserved berths: when the boat is expected to arrive. For temporarily free berths: when it returns. |
| `IReadOnlyDictionary<string, string> Metadata { get; init; }` | Free-form ERP attributes carried through to events. |
| `string TypeDisplayName { get; }` | Human-readable type name, e.g. "Motor Yacht". |

<a id="boatdimensions"></a>
### BoatDimensions

`readonly record struct BoatDimensions`

Length overall and beam, in meters.

| Member | Description |
|---|---|
| `BoatDimensions(float Length, float Beam)` | Length overall and beam, in meters. |
| `float Length { get; init; }` | Length overall. |
| `float Beam { get; init; }` | Maximum width. |

<a id="boattype"></a>
### BoatType

`enum BoatType`

Boat categories the visualizer can render. Each has its own procedural model (see `BoatMeshFactory`).

| Value | Description |
|---|---|
| `MonohullSailboat` = 0 | Single-hull sailing yacht with mast and sails. Nominal 12 × 4 m. |
| `CatamaranSailboat` = 1 | Twin-hull sailing catamaran. Nominal 12 × 7 m. |
| `DayMotorBoat` = 2 | Small open or cuddy motor boat. Nominal 7 × 2.5 m. |
| `CatamaranMotorboat` = 3 | Twin-hull power catamaran. Nominal 13 × 6.5 m. |
| `MotorYacht` = 4 | Multi-deck motor yacht. Nominal 20 × 5.5 m. |
| `FishingBoat` = 5 | Fishing boat with wheelhouse. Nominal 10 × 3.5 m. |
| `JetSki` = 6 | Personal watercraft. Nominal 3.2 × 1.2 m. |
| `Ferry` = 7 | Small coastal passenger ferry with a boxy superstructure and a funnel. Nominal 45 × 11 m — too big for most berths, and mainly there to pass by offshore (see `MarineTraffic`). |

<a id="boattypecatalog"></a>
### BoatTypeCatalog

`static class BoatTypeCatalog`

Reference data for each `BoatType`.

| Member | Description |
|---|---|
| `static IReadOnlyList<BoatType> All { get; }` | Every boat type, in enum order. |
| `static string GetDisplayName(BoatType type)` | Human-readable name, e.g. "Motor Yacht". Used by the default tooltip. |
| `static BoatDimensions GetNominalDimensions(BoatType type)` | Dimensions the procedural (or imported) model is authored at. Instances are scaled from these to the boat's actual length and beam. Also the default size of a new `Boat`. |

<a id="divider"></a>
### Divider

`sealed record Divider`

A structure that separates berths: a finger pier, a row of piles, a floating boom or a single pile. It starts at `Divider.Start` (usually at the pier edge) and runs `Divider.Length` meters along `Divider.HeadingDegrees`.

Dividers are independent of `Berth.HasFingerPiers`, which draws simple finger piers automatically. Turn that off on berths whose separators you define explicitly.

| Member | Description |
|---|---|
| `Divider(string id, Vector2 start, float headingDegrees, float length, DividerType type = DividerType.FingerPier)` | Creates a divider from its start point, heading and length. |
| `string Id { get; init; }` | Unique id (case-insensitive). |
| `string? PierId { get; init; }` | Optional owning pier. Sets the deck height and material of finger piers, and the divider is removed together with the pier. |
| `Vector2 Start { get; init; }` | Start point in plan coordinates (usually at the pier edge). |
| `float HeadingDegrees { get; init; }` | Direction from `Divider.Start`, in degrees (0° = +Z, 90° = +X). |
| `float Length { get; init; }` | Length in meters. |
| `float Width { get; init; }` | Width of a finger pier or boom, or pile diameter. |
| `DividerType Type { get; init; }` | Finger pier, row of piles, floating boom or a single pile at the outer end. |
| `float Spacing { get; init; }` | Distance between piles (`DividerType.Piles`) or floats (`DividerType.Boom`). |
| `IReadOnlyDictionary<string, string> Metadata { get; init; }` | Read-only string attributes the host application attaches to this divider, e.g. its own key or a contract reference. Saved to and loaded from a marina file, and never read by the visualizer. |
| `Vector2 Direction { get; }` | Unit plan-view vector from start to end. |
| `Vector2 Right { get; }` | Unit plan-view vector across the divider: the heading's local +X axis, `(cos h, −sin h)`. |
| `Vector2 End { get; }` | End point (the pile of a finger pier or a `DividerType.SinglePile` divider stands here). |
| `Vector2 Center { get; }` | Middle point. |
| `OrientedRect Bounds { get; }` | Footprint (`Divider.Width` × `Divider.Length`). |
| `static Divider FromCenter(string id, Vector2 center, float length, float headingDegrees, DividerType type = DividerType.FingerPier)` | Creates a divider from its center point, length and orientation. |

<a id="dividertype"></a>
### DividerType

`enum DividerType`

How a divider between berths is built.

| Value | Description |
|---|---|
| `FingerPier` = 0 | Narrow walkable pier with a pile at its outer end. |
| `Piles` = 1 | A row of mooring piles. |
| `Boom` = 2 | A floating boom: a line of floats on the water. |
| `SinglePile` = 3 | A single mooring pile standing at the outer end of the boundary (Mediterranean mooring), with nothing in between. |

<a id="hinterlandscenery"></a>
### HinterlandScenery

`enum HinterlandScenery`

What is drawn on the mainland behind the shore, so the marina does not sit in an empty sea.

| Value | Description |
|---|---|
| `None` = 0 | Bare ground. The cheapest, and right when a reference photo is showing through. |
| `Countryside` = 1 | Scattered trees and hedges thinning out inland. |
| `Fields` = 2 | Blocks of crops in different colours, as seen from the air. |
| `Town` = 3 | Low blocks standing in rows, reading as a town along the coast. |

<a id="landarea"></a>
### LandArea

`sealed record LandArea`

A flat piece of land such as a quay, breakwater or lawn: a polygon outline in plan coordinates with one top height for the whole area. Land berths (`Berth.OnLand`) can be placed on it for boats stored or maintained ashore.

The outline may be convex or concave, in either winding order, but its edges must not cross. Don't repeat the first point at the end.

| Member | Description |
|---|---|
| `LandArea(string id, IEnumerable<Vector2> points, float height, LandKind kind = LandKind.Quay)` | Creates a land area from its outline. |
| `LandArea(string id, OrientedRect area, float height, LandKind kind = LandKind.Quay)` | Creates a rectangular land area. |
| `string Id { get; init; }` | Unique id (case-insensitive). Land berths reference it through `Berth.LandAreaId`. |
| `string? Name { get; init; }` | Display name (tooltips); the id is used when null. |
| `IReadOnlyList<Vector2> Points { get; init; }` | Outline in plan coordinates (X = world X, Y = world Z). The last point connects back to the first. |
| `float Height { get; init; }` | Height of the top surface above the water, in meters, over the whole area. |
| `LandKind Kind { get; init; }` | Surface type. |
| `IReadOnlyList<LandTree> Trees { get; init; }` | Trees standing on the area (usually lawns). Their positions are part of the layout, so they never move between sessions; generate them once with `LandArea.GenerateTrees` or the designer. |
| `IReadOnlyDictionary<string, string> Metadata { get; init; }` | Read-only string attributes the host application attaches to this land area, e.g. its own key or a contract reference. Saved to and loaded from a marina file, and never read by the visualizer. |
| `string DisplayName { get; }` | `LandArea.Name` when set, otherwise `LandArea.Id`. |
| `float Area { get; }` | Plan-view area in square meters. |
| `bool Contains(Vector2 point)` | True when the plan-view point lies inside the outline. |
| `static IReadOnlyList<LandTree> GenerateTrees(IReadOnlyList<Vector2> outline, float treesPer1000SquareMeters, Random random, IEnumerable<OrientedRect>? keepClear = null)` | Scatters trees randomly inside an outline, at about `treesPer1000SquareMeters`, keeping them apart, away from the edges and out of `keepClear` (e.g. land berths). Store the result in `LandArea.Trees`. |
| `ValueTuple<Vector2, Vector2> GetAxisAlignedBounds()` | The smallest axis-aligned rectangle containing the outline. |

<a id="landareabuilder"></a>
### LandAreaBuilder

`sealed class LandAreaBuilder`

Adds land berths to a single `LandAreaBuilder.LandArea` inside `MarinaLayoutBuilder`.

| Member | Description |
|---|---|
| `LandArea LandArea { get; }` | The land area being configured. |
| `LandAreaBuilder AddBerth(string id, Vector2 position, float headingDegrees = 0f, float length = 12f, float width = 5f, Func<Berth, Berth>? customize = null)` | Adds a land berth centered at `position` (see `Berth.OnLand`). |
| `LandAreaBuilder AddBerths(string idPrefix, Vector2 firstPosition, float rowHeadingDegrees, int count, float berthWidth, float berthLength, Func<int, Berth, Berth>? customize = null, float gap = 0.5f, float? boatHeadingDegrees = null)` | Adds a row of `count` land berths side by side, starting at `firstPosition` and continuing along `rowHeadingDegrees`. Ids follow `{idPrefix}01`, continuing after berths already added with the same prefix. |

<a id="landkind"></a>
### LandKind

`enum LandKind`

Surface of a `LandArea`; controls how it is drawn.

| Value | Description |
|---|---|
| `Quay` = 0 | Paved quay or pier head: a solid light concrete block. |
| `Breakwater` = 1 | Rubble-mound breakwater: the area is filled with a pile of rocks sloping down to the water. |
| `Grass` = 2 | Lawn or park: a solid green block. |

<a id="landtree"></a>
### LandTree

`readonly record struct LandTree`

A tree standing on a `LandArea`. Positions and sizes are stored, so trees look the same in every session.

| Member | Description |
|---|---|
| `LandTree(Vector2 Position, float Height, float CrownRadius, TreeShape Shape = TreeShape.Broadleaf)` | A tree standing on a `LandArea`. Positions and sizes are stored, so trees look the same in every session. |
| `Vector2 Position { get; init; }` | Trunk position in plan coordinates. |
| `float Height { get; init; }` | Total height above the land surface, 1–40 m. |
| `float CrownRadius { get; init; }` | Radius of the crown, 0.3–15 m. |
| `TreeShape Shape { get; init; }` | Crown shape. |

<a id="marinadatabag"></a>
### MarinaDataBag

`sealed class MarinaDataBag : IDictionary<string, object>, IReadOnlyDictionary<string, object>`

Mutable key/value store (string → object) for host application data attached to an element of the marina, e.g. contract ids, invoice objects, UI state or cached ERP records. Today it is `Berth.ExternalData`; the name is deliberately not berth-specific so other elements can carry one without a second type.

One bag exists per berth and is shared by every `Berth` snapshot of that berth, so a value saved from an event handler can be read back later from `GetBerth`, from other events, or after status and geometry updates. The visualizer never reads, renders or serializes these values — use `Berth.Metadata` for strings that must survive being saved to a marina file. Keys are case-sensitive. Like the rest of the visualizer API it is not thread-safe.

| Member | Description |
|---|---|
| `MarinaDataBag()` | Creates an empty bag. New berths get one automatically. |
| `MarinaDataBag(IEnumerable<KeyValuePair<string, object?>> items)` | Creates a bag pre-filled with `items` (later duplicates overwrite earlier ones). |
| `object? this[string key] { get; set; }` | Gets or sets a value. Getting a missing key throws; use `MarinaDataBag.Get` or `MarinaDataBag.TryGetValue` to avoid that. |
| `int Count { get; }` | Number of entries. |
| `ICollection<string> Keys { get; }` | All keys. |
| `ICollection<object?> Values { get; }` | All values. |
| `void Add(string key, object? value)` | Adds a value. |
| `void Clear()` | Removes every entry. |
| `bool ContainsKey(string key)` | True when the key is present. |
| `T? Get<T>(string key)` | Returns the value if present and of type `T`; otherwise default. |
| `IEnumerator<KeyValuePair<string, object?>> GetEnumerator()` | Enumerates the entries. |
| `T? GetOrAdd<T>(string key, Func<T?> factory)` | Returns the existing `T` value, or creates it with `factory` and stores it. |
| `bool Remove(string key)` | Removes a key. Returns false when it wasn't present. |
| `void Set(string key, object? value)` | Stores a value, replacing any existing one. |
| `bool TryGet<T>(string key, out T? value)` | Gets the value when it is present and of type `T`. |
| `bool TryGetValue(string key, out object? value)` | Gets a value of any type. |

<a id="marinalayout"></a>
### MarinaLayout

`sealed record MarinaLayout`

Complete description of a marina: piers, berths (flat list, linked by `Berth.PierId`), dividers, multi-berths, surrounding land and land berths (linked by `Berth.LandAreaId`). Pass to `MarinaVisualizer.InitializeLayout`.

| Member | Description |
|---|---|
| `MarinaLayout()` | Creates an instance with default values. |
| `string Name { get; init; }` | Marina name (`IMarinaVisualizer.MarinaName`). |
| `IReadOnlyList<Pier> Piers { get; init; }` | Piers. Ids must be unique. |
| `IReadOnlyList<Berth> Berths { get; init; }` | Berths, each referencing a pier in `MarinaLayout.Piers` or, for land berths, a land area in `MarinaLayout.LandAreas`. Ids must be unique. |
| `IReadOnlyList<Divider> Dividers { get; init; }` | Finger piers, pile rows and booms between berths. |
| `IReadOnlyList<MultiBerth> MultiBerths { get; init; }` | Boats spanning several berths. Member berths take the berth's status and boat when the layout is loaded. |
| `IReadOnlyList<LandArea> LandAreas { get; init; }` | Quays, breakwaters and lawns drawn around the water. Ids must be unique. |
| `Shoreline? Shoreline { get; init; }` | The mainland behind the marina, drawn beneath the `MarinaLayout.LandAreas`. Null for a marina standing in open water, which is how every layout written before this existed reads back. |
| `MarineTraffic? MarineTraffic { get; init; }` | Passing traffic out at sea, or null for empty water. It is decoration rather than layout: the vessels are not berths and cannot be clicked. |
| `static MarinaLayout Empty { get; }` | A layout with nothing in it. |
| `ValueTuple<Vector2, Vector2> ComputeBounds()` | Plan-view bounds of all piers, berths, dividers and land. Returns a default 100 m square when empty. |
| `static MarinaLayout FromObjects(IEnumerable<object> elements, string name = "Marina")` | Builds a layout from elements in any order (the inverse of `MarinaLayout.ToObjects`). The result is not validated. |
| `object[] ToObjects()` | Every element as one flat array, in dependency order: the shoreline if there is one, then land areas, piers, dividers, berths, then multi-berths. Each entry is the immutable record itself (`Shoreline`, `LandArea`, `Pier`, `Divider`, `Berth`, `MultiBerth`), so host code can pattern-match on it. |
| `IReadOnlyList<string> Validate()` | Returns a list of problems; empty when the layout is valid. |

<a id="marinalayoutbuilder"></a>
### MarinaLayoutBuilder

`sealed class MarinaLayoutBuilder`

Fluent helper for composing a `MarinaLayout` with berths auto-positioned along piers.

| Member | Description |
|---|---|
| `MarinaLayoutBuilder(string name = "Marina")` | Starts an empty layout. |
| `MarinaLayoutBuilder AddBerth(Berth berth)` | Adds a berth at an explicit position, size and orientation. |
| `MarinaLayoutBuilder AddDivider(Divider divider)` | Adds a divider at an explicit position, length and orientation. |
| `MarinaLayoutBuilder AddLandArea(LandArea landArea, Action<LandAreaBuilder>? configure = null)` | Adds a quay, breakwater or lawn and, optionally, land berths on it. |
| `MarinaLayoutBuilder AddLandArea(string id, IEnumerable<Vector2> points, float height, LandKind kind = LandKind.Quay, Action<LandAreaBuilder>? configure = null)` | Adds a land area from its outline and, optionally, land berths on it. |
| `MarinaLayoutBuilder AddMultiBerth(MultiBerth berth)` | Puts one boat across several berths added earlier. |
| `MarinaLayoutBuilder AddPier(Pier pier, Action<PierBuilder>? configure = null)` | Adds a pier and, optionally, lays out its berths and dividers. |
| `MarinaLayoutBuilder AddPier(string id, string name, Vector2 start, float headingDegrees, float length, Action<PierBuilder>? configure = null, float width = 2.5f, PierType type = PierType.FloatingWooden)` | Adds a pier from its shore-end point, heading and size, and optionally lays out its berths and dividers. |
| `MarinaLayout Build()` | Creates the layout. It is not validated here; `InitializeLayout` (or `MarinaLayout.Validate`) does that. |
| `MarinaLayoutBuilder WithMarineTraffic(MarineTraffic? traffic)` | Sets the passing traffic out at sea. Replaces any set earlier. |
| `MarinaLayoutBuilder WithShoreline(Shoreline? shoreline)` | Sets the mainland behind the marina, drawn beneath the land areas. Replaces any set earlier. |

<a id="marinalayoutexception"></a>
### MarinaLayoutException

`sealed class MarinaLayoutException : Exception`

Thrown when a layout, pier or berth definition is invalid.

| Member | Description |
|---|---|
| `MarinaLayoutException(IReadOnlyList<string> errors)` | Creates the exception from a list of validation problems. |
| `IReadOnlyList<string> Errors { get; }` | Every problem found (the message joins them). |

<a id="marinetraffic"></a>
### MarineTraffic

`sealed record MarineTraffic`

Passing traffic out at sea: vessels running along straight lanes across the map, well clear of the marina and the land, fading in at one end of their lane and out at the other.

It is decoration, not layout: the vessels are not berths, cannot be clicked, and are worked out from `MarineTraffic.Seed` rather than stored, so turning it up costs nothing in the file. Where the lanes go. A lane is a straight line across the map. It is only kept when every part of it stays `MarineTraffic.Clearance` meters away from the marina, from every land area, and from the mainland behind the shore, so nothing ever appears to sail over a quay or through the piers. Raising `MarineTraffic.Clearance` pushes the traffic further out; raising it past what the map allows simply leaves fewer lanes.

| Member | Description |
|---|---|
| `MarineTraffic()` | Creates an instance with default values. |
| `const int MaximumVessels = 24` | The most vessels drawn at once, at `MarineTraffic.Intensity` 1. |
| `static MarineTraffic None { get; }` | Empty sea. This is what a marina has until traffic is switched on. |
| `static IReadOnlyList<BoatType> DefaultVessels { get; }` | The mix used when `MarineTraffic.Vessels` is left empty: what is plausibly passing a marina offshore. |
| `bool IsEnabled { get; init; }` | Draw the traffic. Default false, so a marina is in empty sea until it is asked for. |
| `float Intensity { get; init; }` | How busy the sea is, 0–1 (default 0.5). Scales the number of vessels up to `MarineTraffic.MaximumVessels`. |
| `float Clearance { get; init; }` | How far a lane must stay from the marina and from any land, in meters (default 300). Nothing is drawn closer than this, so the traffic never crosses a quay, a breakwater or the piers. |
| `float SpeedKnots { get; init; }` | How fast the vessels go, in knots (default 8). They are meant to drift slowly across the view. |
| `float Reach { get; init; }` | Half the length of a lane, in meters (default 600): how far out the traffic runs before it fades away. This is the edge of the map as far as the traffic is concerned. |
| `int Seed { get; init; }` | Keeps the lanes and the vessels on them the same between sessions. Any number will do. |
| `IReadOnlyList<BoatType> Vessels { get; init; }` | The kinds of vessel out there, drawn from at random. Repeat a type to make it more common. Empty means `MarineTraffic.DefaultVessels`. |
| `IReadOnlyDictionary<string, string> Metadata { get; init; }` | Read-only string attributes the host application attaches to the traffic. Saved with the design. |
| `int VesselCount { get; }` | How many vessels this asks for; 0 when it is switched off. |
| `float SpeedMetersPerSecond { get; }` | `MarineTraffic.SpeedKnots` in meters per second. |
| `IReadOnlyList<BoatType> EffectiveVessels { get; }` | The mix actually used: `MarineTraffic.Vessels`, or `MarineTraffic.DefaultVessels` when that is empty. |
| `IEnumerable<string> Validate()` | Problems that would stop the traffic being drawn, empty when it is sound. |

<a id="mooringstyle"></a>
### MooringStyle

`enum MooringStyle`

How a boat that spans several berths lies in them.

| Value | Description |
|---|---|
| `Alongside` = 0 | Side-to: the boat lies parallel to the pier, across the berths, close to the pier end. Typical for a large yacht taking several small berths. |
| `BowIn` = 1 | Bow toward the pier, centered across the berths (e.g. a wide catamaran in two berths). |

<a id="multiberth"></a>
### MultiBerth

`sealed record MultiBerth`

One boat occupying (or reserved for, or temporarily away from) several berths at once.

The visualizer keeps member berths consistent: each carries the berth's `MultiBerth.Status` and `MultiBerth.Boat`, and its `Berth.MultiBerthId` is set. Changing the status or boat of any member berth through the single-berth API changes the whole berth; setting a member Free releases the berth. The boat is drawn once, across the combined area of the berths, using the first berth's orientation as the reference. Finger piers between member berths are not drawn.

| Member | Description |
|---|---|
| `MultiBerth(string id, IReadOnlyList<string> berthIds, Boat boat, BerthStatus status = BerthStatus.Occupied, MooringStyle style = MooringStyle.Alongside)` | Describes a berth. To create one at runtime use `IMarinaVisualizer.MoorAlongside` or `AssignBoatToBerths`; to load one use `MarinaLayout.MultiBerths`. |
| `string Id { get; init; }` | Unique berth id (case-insensitive); member berths carry it in `Berth.MultiBerthId`. |
| `IReadOnlyList<string> BerthIds { get; init; }` | Member berths. The first one is the primary berth: its orientation places the boat and boat clicks resolve to it. |
| `Boat Boat { get; init; }` | The boat; every member berth carries it in `Berth.Boat`. |
| `BerthStatus Status { get; init; }` | Occupied, Reserved or TemporarilyFree. A berth is never Free (release it instead). |
| `MooringStyle Style { get; init; }` | How the boat lies across the berths. |
| `IReadOnlyDictionary<string, string> Metadata { get; init; }` | Read-only string attributes the host application attaches to this multi-berth, e.g. its own key or a contract reference. Saved to and loaded from a marina file, and never read by the visualizer. |
| `string PrimaryBerthId { get; }` | The first member berth; its orientation places the boat. |
| `bool Contains(string berthId)` | True when `berthId` is a member (case-insensitive). |

<a id="orientedrect"></a>
### OrientedRect

`readonly record struct OrientedRect`

Plan-view rectangle rotated about its center. Used as the spatial boundary of berths, piers and land.

| Member | Description |
|---|---|
| `OrientedRect(Vector2 Center, Vector2 Size, float HeadingDegrees)` | Plan-view rectangle rotated about its center. Used as the spatial boundary of berths, piers and land. |
| `Vector2 Center { get; init; }` | Center in plan coordinates (X = world X, Y = world Z). |
| `Vector2 Size { get; init; }` | X = width across the heading, Y = length along the heading. |
| `float HeadingDegrees { get; init; }` | Direction of the rectangle's length axis. |
| `Vector2 Forward { get; }` | Unit vector along the length axis. |
| `Vector2 Right { get; }` | Unit vector along the width axis (the heading's local +X). |
| `float Width { get; }` | Extent across the heading (`Size.X`). |
| `float Length { get; }` | Extent along the heading (`Size.Y`). |
| `bool Contains(Vector2 point)` | True when the plan-view point lies inside or on the rectangle. |
| `ValueTuple<Vector2, Vector2> GetAxisAlignedBounds()` | The smallest axis-aligned rectangle containing this one. |
| `Vector2[] GetCorners()` | Corners in counter-clockwise order (viewed from above). |

<a id="pier"></a>
### Pier

`sealed record Pier`

A straight pier. It starts at `Pier.Start` (usually the shore end) and runs `Pier.Length` meters along `Pier.HeadingDegrees`.

Use the constructor when you know the shore end, or `Pier.FromCenter` when you store the pier's center point, size and orientation. Berths and dividers keep absolute positions, so moving a pier does not move them.

| Member | Description |
|---|---|
| `Pier(string id, string name, Vector2 start, float headingDegrees, float length, float width = 2.5f, PierType type = PierType.FloatingWooden)` | Creates a pier from its shore-end point, heading, length and width. |
| `string Id { get; init; }` | Unique id (case-insensitive). Berths and dividers reference it. |
| `string Name { get; init; }` | Display name, used in tooltips and the pier camera preset. |
| `Vector2 Start { get; init; }` | Shore-end center point in plan coordinates. |
| `float HeadingDegrees { get; init; }` | Direction from `Pier.Start` along the pier, in degrees (0° = +Z, 90° = +X). |
| `float Length { get; init; }` | Length along the heading, in meters. |
| `float Width { get; init; }` | Deck width across the heading, in meters. |
| `PierType Type { get; init; }` | Construction type; controls rendering and the default deck height. |
| `float DeckHeight { get; init; }` | Height of the deck surface above the water, in meters. Defaults depend on `Pier.Type`. |
| `float PilingSpacing { get; init; }` | Distance between columns (`PierType.Concrete`) or cleats (`PierType.FloatingConcrete`) along the pier. |
| `PierSides BerthingSides { get; init; }` | Sides where boats berth. Default `PierSides.Both`. A single-sided pier (e.g. one running along the edge of a `LandArea`) only takes berths on its open side, and its mooring points (bollards, cleats and fenders) are drawn on that side only. |
| `PierServices Services { get; init; }` | Power and water pedestals drawn along the pier. Default `PierServices.None`. They are placed on the berthing sides only, and only next to the berths that exist, so a pier without berths shows none. |
| `IReadOnlyDictionary<string, string> Metadata { get; init; }` | Read-only string attributes the host application attaches to this pier, e.g. its own key or a contract reference. Saved to and loaded from a marina file, and never read by the visualizer. |
| `Vector2 Direction { get; }` | Unit plan-view vector along the pier (from start to end). |
| `Vector2 Right { get; }` | Unit plan-view vector across the pier toward `PierSide.Right`: the right-hand side of someone standing at `Pier.Start` and looking toward `Pier.End`, `(−cos h, sin h)`. For a pier with heading 0° (running along +Z) it points to −X; for heading 180° (running north, toward −Z) it points to +X (east). |
| `Vector2 End { get; }` | Seaward-end center point. |
| `Vector2 Center { get; }` | Center of the deck. |
| `OrientedRect Bounds { get; }` | Deck footprint. |
| `bool IsFloating { get; }` | True for `PierType.FloatingWooden` and `PierType.FloatingConcrete`. |
| `static Pier FromCenter(string id, string name, Vector2 center, float length, float width, float headingDegrees, PierType type = PierType.FloatingWooden)` | Creates a pier from its center point, size and orientation. |
| `static float GetDefaultDeckHeight(PierType type)` | Deck height used when `Pier.DeckHeight` isn't set: 0.5 m wooden, 0.55 m floating concrete, 1.1 m fixed concrete. |
| `static string GetDisplayName(PierType type)` | Human-readable name of a pier type, e.g. "Floating (wooden)". |
| `bool HasBerthsOn(PierSide side)` | True when boats can berth on `side` (see `Pier.BerthingSides`). |
| `Pier WithCenter(Vector2 center)` | Returns a copy moved so its center is at `center`. |

<a id="pierbuilder"></a>
### PierBuilder

`sealed class PierBuilder`

Adds berths and dividers to a single pier inside `MarinaLayoutBuilder`.

| Member | Description |
|---|---|
| `Pier Pier { get; }` | The pier being configured. |
| `PierBuilder AddBerth(string id, PierSide side, float offsetAlong, float berthWidth, float berthLength, Func<Berth, Berth>? customize = null)` | Adds a single berth on one side of the pier at an explicit distance from the pier's start. |
| `PierBuilder AddBerth(Berth berth)` | Adds a berth defined by absolute position (its `Berth.PierId` is set to this pier). |
| `PierBuilder AddBerths(PierSide side, int count, float berthWidth, float berthLength, Func<int, Berth, Berth>? customize = null, float startOffset = 2f, float gap = 0f, DividerType? dividers = null)` | Appends `count` berths on one side, continuing after any berths previously added on that side. Ids follow `{PierId}-L01` / `{PierId}-R01`; bows point at the pier. |
| `PierBuilder AddDivider(Divider divider)` | Adds a divider defined by absolute position (its `Divider.PierId` is set to this pier). |

<a id="pierservices"></a>
### PierServices

`[Flags] enum PierServices`

Supplies offered at a pier's berths, drawn as pedestals beside the berths (`Pier.Services`).

| Value | Description |
|---|---|
| `None` = 0 | No pedestals (default). |
| `Power` = 1 | Electricity: a pedestal with a yellow top. |
| `Water` = 2 | Fresh water: a pedestal with a blue top. |
| `PowerAndWater` = 3 | One pedestal supplying both, with a yellow top and a blue band. |

<a id="pierside"></a>
### PierSide

`enum PierSide`

Which side of a pier berths and dividers are generated on, as seen from the pier's `Pier.Start` (usually the shore) looking toward its `Pier.End` (the sea).

| Value | Description |
|---|---|
| `Left` = 0 | The left-hand side looking from start to end (+X for a pier with heading 0°). Generated ids use "L": `{PierId}-L01`. |
| `Right` = 1 | The right-hand side looking from start to end, where `Pier.Right` points (−X for a pier with heading 0°). Generated ids use "R": `{PierId}-R01`. |

<a id="piersides"></a>
### PierSides

`[Flags] enum PierSides`

The sides of a pier where boats berth (`Pier.BerthingSides`).

| Value | Description |
|---|---|
| `Left` = 1 | Only the `PierSide.Left` side (left looking from start to end). |
| `Right` = 2 | Only the `PierSide.Right` side (right looking from start to end). |
| `Both` = 3 | Both sides (default). |

<a id="piertype"></a>
### PierType

`enum PierType`

Construction of a pier. Controls how it is drawn and its default deck height.

| Value | Description |
|---|---|
| `FloatingWooden` = 0 | Wooden deck on pontoon floats. Default deck height 0.5 m. |
| `FloatingConcrete` = 1 | Monolithic concrete pontoon with rubber fenders and cleats. Default deck height 0.55 m. |
| `Concrete` = 2 | Fixed concrete pier on columns, with curbs and bollards. Default deck height 1.1 m. |

<a id="shoreline"></a>
### Shoreline

`sealed record Shoreline`

The mainland behind the marina: an open line of points whose first and last segments run on for ever, splitting the plan in two, with one side of it being land. It is what stops a marina looking like an island in an empty sea.

Two points are enough — that is simply a straight coast. More points bend it into bays and headlands. The line is open, not a ring: the segment before the first point and the segment after the last one carry on outward without end, so the land behind them never runs out however far the camera pulls back. Which side. `Shoreline.LandOnLeft` picks the half: true for the left of the line walking from the first point to the last. A designer chooses it by clicking the side that should be land. The one rule. The two endless segments must not cross each other, or "the land side" means nothing — `Shoreline.Validate` reports that. Everything else, including a coast that doubles back on itself, is allowed. It is drawn beneath the land areas placed by hand, so a quay traced along the shore sits on top of it and the two read as one piece of ground.

| Member | Description |
|---|---|
| `Shoreline(IEnumerable<Vector2> points, bool landOnLeft)` | Creates a shoreline from the points of its open line. |
| `const float Reach = 12000f` | How far out the endless segments are taken when the shape is built, in meters — at the least. |
| `IReadOnlyList<Vector2> Points { get; init; }` | The points of the open line, in order. |
| `bool LandOnLeft { get; init; }` | True when the land is to the left of the line walked from the first point to the last. |
| `float Height { get; init; }` | Height of the ground above the water, in meters. Default 1.4. |
| `LandKind Kind { get; init; }` | Surface of the ground. Default `LandKind.Grass`, the usual thing behind a marina. |
| `HinterlandScenery Scenery { get; init; }` | What is scattered across it. Default `HinterlandScenery.Countryside`. |
| `int ScenerySeed { get; init; }` | Keeps the scenery the same between sessions. Any number will do. |
| `IReadOnlyDictionary<string, string> Metadata { get; init; }` | Read-only string attributes the host application attaches to the shoreline. Saved with the design. |
| `IReadOnlyList<Vector2> BuildOutline()` | The shoreline as a closed shape covering the land side, ready to be triangulated: the line itself, its two segments run out to `Shoreline.Reach`, and the way round the far edge that keeps the land inside. |
| `bool Contains(Vector2 point)` | True when a point in plan coordinates lies on the land side of the shoreline. |
| `float DistanceToShore(Vector2 point)` | Distance from a point to the line itself (not to the far edge of the built shape), in meters. |
| `IEnumerable<string> Validate()` | Problems that would stop the shoreline being drawn, empty when it is sound. |

<a id="trafficvessel"></a>
### TrafficVessel

`readonly record struct TrafficVessel`

One vessel of the passing traffic, where it is at a moment in time.

| Member | Description |
|---|---|
| `TrafficVessel(BoatType Type, Vector2 Position, float HeadingDegrees, float Opacity)` | One vessel of the passing traffic, where it is at a moment in time. |
| `BoatType Type { get; init; }` | What kind of vessel it is. |
| `Vector2 Position { get; init; }` | Where it is, in plan coordinates. |
| `float HeadingDegrees { get; init; }` | Which way its bow points, in the usual compass sense. |
| `float Opacity { get; init; }` | 0–1. Vessels fade in at the start of their lane and out at the end. |

<a id="treeshape"></a>
### TreeShape

`enum TreeShape`

Shape of a `LandTree`.

| Value | Description |
|---|---|
| `Broadleaf` = 0 | Round crown (plane tree, olive, oak). |
| `Conifer` = 1 | Tall, pointed crown (pine, cypress). |
| `Palm` = 2 | Bare trunk with a spray of fronds on top (palm). Common along a promenade. |
| `Cypress` = 3 | Narrow column (Italian cypress), the exclamation mark of a Mediterranean shore. |
| `Cherry` = 4 | Pink blossom (Japanese cherry). Scattered far more rarely than the rest, so finding one is a small surprise. |

## VirtualMarina.Core.Geometry

<a id="boatmeshfactory"></a>
### BoatMeshFactory

`static class BoatMeshFactory`

Low-poly procedural stand-ins for each `BoatType`. They are placeholders until real GLTF models are wired in; replace a mesh by registering a different `MeshData` under the same id.

Model space: bow points +Z, the waterline is Y = 0, the hull is centered on X/Z, and dimensions match `BoatTypeCatalog.GetNominalDimensions`.

| Member | Description |
|---|---|
| `static MeshData Create(BoatType type, int meshId)` | Builds the procedural model of a boat type. |

<a id="boundingbox"></a>
### BoundingBox

`readonly record struct BoundingBox`

Axis-aligned box.

| Member | Description |
|---|---|
| `BoundingBox(Vector3 Min, Vector3 Max)` | Axis-aligned box. |
| `Vector3 Min { get; init; }` | Minimum corner. |
| `Vector3 Max { get; init; }` | Maximum corner. |
| `Vector3 Size { get; }` | Extent along each axis. |
| `Vector3 Center { get; }` | Middle point. |
| `static BoundingBox FromVertices(float[] vertices, int stride)` | Bounds of the positions in an interleaved vertex buffer (zero box when empty). |
| `bool IntersectRay(Vector3 origin, Vector3 direction, out float distance)` | Slab test. Returns the entry distance along the (not necessarily normalized) direction. |

<a id="glyphfont"></a>
### GlyphFont

`static class GlyphFont`

Minimal stroke font for text laid flat on the water (berth labels). Each character is a mesh of flat, upward-facing strokes, so it renders on every backend without textures.

Glyph model space: the glyph is 1 unit tall along local +Z (the text's "up") and centered on the origin. Reading direction runs along local −X, which is correct when the mesh is placed with `MarinaMath.CreatePlacement(scale, headingOfTextUp, position)` and seen from above. Lowercase letters render as uppercase; unsupported characters render as '?'.

| Member | Description |
|---|---|
| `const float GlyphWidth = 0.6666667f` | Glyph width of `LabelFont.Regular` as a fraction of its height. |
| `const float Advance = 0.8666666f` | Distance between consecutive `LabelFont.Regular` glyph centers, as a fraction of the height. |
| `static IReadOnlyList<char> SupportedCharacters { get; }` | Characters that have a glyph (besides space). |
| `static float AdvanceOf(LabelFont font)` | Distance between consecutive glyph centers in a face, as a fraction of the height. |
| `static IEnumerable<MeshData> CreateAll()` | Every supported character in every face, with ids from `MeshIds.GlyphBase`. Registered by `MeshLibrary.CreateDefault`. |
| `static float GlyphWidthOf(LabelFont font)` | Glyph width in a face, as a fraction of the height. |
| `static float MeasureWidth(int characterCount)` | Width of a line of `LabelFont.Regular` text in units of the glyph height. |
| `static float MeasureWidth(int characterCount, LabelFont font)` | Width of a line of text in units of the glyph height. |
| `static bool TryGetMeshId(char c, out int meshId)` | Mesh id for a character in `LabelFont.Regular`; false for whitespace (nothing to draw). |
| `static bool TryGetMeshId(char c, LabelFont font, out int meshId)` | Mesh id for a character in one face; false for whitespace (nothing to draw). |

<a id="labelfont"></a>
### LabelFont

`enum LabelFont`

The faces berth labels can be set in. They are stroke fonts baked into meshes, not typefaces from the system, so the choice is between a few built-in weights and widths rather than a font file.

| Value | Description |
|---|---|
| `Regular` = 0 | The default: even strokes, normal width. |
| `Bold` = 1 | Heavier strokes, for labels that must read from further away. |
| `Condensed` = 2 | Narrower glyphs, so longer names fit across a berth. |
| `Wide` = 3 | Wider glyphs, easier to read on big berths. |

<a id="landmeshfactory"></a>
### LandMeshFactory

`static class LandMeshFactory`

World-space meshes for the ground: solid slabs for quays and lawns, rock piles for breakwaters, and the mainland behind a `Shoreline`.

| Member | Description |
|---|---|
| `const float WallDepth = 3f` | How far land walls reach below the water surface, in meters. |
| `static MeshData Create(int id, LandArea area, LandStyle? style = null)` | The mesh for a land area: a rock pile for `LandKind.Breakwater`, otherwise a solid slab; plus its trees. |
| `static MeshData CreateRockPile(int id, LandArea area, LandStyle? style = null)` | A rubble mound: the area filled with irregular rocks (low-poly squashed spheres), reaching the land height in the middle and sloping down to the water along the outline, over a dark core that hides the gaps between rocks. |
| `static MeshData CreateShoreline(int id, Shoreline shoreline, LandStyle? style = null)` | The mainland behind the marina: the shoreline's shape as one slab, with whatever scenery it asks for scattered in a band along the coast. |
| `static MeshData CreateSlab(int id, LandArea area, LandStyle? style = null)` | The outline extruded from `LandMeshFactory.WallDepth` below the water up to the land height, with a flat top. |

<a id="marinameshfactory"></a>
### MarinaMeshFactory

`static class MarinaMeshFactory`

Procedural meshes for marina infrastructure, markers and the water surface.

| Member | Description |
|---|---|
| `static MeshData CreateBerthPad(int id)` | Flat 1×1 m quad on Y = 0, facing up, with white vertices (tinted by berth status). |
| `static MeshData CreateBuoy(int id)` | Low-poly sphere, radius 0.5 m, white (status buoy). |
| `static MeshData CreateCylinder(int id)` | 12-sided cylinder, 1 m diameter, from Y = 0 to Y = 1, white (tinted per instance: steel piles, bollards). |
| `static MeshData CreatePiling(int id)` | Octagonal piling, 1 m diameter, from Y = 0 to Y = 1, with a lighter top. |
| `static MeshData CreateSelectionMarker(int id)` | Inverted pyramid "you are here" marker, tip at Y = 0, about 1.6 m tall. |
| `static MeshData CreateUnitBox(int id)` | 1 m cube centered at the origin with white vertices, tinted per instance. |
| `static MeshData CreateWaterGrid(int id, float size, int resolution, Vector2 center)` | Square, finely tessellated grid on Y = 0 centered at `center`. Vertex positions are displaced in the water vertex shader to animate waves. |

<a id="marinetrafficplanner"></a>
### MarineTrafficPlanner

`static class MarineTrafficPlanner`

Lays out the lanes the passing traffic runs along, and says where each vessel is at a given moment.

A lane is a straight line across the map. Lanes are tried at random from the traffic's seed and kept only when the whole line stays `MarineTraffic.Clearance` away from the marina, from every land area, and from the mainland behind the shore — so a vessel can never appear to sail over a quay or through the piers. Planning is done once, when the traffic or the layout changes; `MarineTrafficPlanner.Place` is then called on every frame and only walks the vessels along lanes that are already known to be clear.

| Member | Description |
|---|---|
| `static IEnumerable<TrafficVessel> Place(IReadOnlyList<TrafficLane> lanes, MarineTraffic traffic, double seconds)` | Where every vessel is at a moment in time. Cheap enough to call on every frame: it is a walk along lines that were already checked when they were planned. |
| `static IReadOnlyList<TrafficLane> Plan(MarineTraffic traffic, ValueTuple<Vector2, Vector2> marina, IEnumerable<LandArea> land, Shoreline? shoreline)` | Works out the lanes and the vessels on them. The result is fixed for a given traffic setting and layout, so it is planned once and then only walked forward in time. |

<a id="meshbuilder"></a>
### MeshBuilder

`sealed class MeshBuilder`

Builds flat-shaded low-poly meshes from convex primitives (boxes, prisms, cylinders, cones, spheres).

Every face gets its own vertices so normals stay faceted. Triangle winding is computed so that front faces point away from each primitive's interior; renderers can therefore enable or disable back-face culling freely.

| Member | Description |
|---|---|
| `MeshBuilder()` | Creates an instance with default values. |
| `int VertexCount { get; }` | Vertices added so far. |
| `void AddBox(Vector3 center, Vector3 size, Vector3 color)` | Axis-aligned box. |
| `void AddCylinder(Vector3 from, Vector3 to, float radiusFrom, float radiusTo, int sides, Vector3 color, bool caps = true)` | Cylinder or cone frustum between two arbitrary points. |
| `void AddLoft(IReadOnlyList<Vector3> loopA, IReadOnlyList<Vector3> loopB, Vector3 sideColor, Vector3? capAColor, Vector3? capBColor, Vector3? interior = null)` | Connects two closed loops with the same vertex count (a convex "loft") and optionally caps each end. Covers boxes, prisms, frustums, hulls, cones and cylinders. |
| `void AddPrismX(float x0, float x1, IReadOnlyList<ValueTuple<float, float>> profile, Vector3 color)` | Extrudes a (y, z) profile polygon across X from `x0` to `x1`. |
| `void AddQuadUp(Vector3 a, Vector3 b, Vector3 c, Vector3 d, Vector3 color)` | Flat horizontal quad facing +Y (for markers laid on the water). |
| `void AddSphere(Vector3 center, float radius, int segments, int rings, Vector3 color)` | Low-poly UV sphere. |
| `void AddTriangle(Vector3 a, Vector3 b, Vector3 c, Vector3 color)` | Adds a flat-shaded triangle (normal from the winding a → b → c). Degenerate triangles are skipped. |
| `void AddTriangleFacingAway(Vector3 a, Vector3 b, Vector3 c, Vector3 color, Vector3 interior)` | Adds a triangle wound so its normal points away from `interior`. |
| `void AddTriangularPlate(Vector3 a, Vector3 b, Vector3 c, float thickness, Vector3 color)` | A thin triangular panel (e.g. a sail) with the given thickness. |
| `MeshData Build(int id, string name, bool isWater = false)` | Creates the mesh from everything added so far. |

<a id="meshdata"></a>
### MeshData

`sealed class MeshData`

CPU-side triangle mesh in a backend-neutral format. Each renderer uploads it to its own GPU API.

Interleaved vertex layout (`MeshData.VertexStride` floats per vertex): position (xyz), normal (xyz), color (rgb). Indices are 32-bit triangle lists.

| Member | Description |
|---|---|
| `MeshData(int id, string name, float[] vertices, uint[] indices, bool isWater = false)` | Creates a mesh. Register it with `MeshLibrary.Register` to use or replace a model. |
| `const int VertexStride = 9` | Floats per vertex: position (3), normal (3), color (3). |
| `const int PositionOffset = 0` | Offset of the position within a vertex. |
| `const int NormalOffset = 3` | Offset of the normal within a vertex. |
| `const int ColorOffset = 6` | Offset of the RGB color within a vertex. |
| `int Id { get; }` | Mesh id referenced by `RenderObject.MeshId`. |
| `string Name { get; }` | Name for diagnostics. |
| `float[] Vertices { get; }` | Interleaved vertex data (position, normal, color). |
| `uint[] Indices { get; }` | Triangle list indices into `MeshData.Vertices`. |
| `bool IsWater { get; }` | The water grid is drawn by the water pass instead of the object pass. |
| `int VertexCount { get; }` | Number of vertices. |
| `int TriangleCount { get; }` | Number of triangles. |
| `BoundingBox Bounds { get; }` | Local-space bounds, used for picking and placement. |

<a id="meshids"></a>
### MeshIds

`static class MeshIds`

Well-known mesh ids referenced by render objects.

| Member | Description |
|---|---|
| `const int Water = 1` | The water grid (drawn by the water pass). |
| `const int UnitBox = 2` | 1 m white cube centered at the origin (decks, fingers, land). |
| `const int Piling = 3` | Wooden octagonal piling, 1 m diameter, Y 0–1. |
| `const int BerthPad = 4` | 1 × 1 m flat quad (status pads). |
| `const int SelectionMarker = 5` | Spinning gold selection marker. |
| `const int Buoy = 6` | White sphere, 0.5 m radius (status buoys, boom floats). |
| `const int Cylinder = 7` | White cylinder, 1 m diameter, Y 0–1 (steel piles, bollards). |
| `const int GlyphBase = 300` | First id of the text glyph meshes (see `GlyphFont`). |
| `const int Shoreline = 9000` | The mainland behind the shore (see `Shoreline`), drawn beneath the land areas. |
| `const int LandBase = 10000` | First id of the per-land-area meshes (see `MeshIds.ForLand`). |
| `static int ForBoat(BoatType type)` | Mesh id of a boat model (100 + type). Register a `MeshData` under this id to replace the model. |
| `static int ForLand(int slot)` | Mesh id of a land area's mesh slot (world-space geometry built by `LandMeshFactory`). Loading a layout assigns slots 0, 1, ... in layout order; land areas added later get the next free slot. |

<a id="meshlibrary"></a>
### MeshLibrary

`sealed class MeshLibrary`

The set of meshes a scene can reference. Renderers upload each mesh once, keyed by id, and pick up meshes added later on the next frame.

| Member | Description |
|---|---|
| `MeshLibrary()` | Creates an instance with default values. |
| `int Version { get; }` | Incremented on every `MeshLibrary.Register`; renderers re-upload meshes when it changes. |
| `IReadOnlyCollection<MeshData> All { get; }` | Every registered mesh. |
| `static MeshLibrary CreateDefault(float waterSize, int waterResolution, Vector2 waterCenter)` | A library with the water grid, primitives, text glyphs and one procedural model per `BoatType`. |
| `MeshData Get(int id)` | The mesh with this id. |
| `void Register(MeshData mesh)` | Adds or replaces a mesh (e.g. swap a procedural boat for one loaded from GLTF). |
| `bool TryGet(int id, out MeshData mesh)` | Looks up a mesh by id. |
| `bool Unregister(int id)` | Removes a mesh. Returns false when no mesh has this id. |

<a id="trafficlane"></a>
### TrafficLane

`sealed class TrafficLane`

One straight lane of passing traffic, and the vessels running along it.

| Member | Description |
|---|---|
| `TrafficLane(Vector2 start, Vector2 end)` | Creates a lane between two points in plan coordinates. |
| `Vector2 Start { get; }` | Where the lane begins, in plan coordinates. |
| `Vector2 End { get; }` | Where the lane ends. |
| `Vector2 Direction { get; }` | Unit direction from `TrafficLane.Start` to `TrafficLane.End`. |
| `float Length { get; }` | How long the lane is, in meters. |
| `int VesselCount { get; }` | How many vessels run along this lane. |
| `float DistanceTo(Vector2 point)` | Signed distance from a point to the infinite line the lane lies on, in meters. |

## VirtualMarina.Core.Input

<a id="cameradragaction"></a>
### CameraDragAction

`enum CameraDragAction`

What dragging with a mouse button does. Configure with `MarinaInputController.LeftDragAction` and related properties.

| Value | Description |
|---|---|
| `None` = 0 | Dragging does nothing. |
| `Pan` = 1 | Map-style pan: the ground under the pointer follows it. |
| `Orbit` = 2 | Rotate the camera around its target. |

<a id="inputmodifiers"></a>
### InputModifiers

`[Flags] enum InputModifiers`

Keyboard modifiers held during a pointer or key event.

| Value | Description |
|---|---|
| `None` = 0 | No modifier. |
| `Shift` = 1 | Shift: click adds or removes berths from the selection (like Control); swaps pan and orbit while dragging; orbits with the arrow keys. |
| `Control` = 2 | Control (Cmd on macOS browsers): click adds or removes berths from the selection (like Shift). |
| `Alt` = 4 | Alt: in the designer, turns snapping off while held. |

<a id="marinainputcontroller"></a>
### MarinaInputController

`sealed class MarinaInputController`

Turns raw, platform-neutral pointer and keyboard input into camera moves, hover, clicks and selection. Every host view (WinForms, Blazor, ...) just forwards its native events here.

Defaults: left-drag pans (map-style), right-drag orbits, middle-drag pans, Shift+left-drag orbits, wheel zooms toward the cursor, click selects and shows the tooltip, Ctrl+click or Shift+click adds/removes berths, right-click opens the actions window, double-click focuses a berth, Escape closes the popup and then clears the selection, Home resets the view. The popup stays anchored above its berth while the camera moves. While `MarinaDesigner.IsActive` is true, clicks, double-clicks, pointer movement and Enter/Backspace/Delete/Escape go to the designer instead (camera dragging, the wheel and navigation keys work as usual).

| Member | Description |
|---|---|
| `CameraDragAction LeftDragAction { get; set; }` | What left-dragging does (default `CameraDragAction.Pan`; Shift swaps pan and orbit). |
| `CameraDragAction RightDragAction { get; set; }` | What right-dragging does (default `CameraDragAction.Orbit`). A right-click without dragging opens the actions window. |
| `CameraDragAction MiddleDragAction { get; set; }` | What middle-dragging does (default `CameraDragAction.Pan`). |
| `float OrbitDegreesPerPixel { get; set; }` | Orbit speed for drags, in degrees per pixel (default 0.3). |
| `float ZoomStepFactor { get; set; }` | Zoom factor applied per wheel notch. |
| `float ClickTolerancePixels { get; set; }` | Movement (pixels) below which a press/release counts as a click rather than a drag. |
| `float KeyboardPanMeters { get; set; }` | Distance an arrow/WASD key press pans, in meters (default 10). |
| `float KeyboardOrbitDegrees { get; set; }` | Angle a Shift+arrow or PageUp/PageDown press orbits or tilts, in degrees (default 10). |
| `bool HoverEnabled { get; set; }` | Hit-test on pointer move to highlight the berth under the cursor. |
| `bool IsDragging { get; }` | True while a button is held and the pointer has moved past `MarinaInputController.ClickTolerancePixels`. |
| `void DoubleClick(float x, float y, PointerButton button, InputModifiers modifiers = InputModifiers.None)` | Forward a double-click. A left double-click on a berth focuses the camera on it (at `DefaultFocusAngle`). |
| `bool KeyDown(MarinaKey key, InputModifiers modifiers = InputModifiers.None)` | Returns true when the key was handled. |
| `void PointerDown(float x, float y, PointerButton button, InputModifiers modifiers = InputModifiers.None)` | Forward a mouse/pointer press. Coordinates are view pixels, origin top-left. |
| `void PointerLeave()` | Forward the pointer leaving the view: ends any drag and clears the hover. |
| `void PointerMove(float x, float y, InputModifiers modifiers = InputModifiers.None)` | Forward pointer movement: hovers when no button is held, otherwise pans or orbits. |
| `void PointerUp(float x, float y, PointerButton button, InputModifiers modifiers = InputModifiers.None)` | Forward a button release. Without a drag this is a click: selection, tooltip or actions window. |
| `void Wheel(float notches, float x, float y)` | Wheel input in notches: positive zooms in. |

<a id="marinakey"></a>
### MarinaKey

`enum MarinaKey`

Platform-neutral keys the viewer responds to. Host views map native keys to these.

| Value | Description |
|---|---|
| `Left` = 0 | Pan left (orbit with Shift). WinForms/Blazor map Left arrow and A. |
| `Right` = 1 | Pan right (orbit with Shift). Mapped from Right arrow and D. |
| `Up` = 2 | Pan forward (tilt with Shift). Mapped from Up arrow and W. |
| `Down` = 3 | Pan back (tilt with Shift). Mapped from Down arrow and S. |
| `PageUp` = 4 | Tilt the camera up. |
| `PageDown` = 5 | Tilt the camera down. |
| `ZoomIn` = 6 | Zoom in. Mapped from + and =. |
| `ZoomOut` = 7 | Zoom out. Mapped from - and _. |
| `Home` = 8 | Reset the camera to the overview. |
| `Escape` = 9 | Close the popup; pressed again, clear the selection. In the designer: cancel the drawing, then return to navigation. |
| `Enter` = 10 | Designer: finish the drawing in progress. |
| `Backspace` = 11 | Designer: remove the last placed point. |
| `Delete` = 12 | Designer: with the erase tool, remove the element under the pointer. |
| `Undo` = 13 | Designer: undo the last change (`MarinaDesigner.Undo`). Hosts map Ctrl+Z. |

<a id="pointerbutton"></a>
### PointerButton

`enum PointerButton`

Platform-neutral mouse button, as forwarded by host views to `MarinaInputController`.

| Value | Description |
|---|---|
| `None` = 0 | No button (hover, or an event raised by an API call). |
| `Left` = 1 | Primary button: select, show the tooltip, pan by dragging. |
| `Middle` = 2 | Middle button or wheel press: pan by dragging. |
| `Right` = 3 | Secondary button: open the actions window, orbit by dragging. |

## VirtualMarina.Core.Mathematics

<a id="marinamath"></a>
### MarinaMath

`static class MarinaMath`

Math helpers shared by the domain, camera, picking and scene code.

World space is right-handed and Y-up. The water surface is the plane Y = 0. Plan-view (2D) positions use `Vector2` where X = world X and Y = world Z. Headings are in degrees, rotating about +Y: 0° points along +Z, 90° points along +X.

| Member | Description |
|---|---|
| `const float DegToRad = 0.017453292f` | Multiply degrees by this to get radians. |
| `const float RadToDeg = 57.295776f` | Multiply radians by this to get degrees. |
| `static float Clamp01(float value)` | Clamps to 0..1. |
| `static Matrix4x4 CreatePerspectiveGL(float fieldOfViewRadians, float aspect, float near, float far)` | OpenGL-style perspective projection (clip Z in [-1, 1]) in System.Numerics row-vector convention. Its 16 fields, uploaded in M11..M44 order, form the column-major matrix GLSL expects. |
| `static Matrix4x4 CreatePlacement(Vector3 scale, float headingDegrees, Vector3 translation)` | Scale, then rotate about Y by the heading, then translate. |
| `static float DeltaAngle(float from, float to)` | Shortest signed angle (degrees) that rotates `from` onto `to`. |
| `static float DirectionToHeading(Vector2 direction)` | Heading in degrees (−180..180) of a plan-view direction; the inverse of `MarinaMath.HeadingToDirection`. |
| `static Vector2 HeadingToDirection(float headingDegrees)` | Unit plan-view direction for a heading. |
| `static Vector2 HeadingToRight(float headingDegrees)` | Unit plan-view vector for heading + 90°: `(cos h, −sin h)`, the local +X axis of placements made with `MarinaMath.CreatePlacement`. For heading 0° (+Z) it is +X. |
| `static float StableHash01(string value)` | Deterministic hash of a string mapped to [0, 1). Stable across processes (unlike string.GetHashCode). |
| `static Vector2 ToPlan(Vector3 world)` | World point to plan position (X, Z), dropping the height. |
| `static Vector3 ToWorld(Vector2 planPosition, float y = 0f)` | Plan position (X, Z) to a world point at height `y`. |

<a id="polygonmath"></a>
### PolygonMath

`static class PolygonMath`

Plan-view polygon helpers (point lists in plan coordinates, X = world X, Y = world Z).

| Member | Description |
|---|---|
| `static bool Contains(IReadOnlyList<Vector2> points, Vector2 point)` | True when the point lies inside the polygon (even-odd rule). Points exactly on an edge may go either way. |
| `static float DistanceToBoundary(IReadOnlyList<Vector2> points, Vector2 point)` | Shortest distance from the point to the polygon's outline. |
| `static ValueTuple<Vector2, Vector2> GetBounds(IReadOnlyList<Vector2> points)` | Axis-aligned bounds of the points (zero box when empty). |
| `static bool IsSimple(IReadOnlyList<Vector2> points)` | True when no two non-adjacent edges cross and no edge has zero length. |
| `static float SignedArea(IReadOnlyList<Vector2> points)` | Signed area (shoelace formula on plan X and Y). Positive when the points run counter-clockwise in the X/Y plane (−X to +X, then toward +Y); negative for the opposite direction. |
| `static IReadOnlyList<ValueTuple<int, int, int>> Triangulate(IReadOnlyList<Vector2> points)` | Splits a simple polygon (convex or concave, either winding) into triangles by ear clipping. Returns index triples into `points`, each wound counter-clockwise in the X/Y plane. |

## VirtualMarina.Core.Picking

<a id="berthhit"></a>
### BerthHit

`readonly record struct BerthHit`

Result of a berth hit test (`IMarinaVisualizer.HitTest`).

| Member | Description |
|---|---|
| `BerthHit(string BerthId, float Distance, Vector3 WorldPoint, bool HitBoat)` | Result of a berth hit test (`IMarinaVisualizer.HitTest`). |
| `string BerthId { get; init; }` | Berth that was hit. For a boat spanning several berths, the member berth nearest the hit point. |
| `float Distance { get; init; }` | Distance from the camera along the ray. |
| `Vector3 WorldPoint { get; init; }` | World-space point that was hit. |
| `bool HitBoat { get; init; }` | True when the boat was hit rather than the berth's water area. |

<a id="ray"></a>
### Ray

`readonly record struct Ray`

A half-line in world space, e.g. from the camera through a pixel (`OrbitCamera.ScreenPointToRay`).

| Member | Description |
|---|---|
| `Ray(Vector3 Origin, Vector3 Direction)` | A half-line in world space, e.g. from the camera through a pixel (`OrbitCamera.ScreenPointToRay`). |
| `Vector3 Origin { get; init; }` | Start point. |
| `Vector3 Direction { get; init; }` | Direction (normalized when produced by the camera). |
| `Vector3 GetPoint(float distance)` | The point at `distance` along the ray. |
| `bool IntersectHorizontalPlane(float height, out float distance)` | Intersects the horizontal plane at `height`, in front of the origin only. |

## VirtualMarina.Core.Rendering

<a id="colorrgba"></a>
### ColorRgba

`readonly record struct ColorRgba`

Linear RGBA color with components in 0..1.

| Member | Description |
|---|---|
| `ColorRgba(float R, float G, float B, float A = 1f)` | Linear RGBA color with components in 0..1. |
| `float R { get; init; }` | Red. |
| `float G { get; init; }` | Green. |
| `float B { get; init; }` | Blue. |
| `float A { get; init; }` | Alpha (1 = opaque). |
| `static ColorRgba FromBytes(byte r, byte g, byte b, byte a = 255)` | Creates a color from 0–255 components. |
| `static ColorRgba FromHex(string hex)` | Parses `#RGB`, `#RRGGBB` or `#RRGGBBAA`. |
| `ColorRgba Lerp(ColorRgba other, float t)` | Linear blend toward `other` (t = 0 this color, t = 1 the other). |
| `string ToHex()` | Formats as `#RRGGBB` (alpha is dropped). |
| `Vector3 ToVector3()` | RGB as a vector. |
| `Vector4 ToVector4()` | RGBA as a vector. |
| `ColorRgba WithAlpha(float alpha)` | A copy with a different alpha. |

<a id="iscenerenderer"></a>
### ISceneRenderer

`interface ISceneRenderer : IDisposable`

The only contract a graphics backend must implement. The core library works out what to draw (`RenderFrame`); a backend (desktop OpenGL, WebGL, and later Vulkan, WebGPU, etc.) decides how.

Backend responsibilities: Upload every mesh in `RenderFrame.Meshes` (by id) and re-upload when `RenderFrame.MeshLibraryVersion` changes. Draw opaque objects, then the water grid with the water shader, then transparent objects with blending and depth writes off. Apply the frame uniforms (camera, lighting, water, time) to the shaders in `ShaderSources`. All calls happen on the thread that owns the graphics context.

| Member | Description |
|---|---|
| `string BackendName { get; }` | Human-readable backend name, e.g. "OpenGL 3.3 Core (OpenTK)". |
| `string? DeviceDescription { get; }` | The graphics device actually in use once `ISceneRenderer.Initialize` has run, e.g. "NVIDIA GeForce RTX 3060 — OpenGL 4.6.0", or null when the backend cannot report one. Hosts show it in a status bar or an About box. |
| `void Initialize()` | Compiles shaders and sets up global state. The graphics context must be current. |
| `void Render(RenderFrame frame)` | Draws one frame produced by `MarinaVisualizer.BuildRenderFrame()`. |
| `void Resize(int pixelWidth, int pixelHeight)` | Framebuffer size in device pixels. |

<a id="labelstyle"></a>
### LabelStyle

`sealed class LabelStyle : StyleSection`

Colors of berth names written on the water (`MarinaStyle.Labels`; see `BerthLabelMode`).

| Member | Description |
|---|---|
| `LabelStyle()` | Creates an instance with default values. |
| `LabelFont FontFamily { get; set; }` | The face berth labels are set in. These are built-in stroke faces rather than system typefaces, so the choice is between a few weights and widths (see `LabelFont`). |
| `ColorRgba Color { get; set; }` | Normal label color. |
| `ColorRgba HighlightColor { get; set; }` | Label of a hovered or selected berth. |
| `ColorRgba DisabledColor { get; set; }` | Label of a disabled berth. |

<a id="landstyle"></a>
### LandStyle

`sealed class LandStyle : StyleSection`

Colors of land areas and their trees (`MarinaStyle.Land`). Changing them rebuilds the land meshes.

| Member | Description |
|---|---|
| `LandStyle()` | Creates an instance with default values. |
| `ColorRgba QuayColor { get; set; }` | Top of quays (light concrete). |
| `ColorRgba QuayWallColor { get; set; }` | Sides of quays. |
| `ColorRgba GrassColor { get; set; }` | Top of lawns. |
| `ColorRgba GrassBankColor { get; set; }` | Sides of lawns (earth). |
| `ColorRgba RockColor { get; set; }` | Average color of breakwater rocks. |
| `float RockColorVariation { get; set; }` | How much individual rocks vary in brightness, 0–0.5 (default 0.2). |
| `ColorRgba FoliageColor { get; set; }` | Crowns of broadleaf trees. |
| `ColorRgba ConiferColor { get; set; }` | Crowns of conifers. |
| `ColorRgba TrunkColor { get; set; }` | Tree trunks. |
| `ColorRgba PalmColor { get; set; }` | Fronds of palms. |
| `ColorRgba BlossomColor { get; set; }` | Blossom of cherry trees (`TreeShape.Cherry`). |
| `ColorRgba BuildingColor { get; set; }` | Walls of the buildings in a `HinterlandScenery.Town` behind the shore. |
| `ColorRgba RoofColor { get; set; }` | Roofs of those buildings. |
| `bool ShowTrees { get; set; }` | Draw the trees of land areas (`LandArea.Trees`). Default true. |

<a id="lightingsettings"></a>
### LightingSettings

`sealed class LightingSettings`

Directional sun, ambient light, specular and distance fog. Changes apply on the next frame.

| Member | Description |
|---|---|
| `LightingSettings()` | Creates an instance with default values. |
| `Vector3 SunDirection { get; set; }` | Unit vector pointing toward the sun. |
| `Vector3 SunColor { get; set; }` | Sunlight color and intensity (RGB, linear). |
| `Vector3 AmbientColor { get; set; }` | Ambient light color (RGB, linear). |
| `float SpecularStrength { get; set; }` | Strength of specular highlights on objects (0 = matte). |
| `float Shininess { get; set; }` | Specular exponent; higher values give tighter highlights. |
| `Vector3 SkyColor { get; set; }` | Sky color reflected by the water at grazing angles. |
| `Vector3 FogColor { get; set; }` | Horizon/fog color; also used as the clear color. |
| `float FogDensity { get; set; }` | Squared-exponential fog density per meter. 0 disables fog. |
| `void SetSunAngles(float azimuthDegrees, float elevationDegrees)` | Sets the sun position from compass azimuth (0° = +Z) and elevation above the horizon. |

<a id="marinastyle"></a>
### MarinaStyle

`sealed class MarinaStyle`

Everything that controls how the marina looks and moves: lighting, water and waves, status colors and boat opacities, land and trees, piers, labels, selection highlights and the camera. Available as `MarinaVisualizer.Style` (and `MarinaViewControl.Style` / `<MarinaView MarinaStyle="...">`).

Change properties at any time; the view updates on the next frame. Assign a whole new instance to switch themes. Sections that affect geometry (`MarinaStyle.Land`) rebuild the affected meshes when they change.

| Member | Description |
|---|---|
| `MarinaStyle()` | Creates an instance with default values. |
| `LightingSettings Lighting { get; init; }` | Sun, ambient light, specular highlights, sky and fog. Applied every frame. |
| `WaterSettings Water { get; init; }` | Water colors, waves (height, length, speed), reflections, ripples, sun glints and how much boats move. Applied every frame. |
| `StatusColorScheme Status { get; init; }` | Status colors, pad and boat opacities per status, disabled color and status markers (buoys and posts). |
| `LandStyle Land { get; init; }` | Colors of quays, lawns, breakwater rocks and trees, and whether trees are shown. |
| `StructureStyle Piers { get; init; }` | Colors of wooden and concrete piers, floats, fenders, bollards, piles, booms and service pedestals. |
| `LabelStyle Labels { get; init; }` | Colors of berth names written on the water. |
| `SelectionStyle Selection { get; init; }` | Selection marker and hover/selection highlights. |
| `ViewStyle View { get; init; }` | Camera field of view and animation smoothing. |
| `static MarinaStyle CreateDefault()` | A copy of the defaults. |

<a id="referenceimagelayer"></a>
### ReferenceImageLayer

`sealed record ReferenceImageLayer`

A reference image laid flat in the scene, north (the top row) toward −Z.

| Member | Description |
|---|---|
| `ReferenceImageLayer(ReferenceImage Image, Vector2 Min, Vector2 Max, float Height, float Opacity, bool AboveScene)` | A reference image laid flat in the scene, north (the top row) toward −Z. |
| `ReferenceImage Image { get; init; }` | The picture; upload a texture once per `ReferenceImage.Key`. |
| `Vector2 Min { get; init; }` | Plan-view corner at the image's top-left pixel (north-west): smallest X and Z. |
| `Vector2 Max { get; init; }` | Plan-view corner at the image's bottom-right pixel (south-east): largest X and Z. |
| `float Height { get; init; }` | World Y of the image plane (just above the highest wave). |
| `float Opacity { get; init; }` | 0 = invisible, 1 = opaque (uniform `uOpacity`). |
| `bool AboveScene { get; init; }` | True: draw without depth testing, over piers, land and boats. False: depth-tested, so land and structures hide it. |

<a id="renderanimation"></a>
### RenderAnimation

`[Flags] enum RenderAnimation`

Per-object animations evaluated on the GPU (uniform `uAnimation`), so an animated scene needs no per-frame CPU updates.

| Value | Description |
|---|---|
| `None` = 0 | Static. |
| `FloatOnWater` = 1 | Rise, fall, roll and pitch with the water surface at the object's origin. |
| `SpinAndBob` = 2 | Spin about Y and bob up and down (selection marker). |
| `Pulse` = 4 | Pulse the emissive highlight over time. |
| `AboveWaves` = 8 | Lift the object by the highest height the waves can reach (see `ShaderSources.MaxWaveHeightFactor`), so the water never covers it. Used for text on the water. |

<a id="renderframe"></a>
### RenderFrame

`sealed class RenderFrame`

Everything a backend needs to draw one frame. Matrices use the System.Numerics row-vector convention; uploading the fields in M11..M44 order produces the column-major matrices GLSL expects.

| Member | Description |
|---|---|
| `RenderFrame()` | Creates an instance with default values. |
| `Matrix4x4 View { get; init; }` | Camera view matrix (uniform `uView`). |
| `Matrix4x4 Projection { get; init; }` | OpenGL-style projection matrix (uniform `uProjection`). |
| `Vector3 CameraPosition { get; init; }` | Eye position in world space (uniform `uCameraPos`). |
| `float Time { get; init; }` | Seconds since the visualizer was created. Drives waves, bobbing and pulses in the shaders. |
| `LightingSettings Lighting { get; init; }` | Sun, ambient, specular and fog uniforms. |
| `WaterSettings Water { get; init; }` | Water color and wave uniforms. |
| `IReadOnlyList<RenderObject> Objects { get; init; }` | Scene objects. Draw the opaque ones, then the water, then the transparent ones (see `RenderObject.IsTransparent`). |
| `int SceneVersion { get; init; }` | Incremented whenever `RenderFrame.Objects` changes; lets backends skip re-uploading instance data. |
| `Vector2 MarinaCenter { get; init; }` | Middle of the marina in plan coordinates, which the water shader uses to tell the open sea from the water among the piers: white crests break offshore and run in toward this point. |
| `MeshLibrary Meshes { get; init; }` | Meshes referenced by `RenderFrame.Objects`. |
| `int MeshLibraryVersion { get; }` | `MeshLibrary.Version`; re-upload meshes when it changes. |
| `ReferenceImageLayer? ReferenceImage { get; init; }` | The designer's reference image, or null when none is shown. Draw it after the water and before the transparent objects (so drawing previews stay on top), with the `ShaderSources.ImageVertex` / `ShaderSources.ImageFragment` program. |

<a id="renderobject"></a>
### RenderObject

`readonly record struct RenderObject`

One mesh instance.

| Member | Description |
|---|---|
| `RenderObject(int MeshId, Matrix4x4 World, Vector4 Tint, float Emissive = 0f, RenderAnimation Animation = RenderAnimation.None, float Phase = 0f, float Desaturation = 0f)` | One mesh instance. |
| `int MeshId { get; init; }` | Id in `MeshLibrary`. |
| `Matrix4x4 World { get; init; }` | Model-to-world transform. |
| `Vector4 Tint { get; init; }` | Multiplied with vertex colors. Alpha below 1 renders in the transparent pass. |
| `float Emissive { get; init; }` | 0..1 highlight blend toward a brightened base color. |
| `RenderAnimation Animation { get; init; }` | GPU-side animation flags. |
| `float Phase { get; init; }` | Per-object animation phase offset in radians, so objects don't move in lockstep. |
| `float Desaturation { get; init; }` | 0 = full color, 1 = grayscale (used for disabled berths and their boats). |
| `bool IsTransparent { get; }` | True when the tint alpha is below 1: draw in the transparent pass (after the water, blending on, depth writes off). |

<a id="selectionstyle"></a>
### SelectionStyle

`sealed class SelectionStyle : StyleSection`

Selection marker and highlight strengths (`MarinaStyle.Selection`).

| Member | Description |
|---|---|
| `SelectionStyle()` | Creates an instance with default values. |
| `bool ShowMarker { get; set; }` | Show the spinning marker above selected berths. Default true. |
| `ColorRgba MarkerTint { get; set; }` | Multiplied with the marker's gold color (white keeps it gold). |
| `float MarkerScale { get; set; }` | Marker size multiplier, 0.2–5 (default 1). |
| `float SelectedGlow { get; set; }` | Glow of selected pads and boats, 0–1 (default 0.45). |
| `float HoverGlow { get; set; }` | Glow of the hovered pad and boat, 0–1 (default 0.25). |
| `bool Pulse { get; set; }` | Pulse the glow of selected berths. Default true. |

<a id="shaderdialect"></a>
### ShaderDialect

`enum ShaderDialect`

GLSL flavor to generate.

| Value | Description |
|---|---|
| `DesktopGL33` = 0 | Desktop OpenGL 3.3 core profile (GLSL 330). |
| `WebGL2` = 1 | WebGL 2 / OpenGL ES 3.0 (GLSL ES 300). |

<a id="shadersources"></a>
### ShaderSources

`static class ShaderSources`

Shared GLSL for every OpenGL-family backend. The bodies are written in the common subset of GLSL 330 and GLSL ES 300; only the version/precision header differs. That keeps the waves, shading and GPU animation identical on desktop and in the browser.

Attributes: location 0 = position, 1 = normal, 2 = color. Frame uniforms: uView, uProjection, uCameraPos, uTime, uSunDirection, uSunColor, uAmbientColor, uSpecularStrength, uShininess, uSkyColor, uFogColor, uFogDensity, uWaterDeep, uWaterShallow, uWaveAmplitude, uWaveFrequency, uWaveSpeed, uSkyReflection, uRipples, uSunGlints (water) and uFloatMotion (model). Object uniforms: uModel, uTint, uEmissive, uDesaturation, uAnimation, uPhase.

| Member | Description |
|---|---|
| `static readonly float[] ImageQuadCorners` | The unit square drawn by the image program: two triangles as 6 (x, y) corners. |
| `static float MaxWaveHeightFactor { get; }` | Highest the water surface can ever rise, as a multiple of `WaterSettings.WaveAmplitude`: the sum of the component amplitudes. The rendered grid interpolates between vertex heights, so it can't exceed this either. |
| `static string ImageFragment(ShaderDialect dialect)` | Fragment shader for the reference image: samples `uImage` (texture unit 0) with `uOpacity`. |
| `static string ImageVertex(ShaderDialect dialect)` | Vertex shader for the reference image: attribute 0 is a unit-square corner (vec2, 0..1); uniforms `uImageMin`, `uImageMax` (plan X/Z corners) and `uImageHeight` place it, and the corner doubles as the texture coordinate. |
| `static string ModelFragment(ShaderDialect dialect)` | Fragment shader for all objects: lighting, tint, emissive highlight, desaturation and fog. |
| `static string ModelVertex(ShaderDialect dialect)` | Vertex shader for all objects: placement plus GPU animations (floating, spin, above-waves lift). |
| `static string WaterFragment(ShaderDialect dialect)` | Fragment shader for the water: fresnel sky reflection, ripples, sun glints and fog. |
| `static string WaterVertex(ShaderDialect dialect)` | Vertex shader for the water grid: displaces vertices by the wave function. |

<a id="structurestyle"></a>
### StructureStyle

`sealed class StructureStyle : StyleSection`

Colors of piers and their fittings (`MarinaStyle.Piers`). Seams, walers, curbs and columns are shades of these.

| Member | Description |
|---|---|
| `StructureStyle()` | Creates an instance with default values. |
| `ColorRgba WoodColor { get; set; }` | Deck of wooden piers and wooden finger piers. |
| `ColorRgba ConcreteColor { get; set; }` | Deck of concrete piers, pontoons and their finger piers. |
| `ColorRgba FloatColor { get; set; }` | Pontoon floats under wooden floating piers. |
| `ColorRgba FenderColor { get; set; }` | Rubber fenders of floating concrete piers. |
| `ColorRgba BollardColor { get; set; }` | Bollards and cleats. |
| `ColorRgba SteelColor { get; set; }` | Steel piles (pile dividers and finger pier ends next to concrete piers). |
| `ColorRgba BoomFloatColor { get; set; }` | Floats of boom dividers. |
| `ColorRgba BoomEndColor { get; set; }` | End floats of boom dividers. |
| `ColorRgba PedestalColor { get; set; }` | Body of the power/water pedestals along a pier (`Pier.Services`). |
| `ColorRgba PowerColor { get; set; }` | Top of pedestals that supply electricity. |
| `ColorRgba WaterColor { get; set; }` | Top (or band) of pedestals that supply water. |

<a id="stylesection"></a>
### StyleSection

`abstract class StyleSection`

Base of the style sections that notify the visualizer when they change.

| Member | Description |
|---|---|
| `protected StyleSection()` | Creates an instance with default values. |
| `event EventHandler? Changed` | A property of this section changed. |
| `static protected float Clamp(float value, float min, float max)` | Clamps to a range, treating NaN as `min`. |
| `protected void OnChanged()` | Raises `StyleSection.Changed`. |
| `protected void SetField<T>(ref T? field, T? value)` | Stores a value and raises `StyleSection.Changed` when it differs. |

<a id="viewstyle"></a>
### ViewStyle

`sealed class ViewStyle : StyleSection`

Camera optics and motion (`MarinaStyle.View`). Applied to `MarinaVisualizer.Camera` when they change.

| Member | Description |
|---|---|
| `ViewStyle()` | Creates an instance with default values. |
| `float FieldOfViewDegrees { get; set; }` | Vertical field of view in degrees, 15–100 (default 45). |
| `float CameraSmoothing { get; set; }` | How quickly the camera eases toward a new position; 0 jumps instantly (default 10). |

<a id="watersettings"></a>
### WaterSettings

`sealed class WaterSettings`

Water surface look and wave animation (`MarinaStyle.Water`). Applied every frame. `WaterSettings.Size` and `WaterSettings.GridResolution` are only read when the visualizer is created.

| Member | Description |
|---|---|
| `WaterSettings()` | Creates an instance with default values. |
| `float Size { get; init; }` | Edge length of the square water grid in meters. |
| `int GridResolution { get; init; }` | Cells per side of the water grid. Higher values give smoother waves. |
| `Vector3 DeepColor { get; set; }` | Water color in shade (RGB, linear). |
| `Vector3 ShallowColor { get; set; }` | Water color where the sun lights it (RGB, linear). |
| `float WaveAmplitude { get; set; }` | How big the waves are: base wave height in meters (default 0.08; 0 = flat water). |
| `float WaveFrequency { get; set; }` | How close together the waves are: multiplier on wave frequency (larger = shorter, choppier waves; default 1). |
| `float WaveSpeed { get; set; }` | How fast the waves travel: multiplier on wave speed. Set to 0 to freeze the water and floating boats (default 1). |
| `float SkyReflection { get; set; }` | Strength of the sky reflected on the water, 0–1 (default 1). These reflections form the bright, cloud-like patches that appear on the water toward the horizon and when seen from high above; lower it for a calmer, darker surface. |
| `float Ripples { get; set; }` | Strength of the small ripples that break up the reflections, 0–2 (default 1; 0 gives a smooth, glassy surface). |
| `float SunGlints { get; set; }` | Strength of the sparkling sun glints on the water, 0–2 (default 1). |
| `float Whitecaps { get; set; }` | How strongly white crests break on the open water, 0–1 (default 0.55; 0 turns them off). They appear only beyond `WaterSettings.WhitecapDistance` from the middle of the marina and run inward, so the sea offshore is alive while the water among the piers stays calm. They fade out as the waves flatten. |
| `float WhitecapDistance { get; set; }` | How far from the middle of the marina the white crests start, in meters (default 220). Nothing breaks nearer than this, so the marina itself never fills with foam. |
| `float BoatMotion { get; set; }` | How much boats, buoys and boom floats rise, fall and roll with the waves, 0–3 (default 1; 0 keeps them still while the water moves). |

## VirtualMarina.Core.Serialization

<a id="marinadocument"></a>
### MarinaDocument

`sealed class MarinaDocument`

A whole marina in one file: its layout (land areas, piers, dividers, berths and berths), how it is drawn and animated (water, waves, lighting, status colors, labels, camera) and the designer's tool settings. Saved as JSON by the designer and loaded by the host application, so personalization travels with the design instead of living in host code.

The format is `{ "format": "virtualmarina.marina", "formatVersion": "1.0", ... }`. Reading is deliberately forgiving so files survive version changes in both directions: Missing properties fall back to the current defaults, so files written by older versions keep working. Properties this version doesn't know are kept in `MarinaDocument.Extensions` (document level) or alongside the element they belong to, and written back out, so a newer file survives a round trip through an older application. Unknown enum names fall back to the default instead of throwing. A file whose major version is newer than `MarinaDocument.CurrentVersion` is refused unless you pass `allowNewerVersion`: its meaning can't be guessed. Use `MarinaDocument.Extensions` for your own data (ERP ids, tariffs, ...); those keys are never touched by the library.

| Member | Description |
|---|---|
| `MarinaDocument()` | Creates an instance with default values. |
| `const string FormatName = "virtualmarina.marina"` | Value of the file's `format` property: `virtualmarina.marina`. |
| `const string FileExtension = ".marina.json"` | Recommended file extension, `.marina.json`. |
| `const string FileDialogFilter = "Marina design (*.marina.json)\|*.marina.json\|JSON files (*.json)\|*.json\|All files (*.*)\|*.*"` | File filter for open/save dialogs. |
| `static Version CurrentVersion { get; }` | The version of the format this build writes (2.0). |
| `string Name { get; set; }` | Name of the marina, shown in titles and stored as `MarinaLayout.Name`. |
| `string? Description { get; set; }` | Free-text description of the design (not used by the renderer). |
| `MarinaLayout Layout { get; set; }` | The marina itself: land areas, piers, dividers, berths and multi-berths. |
| `MarinaStyle Style { get; set; }` | How the marina is drawn and animated: water and waves, lighting, status colors, land, piers, labels, selection, camera optics. |
| `BerthLabelMode BerthLabels { get; set; }` | Which berths show their name on the water. |
| `CameraPose? Camera { get; set; }` | Where the camera should start, or null to use the automatic overview. |
| `IReadOnlyList<CameraPreset> CameraPresets { get; set; }` | Named viewpoints saved with the design, offered by the host as "go to this view". The ones generated from the layout (Overview, Top Down, one per pier) are not stored: they are rebuilt from the piers on load, so they stay right when the marina changes. |
| `DesignerSettings? Designer { get; set; }` | The designer's tool settings when the file was saved, so a design reopens the way it was left. Null when not stored. |
| `ReferenceImageRecord? ReferenceImage { get; set; }` | The picture the marina was traced on, with its place and scale, or null. Stored so a design can be reopened and corrected against the same photo later; see `ReferenceImageRecord`. |
| `Version Version { get; }` | Version of the format the document was read from; `MarinaDocument.CurrentVersion` for a new one. |
| `string? Generator { get; set; }` | What wrote the file, e.g. "VirtualMarina Designer 1.0". |
| `DateTimeOffset? SavedUtc { get; set; }` | When the file was written. |
| `IDictionary<string, JsonElement> Extensions { get; }` | Anything else stored in the file: your own sections, and sections written by a newer version of the format. Keys here are written back exactly as they came in. |
| `bool IsFromNewerVersion { get; }` | True when the file came from a newer minor version, so it may contain settings this build ignores. |
| `void ApplyTo(MarinaVisualizer marina, bool applyStyle = true, bool applyCamera = true, bool applyDesignerSettings = true, bool applyReferenceImage = true)` | Loads the document into a visualizer: the style and label mode first (so the water grid is built at the stored size), then the layout, then the camera and the designer's settings. |
| `static MarinaDocument FromVisualizer(MarinaVisualizer marina, string? generator = null, bool includeCamera = true, bool includeDesignerSettings = true, bool includeReferenceImage = true)` | Captures a visualizer: its layout, style, label mode, camera and (optionally) the designer's settings. |
| `T? GetExtension<T>(string key)` | Reads back data stored with `MarinaDocument.SetExtension` (or written by another application), or the default when missing or unreadable. |
| `static MarinaDocument Load(string path, bool allowNewerVersion = false)` | Reads a document from a file. |
| `static MarinaDocument Parse(string json, bool allowNewerVersion = false)` | Reads a document from JSON. |
| `void Save(string path, bool indented = true)` | Writes the document to a file (UTF-8). |
| `void SetExtension<T>(string key, T? value)` | Stores your own data in the file under `key` (serialized with the marina JSON settings). |
| `string ToJson(bool indented = true)` | Writes the document as JSON. |
| `IReadOnlyList<string> Validate()` | Errors in the stored layout, empty when it is valid (the same checks `InitializeLayout` makes). |

<a id="marinaformatexception"></a>
### MarinaFormatException

`sealed class MarinaFormatException : Exception`

A marina file could not be read: it is not JSON, not a marina file, or comes from a newer major version of the format.

| Member | Description |
|---|---|
| `MarinaFormatException(string message)` | Creates the exception. |
| `MarinaFormatException(string message, Exception innerException)` | Creates the exception from a lower-level failure. |

<a id="marinajson"></a>
### MarinaJson

`static class MarinaJson`

The JSON settings `MarinaDocument` reads and writes with: camelCase names, compact `[x, y]` vectors, hex colors and forgiving enums. Reuse them when you store marina data of your own.

Reading is deliberately tolerant, because a file may come from a newer version of the format: unknown properties are kept aside (see `MarinaDocument.Extensions`), unknown enum names fall back to the default, numbers may be quoted, and comments and trailing commas are allowed. Serialization is source-generated, so it keeps working in trimmed and WebAssembly builds.

| Member | Description |
|---|---|
| `static JsonSerializerOptions Options { get; }` | The options marina files are written with (indented). |
| `static JsonSerializerOptions CompactOptions { get; }` | The same options without indentation, for compact storage (e.g. a database column). |

<a id="referenceimagerecord"></a>
### ReferenceImageRecord

`sealed record ReferenceImageRecord`

The tracing image stored inside a marina file: the picture itself plus where it sits and how it is shown, so a design reopens on the same photo, at the same place and the same scale, months later.

The picture is stored as the original PNG/JPEG file, not as pixels, so it costs roughly what the file costs on disk. A `ReferenceImage` can only be written when it still knows those bytes (`ReferenceImage.CanBeSaved`); one built from raw pixels alone is skipped rather than bloating the file with a bitmap. The measuring line is deliberately not part of this: it is scaffolding for calibrating the picture, and once the scale is right it has no meaning.

| Member | Description |
|---|---|
| `ReferenceImageRecord()` | Creates an instance with default values. |
| `ReferenceImage Image { get; init; }` | The picture, with its original file bytes. |
| `Vector2 Center { get; init; }` | Center of the picture in plan coordinates. |
| `float MetersPerPixel { get; init; }` | Ground size of one pixel, in meters: the scale the picture was calibrated to. |
| `float Opacity { get; init; }` | How solid the picture is drawn, 0–1. |
| `bool Visible { get; init; }` | Whether the picture is shown at all. |
| `bool AboveScene { get; init; }` | Whether it is drawn over land and piers rather than under them. |
| `void ApplyTo(MarinaDesigner designer)` | Puts the picture back under a designer, where it was and at the scale it was. |
| `static ReferenceImageRecord? FromDesigner(MarinaDesigner designer)` | Reads the tracing image out of a designer, or null when there is none worth storing. |

## VirtualMarina.Rendering.OpenGL

<a id="openglscenerenderer"></a>
### OpenGlSceneRenderer

`sealed class OpenGlSceneRenderer : ISceneRenderer, IDisposable`

`ISceneRenderer` for desktop OpenGL 3.3 core profile.

The host must make its GL context current before calling any member (including `OpenGlSceneRenderer.Dispose`) and must have loaded OpenTK's bindings (OpenTK's GLControl and GameWindow do this automatically). The designer's reference image must carry decoded RGBA pixels (`ReferenceImage.Rgba`); encoded-only images are skipped.

| Member | Description |
|---|---|
| `OpenGlSceneRenderer()` | Creates an instance with default values. |
| `string BackendName { get; }` | *(See the interface member.)* |
| `string? DeviceDescription { get; }` | GL_RENDERER / GL_VERSION of the active context, available after `OpenGlSceneRenderer.Initialize`. |
| `bool IsInitialized { get; }` | True once shaders are compiled. |
| `void Dispose()` | Deletes GPU meshes and shader programs. The GL context must be current. |
| `void Initialize()` | *(See the interface member.)* |
| `void Render(RenderFrame frame)` | *(See the interface member.)* |
| `void Resize(int pixelWidth, int pixelHeight)` | *(See the interface member.)* |

## VirtualMarina.WinForms

<a id="marinadesignerpanel"></a>
### MarinaDesignerPanel

`sealed class MarinaDesignerPanel : UserControl`

Ready-made tool panel for `MarinaDesigner`: design mode, tools (land area, pier, berths, land berths, trees, erase), their settings, undo, and the reference image (load, opacity, move, scale calibration). Put it next to a `MarinaViewControl` and set `MarinaDesignerPanel.Marina`.

| Member | Description |
|---|---|
| `MarinaDesignerPanel()` | Creates the panel. Set `MarinaDesignerPanel.Marina` to connect it. |
| `MarinaVisualizer? Marina { get; set; }` | The visualizer whose `MarinaVisualizer.Designer` this panel drives. |
| `event EventHandler<ThreadExceptionEventArgs>? ImageLoadFailed` | Raised when loading an image file fails (the default shows a message box). |
| `protected override void Dispose(bool disposing)` | Unsubscribes from the designer. |

<a id="marinaviewcontrol"></a>
### MarinaViewControl

`sealed class MarinaViewControl : UserControl`

Drop-in WinForms control that renders a `MarinaVisualizer` with OpenGL, and shows the selection tooltip / actions window above the selected berth.

| Member | Description |
|---|---|
| `MarinaViewControl()` | Creates the control with its own empty `MarinaVisualizer` (available as `MarinaViewControl.Marina`). |
| `MarinaViewControl(MarinaVisualizer marina)` | Creates the control for an existing visualizer (e.g. one configured before the form is shown). |
| `MarinaVisualizer Marina { get; set; }` | The visualizer this control displays. Can be swapped at runtime; the control's Marina events follow the new instance. |
| `MarinaStyle Style { get; set; }` | How the marina is drawn and animated: lighting, waves, status colors, boat opacities, land and trees, piers, labels, selection and camera. The same object as `Marina.Style`; change its properties or assign a new `MarinaStyle`. |
| `int FrameIntervalMilliseconds { get; set; }` | Delay between frames in milliseconds (the Windows timer resolution is about 15 ms). |
| `bool Animate { get; set; }` | Pauses or resumes the render loop. |
| `string RendererDescription { get; }` | Backend and GPU description after the first frame, e.g. for a status bar. |
| `event EventHandler<ThreadExceptionEventArgs>? RenderError` | Raised if OpenGL initialization or rendering fails (e.g. no OpenGL 3.3 driver). |
| `event EventHandler<BerthEventArgs>? BerthClicked` | A berth (its water area or its boat) was clicked or double-clicked with any mouse button. Raised after the selection change the click caused. Never raised for disabled berths. |
| `event EventHandler<BerthSelectedEventArgs>? BerthSelected` | A single berth was selected: by a left or right click (including on an already selected berth), through the API, or because the open popup's content needs refreshing (`BerthSelectedEventArgs.Reason`). Fill `BerthSelectedEventArgs.Tooltip` (pre-filled with berth and boat details) and `BerthSelectedEventArgs.Actions` (empty) to control the popup: a left click shows the tooltip, a right click shows the actions window. |
| `event EventHandler<MultiBerthSelectedEventArgs>? MultiBerthSelected` | Two or more berths are selected (Ctrl+click or Shift+click, right-click inside a multi-selection, or `IMarinaVisualizer.SetSelection`). Fill the tooltip and actions for the whole selection. |
| `event EventHandler<SelectionChangedEventArgs>? SelectionChanged` | The set of selected berths or the primary berth changed, including the selection being cleared. |
| `event EventHandler? SelectionCleared` | The selection became empty (raised right after `IMarinaVisualizer.SelectionChanged`). |
| `event EventHandler<BerthActionInvokedEventArgs>? BerthActionInvoked` | The user clicked an enabled action in the actions window (or `IMarinaVisualizer.InvokeBerthAction` was called). The window closes afterwards unless `BerthActionInvokedEventArgs.KeepPopupOpen` or `BerthAction.KeepOpen` is set. |
| `event EventHandler<BerthPopupChangedEventArgs>? PopupChanged` | The tooltip/actions popup opened, closed or changed content. Host views re-render `IMarinaVisualizer.ActivePopup`. |
| `event EventHandler<BerthHoverEventArgs>? BerthHoverChanged` | The berth under the pointer changed (null when the pointer left all berths). Disabled berths are never hovered. |
| `event EventHandler<BerthStatusChangedEventArgs>? BerthStatusChanged` | A berth's status or assigned boat changed, through any API (single updates, batches, berths). Raised once per berth, with the previous and current snapshots. |
| `event EventHandler<LayoutChangedEventArgs>? LayoutChanged` | Piers, berths, dividers, berths or land areas were added, updated or removed, or the layout was initialized or cleared. Inside `IMarinaVisualizer.BeginUpdate` or `IMarinaVisualizer.BatchUpdate` the notifications are coalesced into one `LayoutChangeKind.BatchUpdated`. |
| `protected override void Dispose(bool disposing)` | Stops rendering and releases GPU resources, the GL control and the popup. |
| `ReferenceImage LoadReferenceImage(string path, float? metersPerPixel = null)` | Loads an image file (PNG, JPEG, BMP, GIF or TIFF) and shows it as the designer's reference image, north at the top. |
| `protected override void OnHandleCreated(EventArgs e)` | Creates the OpenGL surface and starts the render loop once the window handle exists (not in the designer). |
| `protected override void OnPaint(PaintEventArgs e)` | In the designer (or if OpenGL is unavailable) draws a placeholder describing the 3D render area. |

<a id="referenceimageloader"></a>
### ReferenceImageLoader

`static class ReferenceImageLoader`

Decodes image files into `ReferenceImage`s for the designer (GDI+: PNG, JPEG, BMP, GIF, TIFF).

| Member | Description |
|---|---|
| `const int MaxDimension = 8192` | Largest width or height kept; bigger images are scaled down to stay within common GPU texture limits. |
| `static string FileDialogFilter { get; }` | File filter for an `OpenFileDialog`, with the descriptions in the current language. |
| `static ReferenceImage Decoded(ReferenceImage image)` | The same picture with pixels a desktop renderer can draw. A picture loaded from a marina file arrives as an undecoded PNG; this turns it into one the OpenGL backend can upload, keeping the bytes for the next save. Returns the image unchanged when it already has pixels. |
| `static ReferenceImage FromBytes(byte[] encodedData, string contentType = "image/png")` | Decodes image file bytes, keeping them on the result so it can be written to a marina file. This is also how a picture that came out of a design is made drawable: a desktop renderer needs pixels, not a PNG. |
| `static ReferenceImage FromFile(string path)` | Loads and decodes an image file, keeping the file itself so a design can store the picture. |
| `static ReferenceImage FromImage(Image image, byte[]? encodedData = null, string? contentType = null)` | Converts a GDI+ image to RGBA pixels (scaled down when larger than `ReferenceImageLoader.MaxDimension`). |
| `static ReferenceImage FromStream(Stream stream)` | Decodes an image from a stream. The bytes are kept, so the picture can be stored in a design. |

