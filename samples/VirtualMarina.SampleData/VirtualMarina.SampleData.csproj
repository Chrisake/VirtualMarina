<Project Sdk="Microsoft.NET.Sdk">

  <!-- Mock marina shared by both test hosts, so they exercise identical data. -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>VirtualMarina.SampleData</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\VirtualMarina.Core\VirtualMarina.Core.csproj" />
  </ItemGroup>

</Project>
